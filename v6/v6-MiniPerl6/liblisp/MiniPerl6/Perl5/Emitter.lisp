;; Do not edit this file - Generated by MiniPerl6
;; class CompUnit
(defpackage mp-CompUnit
  (:use common-lisp))
;; (in-package mp-CompUnit)
(if (not (ignore-errors (find-class 'mp-CompUnit)))
  (defclass mp-CompUnit () ()))

(let (x) 
  (setq x (make-instance 'mp-CompUnit))
  (defun proto-mp-CompUnit () x))
;; has $.name
(let ((new-slots (list (list :name 'sv-name
  :readers '(sv-name)
  :writers '((setf sv-name))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-CompUnit)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-CompUnit :direct-slots new-slots))

;; has $.attributes
(let ((new-slots (list (list :name 'sv-attributes
  :readers '(sv-attributes)
  :writers '((setf sv-attributes))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-CompUnit)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-CompUnit :direct-slots new-slots))

;; has $.methods
(let ((new-slots (list (list :name 'sv-methods
  :readers '(sv-methods)
  :writers '((setf sv-methods))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-CompUnit)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-CompUnit :direct-slots new-slots))

;; has $.body
(let ((new-slots (list (list :name 'sv-body
  :readers '(sv-body)
  :writers '((setf sv-body))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-CompUnit)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-CompUnit :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-CompUnit))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "package " (format nil "~{~a~}" (list (sv-name sv-self) (format nil "~{~a~}" (list ";" (format nil "~{~a~}" (list (sv-newline (proto-mp-Main) ) (format nil "~{~a~}" (list "sub new { shift; bless { @_ }, \"" (format nil "~{~a~}" (list (sv-name sv-self) (format nil "~{~a~}" (list "\" }" (format nil "~{~a~}" (list (sv-newline (proto-mp-Main) ) (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-body sv-self)) (format nil "~{~a~}" (list ";" (sv-newline (proto-mp-Main) )))) (format nil "~{~a~}" (list (sv-newline (proto-mp-Main) ) (sv-newline (proto-mp-Main) ))))))))))))))))))))))))




;; class Val::Int
(defpackage mp-Val-Int
  (:use common-lisp))
;; (in-package mp-Val-Int)
(if (not (ignore-errors (find-class 'mp-Val-Int)))
  (defclass mp-Val-Int () ()))

(let (x) 
  (setq x (make-instance 'mp-Val-Int))
  (defun proto-mp-Val-Int () x))
;; has $.int
(let ((new-slots (list (list :name 'sv-int
  :readers '(sv-int)
  :writers '((setf sv-int))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Val-Int)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Val-Int :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Val-Int))
  (block mp6-function
    (progn (sv-int sv-self))))




;; class Val::Bit
(defpackage mp-Val-Bit
  (:use common-lisp))
;; (in-package mp-Val-Bit)
(if (not (ignore-errors (find-class 'mp-Val-Bit)))
  (defclass mp-Val-Bit () ()))

(let (x) 
  (setq x (make-instance 'mp-Val-Bit))
  (defun proto-mp-Val-Bit () x))
;; has $.bit
(let ((new-slots (list (list :name 'sv-bit
  :readers '(sv-bit)
  :writers '((setf sv-bit))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Val-Bit)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Val-Bit :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Val-Bit))
  (block mp6-function
    (progn (sv-bit sv-self))))




;; class Val::Num
(defpackage mp-Val-Num
  (:use common-lisp))
;; (in-package mp-Val-Num)
(if (not (ignore-errors (find-class 'mp-Val-Num)))
  (defclass mp-Val-Num () ()))

(let (x) 
  (setq x (make-instance 'mp-Val-Num))
  (defun proto-mp-Val-Num () x))
;; has $.num
(let ((new-slots (list (list :name 'sv-num
  :readers '(sv-num)
  :writers '((setf sv-num))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Val-Num)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Val-Num :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Val-Num))
  (block mp6-function
    (progn (sv-num sv-self))))




;; class Val::Buf
(defpackage mp-Val-Buf
  (:use common-lisp))
;; (in-package mp-Val-Buf)
(if (not (ignore-errors (find-class 'mp-Val-Buf)))
  (defclass mp-Val-Buf () ()))

(let (x) 
  (setq x (make-instance 'mp-Val-Buf))
  (defun proto-mp-Val-Buf () x))
;; has $.buf
(let ((new-slots (list (list :name 'sv-buf
  :readers '(sv-buf)
  :writers '((setf sv-buf))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Val-Buf)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Val-Buf :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Val-Buf))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "\\'" (format nil "~{~a~}" (list (sv-buf sv-self) "\\'")))))))




;; class Val::Undef
(defpackage mp-Val-Undef
  (:use common-lisp))
;; (in-package mp-Val-Undef)
(if (not (ignore-errors (find-class 'mp-Val-Undef)))
  (defclass mp-Val-Undef () ()))

(let (x) 
  (setq x (make-instance 'mp-Val-Undef))
  (defun proto-mp-Val-Undef () x))
;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Val-Undef))
  (block mp6-function
    (progn "(undef)")))




;; class Val::Object
(defpackage mp-Val-Object
  (:use common-lisp))
;; (in-package mp-Val-Object)
(if (not (ignore-errors (find-class 'mp-Val-Object)))
  (defclass mp-Val-Object () ()))

(let (x) 
  (setq x (make-instance 'mp-Val-Object))
  (defun proto-mp-Val-Object () x))
;; has $.class
(let ((new-slots (list (list :name 'sv-class
  :readers '(sv-class)
  :writers '((setf sv-class))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Val-Object)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Val-Object :direct-slots new-slots))

;; has $.fields
(let ((new-slots (list (list :name 'sv-fields
  :readers '(sv-fields)
  :writers '((setf sv-fields))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Val-Object)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Val-Object :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Val-Object))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "bless(" (format nil "~{~a~}" (list (prin1-to-string (sv-fields sv-self)) (format nil "~{~a~}" (list ", " (format nil "~{~a~}" (list (prin1-to-string (sv-class sv-self)) ")")))))))))))




;; class Lit::Seq
(defpackage mp-Lit-Seq
  (:use common-lisp))
;; (in-package mp-Lit-Seq)
(if (not (ignore-errors (find-class 'mp-Lit-Seq)))
  (defclass mp-Lit-Seq () ()))

(let (x) 
  (setq x (make-instance 'mp-Lit-Seq))
  (defun proto-mp-Lit-Seq () x))
;; has $.seq
(let ((new-slots (list (list :name 'sv-seq
  :readers '(sv-seq)
  :writers '((setf sv-seq))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lit-Seq)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lit-Seq :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Lit-Seq))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-seq sv-self)) ", ") ")")))))))




;; class Lit::Array
(defpackage mp-Lit-Array
  (:use common-lisp))
;; (in-package mp-Lit-Array)
(if (not (ignore-errors (find-class 'mp-Lit-Array)))
  (defclass mp-Lit-Array () ()))

(let (x) 
  (setq x (make-instance 'mp-Lit-Array))
  (defun proto-mp-Lit-Array () x))
;; has $.array
(let ((new-slots (list (list :name 'sv-array
  :readers '(sv-array)
  :writers '((setf sv-array))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lit-Array)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lit-Array :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Lit-Array))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "[" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-array sv-self)) ", ") "]")))))))




;; class Lit::Hash
(defpackage mp-Lit-Hash
  (:use common-lisp))
;; (in-package mp-Lit-Hash)
(if (not (ignore-errors (find-class 'mp-Lit-Hash)))
  (defclass mp-Lit-Hash () ()))

(let (x) 
  (setq x (make-instance 'mp-Lit-Hash))
  (defun proto-mp-Lit-Hash () x))
;; has $.hash
(let ((new-slots (list (list :name 'sv-hash
  :readers '(sv-hash)
  :writers '((setf sv-hash))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lit-Hash)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lit-Hash :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Lit-Hash))
  (block mp6-function
    (let ((sv-fields nil)(sv-str nil)) (setf sv-fields (sv-hash sv-self))(setf sv-str "")(dolist (sv-field sv-fields) (progn (setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit (elt field 0) ) (format nil "~{~a~}" (list " => " (format nil "~{~a~}" (list (sv-emit (elt field 1) ) ",")))))))))))(format nil "~{~a~}" (list "{ " (format nil "~{~a~}" (list sv-str " }")))))))




;; class Lit::Code
(defpackage mp-Lit-Code
  (:use common-lisp))
;; (in-package mp-Lit-Code)
(if (not (ignore-errors (find-class 'mp-Lit-Code)))
  (defclass mp-Lit-Code () ()))

(let (x) 
  (setq x (make-instance 'mp-Lit-Code))
  (defun proto-mp-Lit-Code () x))
1



;; class Lit::Object
(defpackage mp-Lit-Object
  (:use common-lisp))
;; (in-package mp-Lit-Object)
(if (not (ignore-errors (find-class 'mp-Lit-Object)))
  (defclass mp-Lit-Object () ()))

(let (x) 
  (setq x (make-instance 'mp-Lit-Object))
  (defun proto-mp-Lit-Object () x))
;; has $.class
(let ((new-slots (list (list :name 'sv-class
  :readers '(sv-class)
  :writers '((setf sv-class))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lit-Object)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lit-Object :direct-slots new-slots))

;; has $.fields
(let ((new-slots (list (list :name 'sv-fields
  :readers '(sv-fields)
  :writers '((setf sv-fields))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lit-Object)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lit-Object :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Lit-Object))
  (block mp6-function
    (let ((sv-fields nil)(sv-str nil)) (setf sv-fields (sv-fields sv-self))(setf sv-str "")(dolist (sv-field sv-fields) (progn (setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit (elt field 0) ) (format nil "~{~a~}" (list " => " (format nil "~{~a~}" (list (sv-emit (elt field 1) ) ",")))))))))))(format nil "~{~a~}" (list (sv-class sv-self) (format nil "~{~a~}" (list "->new( " (format nil "~{~a~}" (list sv-str " )")))))))))




;; class Index
(defpackage mp-Index
  (:use common-lisp))
;; (in-package mp-Index)
(if (not (ignore-errors (find-class 'mp-Index)))
  (defclass mp-Index () ()))

(let (x) 
  (setq x (make-instance 'mp-Index))
  (defun proto-mp-Index () x))
;; has $.obj
(let ((new-slots (list (list :name 'sv-obj
  :readers '(sv-obj)
  :writers '((setf sv-obj))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Index)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Index :direct-slots new-slots))

;; has $.index
(let ((new-slots (list (list :name 'sv-index
  :readers '(sv-index)
  :writers '((setf sv-index))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Index)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Index :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Index))
  (block mp6-function
    (progn (format nil "~{~a~}" (list (sv-emit (sv-obj sv-self) ) (format nil "~{~a~}" (list "->[" (format nil "~{~a~}" (list (sv-emit (sv-index sv-self) ) "]")))))))))




;; class Lookup
(defpackage mp-Lookup
  (:use common-lisp))
;; (in-package mp-Lookup)
(if (not (ignore-errors (find-class 'mp-Lookup)))
  (defclass mp-Lookup () ()))

(let (x) 
  (setq x (make-instance 'mp-Lookup))
  (defun proto-mp-Lookup () x))
;; has $.obj
(let ((new-slots (list (list :name 'sv-obj
  :readers '(sv-obj)
  :writers '((setf sv-obj))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lookup)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lookup :direct-slots new-slots))

;; has $.index
(let ((new-slots (list (list :name 'sv-index
  :readers '(sv-index)
  :writers '((setf sv-index))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Lookup)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Lookup :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Lookup))
  (block mp6-function
    (progn (format nil "~{~a~}" (list (sv-emit (sv-obj sv-self) ) (format nil "~{~a~}" (list "->{" (format nil "~{~a~}" (list (sv-emit (sv-index sv-self) ) "}")))))))))




;; class Var
(defpackage mp-Var
  (:use common-lisp))
;; (in-package mp-Var)
(if (not (ignore-errors (find-class 'mp-Var)))
  (defclass mp-Var () ()))

(let (x) 
  (setq x (make-instance 'mp-Var))
  (defun proto-mp-Var () x))
;; has $.sigil
(let ((new-slots (list (list :name 'sv-sigil
  :readers '(sv-sigil)
  :writers '((setf sv-sigil))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Var)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Var :direct-slots new-slots))

;; has $.twigil
(let ((new-slots (list (list :name 'sv-twigil
  :readers '(sv-twigil)
  :writers '((setf sv-twigil))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Var)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Var :direct-slots new-slots))

;; has $.name
(let ((new-slots (list (list :name 'sv-name
  :readers '(sv-name)
  :writers '((setf sv-name))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Var)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Var :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Var))
  (block mp6-function
    (let ((sv-table nil)) (setf sv-table (let ((h (make-hash-table :test 'equal))) (setf (gethash "$" h) "$")(setf (gethash "@" h) "$List_")(setf (gethash "%" h) "$Hash_")(setf (gethash "&" h) "$Code_") h))(if (sv-bool (sv-eq (sv-twigil sv-self) ".")) (format nil "~{~a~}" (list "$self->{" (format nil "~{~a~}" (list (sv-name sv-self) "}")))) (if (sv-bool (sv-eq (sv-name sv-self) "/")) (format nil "~{~a~}" (list (gethash (sv-sigil sv-self) sv-table) "MATCH")) (format nil "~{~a~}" (list (gethash (sv-sigil sv-self) sv-table) (sv-name sv-self))))))))

;; method name
(if (not (ignore-errors (find-method 'sv-name () ())))
  (defgeneric sv-name (sv-self)
      (:documentation "a method")))
(defmethod sv-name ((sv-self mp-Var))
  (block mp6-function
    (progn (sv-name sv-self))))




;; class Bind
(defpackage mp-Bind
  (:use common-lisp))
;; (in-package mp-Bind)
(if (not (ignore-errors (find-class 'mp-Bind)))
  (defclass mp-Bind () ()))

(let (x) 
  (setq x (make-instance 'mp-Bind))
  (defun proto-mp-Bind () x))
;; has $.parameters
(let ((new-slots (list (list :name 'sv-parameters
  :readers '(sv-parameters)
  :writers '((setf sv-parameters))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Bind)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Bind :direct-slots new-slots))

;; has $.arguments
(let ((new-slots (list (list :name 'sv-arguments
  :readers '(sv-arguments)
  :writers '((setf sv-arguments))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Bind)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Bind :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Bind))
  (block mp6-function
    (progn (if (sv-bool (isa (sv-parameters sv-self) "Lit::Array")) (let ((sv-a nil)(sv-str nil)(sv-i nil)) (setf sv-a (sv-array (sv-parameters sv-self) ))(setf sv-str "do { ")(setf sv-i 0)(dolist (sv-var sv-a) (let ((sv-bind nil)) (setf sv-bind (let ((m (make-instance 'mp-Bind))) (setf (sv-parameters m) sv-var)(setf (sv-arguments m) (let ((m (make-instance 'mp-Index))) (setf (sv-obj m) (sv-arguments sv-self))(setf (sv-index m) (let ((m (make-instance 'mp-Val-Int))) (setf (sv-int m) sv-i) m)) m)) m))(setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list " " (format nil "~{~a~}" (list (sv-emit sv-bind ) "; ")))))))(setf sv-i (+ sv-i 1))))(return-from mp6-function (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit (sv-parameters sv-self) ) " }")))))) (progn ))(if (sv-bool (isa (sv-parameters sv-self) "Lit::Hash")) (let ((sv-a nil)(sv-b nil)(sv-str nil)(sv-i nil)(sv-arg nil)) (setf sv-a (sv-hash (sv-parameters sv-self) ))(setf sv-b (sv-hash (sv-arguments sv-self) ))(setf sv-str "do { ")(setf sv-i 0)(dolist (sv-var sv-a) (let ((sv-bind nil)) (setf sv-arg (let ((m (make-instance 'mp-Val-Undef)))  m))(dolist (sv-var2 sv-b) (progn (if (sv-bool (sv-eq (sv-buf (elt var2 0) ) (sv-buf (elt var 0) ))) (progn (setf sv-arg (elt var2 1))) (progn ))))(setf sv-bind (let ((m (make-instance 'mp-Bind))) (setf (sv-parameters m) (elt var 1))(setf (sv-arguments m) sv-arg) m))(setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list " " (format nil "~{~a~}" (list (sv-emit sv-bind ) "; ")))))))(setf sv-i (+ sv-i 1))))(return-from mp6-function (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit (sv-parameters sv-self) ) " }")))))) (progn ))(if (sv-bool (isa (sv-parameters sv-self) "Lit::Object")) (let ((sv-class nil)(sv-a nil)(sv-b nil)(sv-str nil)(sv-i nil)(sv-arg nil)) (setf sv-class (sv-class (sv-parameters sv-self) ))(setf sv-a (sv-fields (sv-parameters sv-self) ))(setf sv-b (sv-arguments sv-self))(setf sv-str "do { ")(setf sv-i 0)(dolist (sv-var sv-a) (let ((sv-bind nil)) (setf sv-bind (let ((m (make-instance 'mp-Bind))) (setf (sv-parameters m) (elt var 1))(setf (sv-arguments m) (let ((m (make-instance 'mp-Call))) (setf (sv-invocant m) sv-b)(setf (sv-method m) (sv-buf (elt var 0) ))(setf (sv-arguments m) (list ))(setf (sv-hyper m) 0) m)) m))(setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list " " (format nil "~{~a~}" (list (sv-emit sv-bind ) "; ")))))))(setf sv-i (+ sv-i 1))))(return-from mp6-function (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit (sv-parameters sv-self) ) " }")))))) (progn ))(format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-emit (sv-parameters sv-self) ) (format nil "~{~a~}" (list " = " (format nil "~{~a~}" (list (sv-emit (sv-arguments sv-self) ) ")")))))))))))




;; class Proto
(defpackage mp-Proto
  (:use common-lisp))
;; (in-package mp-Proto)
(if (not (ignore-errors (find-class 'mp-Proto)))
  (defclass mp-Proto () ()))

(let (x) 
  (setq x (make-instance 'mp-Proto))
  (defun proto-mp-Proto () x))
;; has $.name
(let ((new-slots (list (list :name 'sv-name
  :readers '(sv-name)
  :writers '((setf sv-name))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Proto)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Proto :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Proto))
  (block mp6-function
    (progn (format nil "~{~a~}" (list (sv-name sv-self))))))




;; class Call
(defpackage mp-Call
  (:use common-lisp))
;; (in-package mp-Call)
(if (not (ignore-errors (find-class 'mp-Call)))
  (defclass mp-Call () ()))

(let (x) 
  (setq x (make-instance 'mp-Call))
  (defun proto-mp-Call () x))
;; has $.invocant
(let ((new-slots (list (list :name 'sv-invocant
  :readers '(sv-invocant)
  :writers '((setf sv-invocant))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Call)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Call :direct-slots new-slots))

;; has $.hyper
(let ((new-slots (list (list :name 'sv-hyper
  :readers '(sv-hyper)
  :writers '((setf sv-hyper))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Call)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Call :direct-slots new-slots))

;; has $.method
(let ((new-slots (list (list :name 'sv-method
  :readers '(sv-method)
  :writers '((setf sv-method))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Call)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Call :direct-slots new-slots))

;; has $.arguments
(let ((new-slots (list (list :name 'sv-arguments
  :readers '(sv-arguments)
  :writers '((setf sv-arguments))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Call)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Call :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Call))
  (block mp6-function
    (let ((sv-invocant nil)(sv-meth nil)(sv-call nil)) (setf sv-invocant (sv-emit (sv-invocant sv-self) ))(if (sv-bool (sv-eq sv-invocant "self")) (progn (setf sv-invocant "$self")) (progn ))(if (sv-bool (sv-eq (sv-method sv-self) "values")) (progn (if (sv-bool (sv-hyper sv-self)) (progn (progn (write-line (format nil "~{~a~}" (list "not implemented")) *error-output*) (sb-ext:quit))) (progn (return-from mp6-function (format nil "~{~a~}" (list "@{" (format nil "~{~a~}" (list sv-invocant "}")))))))) (progn ))(if (sv-bool (sv-or (sv-eq (sv-method sv-self) "perl") (sv-or (sv-eq (sv-method sv-self) "yaml") (sv-or (sv-eq (sv-method sv-self) "say") (sv-or (sv-eq (sv-method sv-self) "join") (sv-or (sv-eq (sv-method sv-self) "chars") (sv-eq (sv-method sv-self) "isa"))))))) (progn (if (sv-bool (sv-hyper sv-self)) (progn (return-from mp6-function (format nil "~{~a~}" (list "[ map { Main::" (format nil "~{~a~}" (list (sv-method sv-self) (format nil "~{~a~}" (list "( $_, " (format nil "~{~a~}" (list ", " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") (format nil "~{~a~}" (list ")" (format nil "~{~a~}" (list " } @{ " (format nil "~{~a~}" (list sv-invocant " } ]")))))))))))))))))) (progn (return-from mp6-function (format nil "~{~a~}" (list "Main::" (format nil "~{~a~}" (list (sv-method sv-self) (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list sv-invocant (format nil "~{~a~}" (list ", " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))))))))))))) (progn ))(setf sv-meth (sv-method sv-self))(if (sv-bool (sv-eq sv-meth "postcircumfix:<( )>")) (progn (setf sv-meth "")) (progn ))(setf sv-call (format nil "~{~a~}" (list "->" (format nil "~{~a~}" (list sv-meth (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))))))(if (sv-bool (sv-hyper sv-self)) (progn (format nil "~{~a~}" (list "[ map { $_" (format nil "~{~a~}" (list sv-call (format nil "~{~a~}" (list " } @{ " (format nil "~{~a~}" (list sv-invocant " } ]"))))))))) (progn (format nil "~{~a~}" (list sv-invocant sv-call)))))))




;; class Apply
(defpackage mp-Apply
  (:use common-lisp))
;; (in-package mp-Apply)
(if (not (ignore-errors (find-class 'mp-Apply)))
  (defclass mp-Apply () ()))

(let (x) 
  (setq x (make-instance 'mp-Apply))
  (defun proto-mp-Apply () x))
;; has $.code
(let ((new-slots (list (list :name 'sv-code
  :readers '(sv-code)
  :writers '((setf sv-code))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Apply)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Apply :direct-slots new-slots))

;; has $.arguments
(let ((new-slots (list (list :name 'sv-arguments
  :readers '(sv-arguments)
  :writers '((setf sv-arguments))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Apply)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Apply :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Apply))
  (block mp6-function
    (let ((sv-code nil)) (setf sv-code (sv-code sv-self))(if (sv-bool (isa sv-code "Str")) (progn ) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-emit (sv-code sv-self) ) (format nil "~{~a~}" (list ")->(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))))))))(if (sv-bool (sv-eq sv-code "self")) (progn (return-from mp6-function "$self")) (progn ))(if (sv-bool (sv-eq sv-code "make")) (progn (return-from mp6-function (format nil "~{~a~}" (list "return(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "say")) (progn (return-from mp6-function (format nil "~{~a~}" (list "Main::say(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "print")) (progn (return-from mp6-function (format nil "~{~a~}" (list "Main::print(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "warn")) (progn (return-from mp6-function (format nil "~{~a~}" (list "warn(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "array")) (progn (return-from mp6-function (format nil "~{~a~}" (list "@{" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") "}")))))) (progn ))(if (sv-bool (sv-eq sv-code "prefix:<~>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(\"\" . " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "prefix:<!>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") " ? 0 : 1)")))))) (progn ))(if (sv-bool (sv-eq sv-code "prefix:<?>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") " ? 1 : 0)")))))) (progn ))(if (sv-bool (sv-eq sv-code "prefix:<$>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "${" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") "}")))))) (progn ))(if (sv-bool (sv-eq sv-code "prefix:<@>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "@{" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") "}")))))) (progn ))(if (sv-bool (sv-eq sv-code "prefix:<%>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "%{" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ") "}")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<~>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " . ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<+>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " + ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<->")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " - ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<>>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " > ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<x>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " x ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<&&>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " && ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<||>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " || ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<eq>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " eq ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<ne>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " ne ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<==>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " == ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "infix:<!=>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) " != ") ")")))))) (progn ))(if (sv-bool (sv-eq sv-code "ternary:<?? !!>")) (progn (return-from mp6-function (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-emit (elt arguments 0) ) (format nil "~{~a~}" (list " ? " (format nil "~{~a~}" (list (sv-emit (elt arguments 1) ) (format nil "~{~a~}" (list " : " (format nil "~{~a~}" (list (sv-emit (elt arguments 2) ) ")")))))))))))))) (progn ))(format nil "~{~a~}" (list (sv-code sv-self) (format nil "~{~a~}" (list "(" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-arguments sv-self)) ", ") ")")))))))))




;; class Return
(defpackage mp-Return
  (:use common-lisp))
;; (in-package mp-Return)
(if (not (ignore-errors (find-class 'mp-Return)))
  (defclass mp-Return () ()))

(let (x) 
  (setq x (make-instance 'mp-Return))
  (defun proto-mp-Return () x))
;; has $.result
(let ((new-slots (list (list :name 'sv-result
  :readers '(sv-result)
  :writers '((setf sv-result))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Return)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Return :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Return))
  (block mp6-function
    (progn (return-from mp6-function (format nil "~{~a~}" (list "return(" (format nil "~{~a~}" (list (sv-emit (sv-result sv-self) ) ")"))))))))




;; class If
(defpackage mp-If
  (:use common-lisp))
;; (in-package mp-If)
(if (not (ignore-errors (find-class 'mp-If)))
  (defclass mp-If () ()))

(let (x) 
  (setq x (make-instance 'mp-If))
  (defun proto-mp-If () x))
;; has $.cond
(let ((new-slots (list (list :name 'sv-cond
  :readers '(sv-cond)
  :writers '((setf sv-cond))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-If)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-If :direct-slots new-slots))

;; has $.body
(let ((new-slots (list (list :name 'sv-body
  :readers '(sv-body)
  :writers '((setf sv-body))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-If)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-If :direct-slots new-slots))

;; has $.otherwise
(let ((new-slots (list (list :name 'sv-otherwise
  :readers '(sv-otherwise)
  :writers '((setf sv-otherwise))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-If)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-If :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-If))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "do { if (" (format nil "~{~a~}" (list (sv-emit (sv-cond sv-self) ) (format nil "~{~a~}" (list ") { " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-body sv-self)) ";") (format nil "~{~a~}" (list " } else { " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-otherwise sv-self)) ";") " } }")))))))))))))))




;; class For
(defpackage mp-For
  (:use common-lisp))
;; (in-package mp-For)
(if (not (ignore-errors (find-class 'mp-For)))
  (defclass mp-For () ()))

(let (x) 
  (setq x (make-instance 'mp-For))
  (defun proto-mp-For () x))
;; has $.cond
(let ((new-slots (list (list :name 'sv-cond
  :readers '(sv-cond)
  :writers '((setf sv-cond))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-For)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-For :direct-slots new-slots))

;; has $.body
(let ((new-slots (list (list :name 'sv-body
  :readers '(sv-body)
  :writers '((setf sv-body))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-For)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-For :direct-slots new-slots))

;; has $.topic
(let ((new-slots (list (list :name 'sv-topic
  :readers '(sv-topic)
  :writers '((setf sv-topic))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-For)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-For :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-For))
  (block mp6-function
    (let ((sv-cond nil)) (setf sv-cond (sv-cond sv-self))(if (sv-bool (sv-and (isa sv-cond "Var") (sv-eq (sv-sigil sv-cond ) "@"))) (progn (setf sv-cond (let ((m (make-instance 'mp-Apply))) (setf (sv-code m) "prefix:<@>")(setf (sv-arguments m) (list sv-cond)) m))) (progn ))(format nil "~{~a~}" (list "do { for my " (format nil "~{~a~}" (list (sv-emit (sv-topic sv-self) ) (format nil "~{~a~}" (list " ( " (format nil "~{~a~}" (list (sv-emit sv-cond ) (format nil "~{~a~}" (list " ) { " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-body sv-self)) ";") " } }")))))))))))))))




;; class Decl
(defpackage mp-Decl
  (:use common-lisp))
;; (in-package mp-Decl)
(if (not (ignore-errors (find-class 'mp-Decl)))
  (defclass mp-Decl () ()))

(let (x) 
  (setq x (make-instance 'mp-Decl))
  (defun proto-mp-Decl () x))
;; has $.decl
(let ((new-slots (list (list :name 'sv-decl
  :readers '(sv-decl)
  :writers '((setf sv-decl))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Decl)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Decl :direct-slots new-slots))

;; has $.type
(let ((new-slots (list (list :name 'sv-type
  :readers '(sv-type)
  :writers '((setf sv-type))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Decl)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Decl :direct-slots new-slots))

;; has $.var
(let ((new-slots (list (list :name 'sv-var
  :readers '(sv-var)
  :writers '((setf sv-var))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Decl)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Decl :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Decl))
  (block mp6-function
    (let ((sv-decl nil)(sv-name nil)) (setf sv-decl (sv-decl sv-self))(setf sv-name (sv-name (sv-var sv-self) ))(if (sv-bool (sv-eq sv-decl "has")) (format nil "~{~a~}" (list "sub " (format nil "~{~a~}" (list sv-name (format nil "~{~a~}" (list " { " (format nil "~{~a~}" (list "@_ == 1 " (format nil "~{~a~}" (list "? ( $_[0]->{" (format nil "~{~a~}" (list sv-name (format nil "~{~a~}" (list "} ) " (format nil "~{~a~}" (list ": ( $_[0]->{" (format nil "~{~a~}" (list sv-name (format nil "~{~a~}" (list "} = $_[1] ) " "}")))))))))))))))))))) (format nil "~{~a~}" (list (sv-decl sv-self) (format nil "~{~a~}" (list " " (format nil "~{~a~}" (list (sv-type sv-self) (format nil "~{~a~}" (list " " (sv-emit (sv-var sv-self) )))))))))))))




;; class Sig
(defpackage mp-Sig
  (:use common-lisp))
;; (in-package mp-Sig)
(if (not (ignore-errors (find-class 'mp-Sig)))
  (defclass mp-Sig () ()))

(let (x) 
  (setq x (make-instance 'mp-Sig))
  (defun proto-mp-Sig () x))
;; has $.invocant
(let ((new-slots (list (list :name 'sv-invocant
  :readers '(sv-invocant)
  :writers '((setf sv-invocant))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Sig)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Sig :direct-slots new-slots))

;; has $.positional
(let ((new-slots (list (list :name 'sv-positional
  :readers '(sv-positional)
  :writers '((setf sv-positional))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Sig)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Sig :direct-slots new-slots))

;; has $.named
(let ((new-slots (list (list :name 'sv-named
  :readers '(sv-named)
  :writers '((setf sv-named))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Sig)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Sig :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Sig))
  (block mp6-function
    (progn " print \\'Signature - TODO\\'; die \\'Signature - TODO\\'; ")))

;; method invocant
(if (not (ignore-errors (find-method 'sv-invocant () ())))
  (defgeneric sv-invocant (sv-self)
      (:documentation "a method")))
(defmethod sv-invocant ((sv-self mp-Sig))
  (block mp6-function
    (progn (sv-invocant sv-self))))

;; method positional
(if (not (ignore-errors (find-method 'sv-positional () ())))
  (defgeneric sv-positional (sv-self)
      (:documentation "a method")))
(defmethod sv-positional ((sv-self mp-Sig))
  (block mp6-function
    (progn (sv-positional sv-self))))




;; class Method
(defpackage mp-Method
  (:use common-lisp))
;; (in-package mp-Method)
(if (not (ignore-errors (find-class 'mp-Method)))
  (defclass mp-Method () ()))

(let (x) 
  (setq x (make-instance 'mp-Method))
  (defun proto-mp-Method () x))
;; has $.name
(let ((new-slots (list (list :name 'sv-name
  :readers '(sv-name)
  :writers '((setf sv-name))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Method)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Method :direct-slots new-slots))

;; has $.sig
(let ((new-slots (list (list :name 'sv-sig
  :readers '(sv-sig)
  :writers '((setf sv-sig))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Method)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Method :direct-slots new-slots))

;; has $.block
(let ((new-slots (list (list :name 'sv-block
  :readers '(sv-block)
  :writers '((setf sv-block))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Method)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Method :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Method))
  (block mp6-function
    (let ((sv-sig nil)(sv-invocant nil)(sv-pos nil)(sv-str nil)(sv-pos nil)(sv-bind nil)) (setf sv-sig (sv-sig sv-self))(setf sv-invocant (sv-invocant sv-sig ))(setf sv-pos (sv-positional sv-sig ))(setf sv-str "my $List__ = \\@_; ")(setf sv-pos (sv-positional sv-sig ))(dolist (sv-field sv-pos) (progn (if (sv-bool (isa sv-field "Lit::Array")) (progn (setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list "my (" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-array sv-field )) ", ") "); ")))))))) (progn (setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list "my " (format nil "~{~a~}" (list (sv-emit sv-field ) "; ")))))))))))(setf sv-bind (let ((m (make-instance 'mp-Bind))) (setf (sv-parameters m) (let ((m (make-instance 'mp-Lit-Array))) (setf (sv-array m) (sv-positional sv-sig )) m))(setf (sv-arguments m) (let ((m (make-instance 'mp-Var))) (setf (sv-sigil m) "@")(setf (sv-twigil m) "")(setf (sv-name m) "_") m)) m))(setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit sv-bind ) "; ")))))(format nil "~{~a~}" (list "sub " (format nil "~{~a~}" (list (sv-name sv-self) (format nil "~{~a~}" (list " { " (format nil "~{~a~}" (list "my " (format nil "~{~a~}" (list (sv-emit sv-invocant ) (format nil "~{~a~}" (list " = shift; " (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-block sv-self)) "; ") " }")))))))))))))))))))




;; class Sub
(defpackage mp-Sub
  (:use common-lisp))
;; (in-package mp-Sub)
(if (not (ignore-errors (find-class 'mp-Sub)))
  (defclass mp-Sub () ()))

(let (x) 
  (setq x (make-instance 'mp-Sub))
  (defun proto-mp-Sub () x))
;; has $.name
(let ((new-slots (list (list :name 'sv-name
  :readers '(sv-name)
  :writers '((setf sv-name))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Sub)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Sub :direct-slots new-slots))

;; has $.sig
(let ((new-slots (list (list :name 'sv-sig
  :readers '(sv-sig)
  :writers '((setf sv-sig))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Sub)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Sub :direct-slots new-slots))

;; has $.block
(let ((new-slots (list (list :name 'sv-block
  :readers '(sv-block)
  :writers '((setf sv-block))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Sub)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Sub :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Sub))
  (block mp6-function
    (let ((sv-sig nil)(sv-pos nil)(sv-str nil)(sv-pos nil)(sv-bind nil)) (setf sv-sig (sv-sig sv-self))(setf sv-pos (sv-positional sv-sig ))(setf sv-str "my $List__ = \\@_; ")(setf sv-pos (sv-positional sv-sig ))(dolist (sv-field sv-pos) (progn (if (sv-bool (isa sv-field "Lit::Array")) (progn (setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list "my (" (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-array sv-field )) ", ") "); ")))))))) (progn (setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list "my " (format nil "~{~a~}" (list (sv-emit sv-field ) "; ")))))))))))(setf sv-bind (let ((m (make-instance 'mp-Bind))) (setf (sv-parameters m) (let ((m (make-instance 'mp-Lit-Array))) (setf (sv-array m) (sv-positional sv-sig )) m))(setf (sv-arguments m) (let ((m (make-instance 'mp-Var))) (setf (sv-sigil m) "@")(setf (sv-twigil m) "")(setf (sv-name m) "_") m)) m))(setf sv-str (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-emit sv-bind ) "; ")))))(format nil "~{~a~}" (list "sub " (format nil "~{~a~}" (list (sv-name sv-self) (format nil "~{~a~}" (list " { " (format nil "~{~a~}" (list sv-str (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-block sv-self)) "; ") " }")))))))))))))




;; class Do
(defpackage mp-Do
  (:use common-lisp))
;; (in-package mp-Do)
(if (not (ignore-errors (find-class 'mp-Do)))
  (defclass mp-Do () ()))

(let (x) 
  (setq x (make-instance 'mp-Do))
  (defun proto-mp-Do () x))
;; has $.block
(let ((new-slots (list (list :name 'sv-block
  :readers '(sv-block)
  :writers '((setf sv-block))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Do)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Do :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Do))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "do { " (format nil "~{~a~}" (list (sv-join (mapcar #'sv-emit (sv-block sv-self)) "; ") " }")))))))




;; class Use
(defpackage mp-Use
  (:use common-lisp))
;; (in-package mp-Use)
(if (not (ignore-errors (find-class 'mp-Use)))
  (defclass mp-Use () ()))

(let (x) 
  (setq x (make-instance 'mp-Use))
  (defun proto-mp-Use () x))
;; has $.mod
(let ((new-slots (list (list :name 'sv-mod
  :readers '(sv-mod)
  :writers '((setf sv-mod))
  :initform 'nil
  :initfunction (constantly nil)))))
(dolist (slot-defn (sb-mop:class-direct-slots (find-class 'mp-Use)))
(push (list :name (sb-mop:slot-definition-name slot-defn)
  :readers (sb-mop:slot-definition-readers slot-defn)
  :writers (sb-mop:slot-definition-writers slot-defn)
  :initform (sb-mop:slot-definition-initform slot-defn)
  :initfunction (sb-mop:slot-definition-initfunction slot-defn))
new-slots))
(sb-mop:ensure-class 'mp-Use :direct-slots new-slots))

;; method emit
(if (not (ignore-errors (find-method 'sv-emit () ())))
  (defgeneric sv-emit (sv-self)
      (:documentation "a method")))
(defmethod sv-emit ((sv-self mp-Use))
  (block mp6-function
    (progn (format nil "~{~a~}" (list "use " (sv-mod sv-self))))))





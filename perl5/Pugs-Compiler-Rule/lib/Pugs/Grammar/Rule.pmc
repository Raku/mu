# !!! DO NOT EDIT !!!
# This file was generated by util/gen-rule-pmc.pl at Sat Jul  7 11:42:00 2007

package Pugs::Grammar::Rule;
use utf8;
no strict 'refs';
use Pugs::Runtime::Match;
use Pugs::Runtime::Regex;

no warnings 'redefine';

our %rule_terms;
our %variables;
*pod_begin = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1150} = $pos or 1 )
           && (
           (
              (   ( $pad{I1151} = $pos or 1 ) && 
               ## <perl5>
               ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                   ? ( $pos += length( $1 ) or 1 )
                   : 0
               )
               ## </perl5>
 ||    ( ( $pos = $pad{I1151} ) && 0 ) )
           &&
             ## <constant>
             ( ( substr( $s, $pos, 1 ) eq '=' )
                 ? ( $pos += 1 or 1 )
                 : 0
             )
             ## </constant>

           &&
             ## <constant>
             ( ( substr( $s, $pos, 1 ) eq 'e' )
                 ? ( $pos += 1 or 1 )
                 : 0
             )
             ## </constant>

           &&
             ## <constant>
             ( ( substr( $s, $pos, 1 ) eq 'n' )
                 ? ( $pos += 1 or 1 )
                 : 0
             )
             ## </constant>

           &&
              (   ( $pad{I1152} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq 'd' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1152} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1153} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1153} ) && 0 ) )) {}; $bool = 1 }
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1150} ) or 1 )
           &&            (
              (   ( $pad{I1154} = $pos or 1 ) && 
               ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1154} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1155} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1155} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1156} = $pos or 1 ) && 
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match = 
                           $grammar->pod_begin( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               } ||    ( ( $pos = $pad{I1156} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for pod_begin
 # Data::Bind->sub_signature
#  (\&pod_begin, );
;
*pod_other = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1157} = $pos or 1 )
           && (
           (
              (   ( $pad{I1158} = $pos or 1 ) && 
               ## <perl5>
               ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                   ? ( $pos += length( $1 ) or 1 )
                   : 0
               )
               ## </perl5>
 ||    ( ( $pos = $pad{I1158} ) && 0 ) )
           &&
             ## <constant>
             ( ( substr( $s, $pos, 1 ) eq '=' )
                 ? ( $pos += 1 or 1 )
                 : 0
             )
             ## </constant>

           &&
             ## <constant>
             ( ( substr( $s, $pos, 1 ) eq 'c' )
                 ? ( $pos += 1 or 1 )
                 : 0
             )
             ## </constant>

           &&
             ## <constant>
             ( ( substr( $s, $pos, 1 ) eq 'u' )
                 ? ( $pos += 1 or 1 )
                 : 0
             )
             ## </constant>

           &&
              (   ( $pad{I1159} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq 't' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1159} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1160} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1160} ) && 0 ) )) {}; $bool = 1 }
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1157} ) or 1 )
           &&            (
              (   ( $pad{I1161} = $pos or 1 ) && 
               ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1161} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1162} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1162} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1163} = $pos or 1 ) && 
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match = 
                           $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               } ||    ( ( $pos = $pad{I1163} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for pod_other
 # Data::Bind->sub_signature
#  (\&pod_other, );
;
*ws = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
        (   ( $pad{I1164} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1165} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1166} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '#' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1166} ) && 0 ) )
               &&
                 do { while (
                  (   ( $pad{I1167} = $pos or 1 ) && 
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
 ||    ( ( $pos = $pad{I1167} ) && 0 ) )) {}; $bool = 1 }
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1165} ) or 1 )
               &&                (
                  (   ( $pad{I1168} = $pos or 1 ) && 
                   ## <perl5>
                   ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                       ? ( $pos += length( $1 ) or 1 )
                       : 0
                   )
                   ## </perl5>
 ||    ( ( $pos = $pad{I1168} ) && 0 ) )
               &&
                 (
                  (   ( $pad{I1169} = $pos or 1 ) && 
                     (
                        (   ( $pad{I1170} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '=' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1170} ) && 0 ) )
                     &&
                       (
                        (   ( $pad{I1171} = $pos or 1 ) && 
                           ## <alt>
                           (
                               ( $pad{I1172} = $pos or 1 )
                               && (
                               (
                                  (   ( $pad{I1173} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'b' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1173} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'g' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'i' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1174} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'n' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1174} ) && 0 ) )
                               &&
                                  (   ( $pad{I1175} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1175} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'E' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'N' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1176} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'D' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1176} ) && 0 ) )
                               &&
                                 do { while (
                                  (   ( $pad{I1177} = $pos or 1 ) && 
                                     ## <perl5>
                                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                                         ? ( $pos += length( $1 ) or 1 )
                                         : 0
                                     )
                                     ## </perl5>
 ||    ( ( $pos = $pad{I1177} ) && 0 ) )) {}; $bool = 1 }
                               &&
                                 do { while (
                                  (   ( $pad{I1178} = $pos or 1 ) && 
                                     ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1178} ) && 0 ) )) {}; $bool = 1 }
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1179} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'b' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1179} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'g' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'i' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1180} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'n' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1180} ) && 0 ) )
                               &&
                                  (   ( $pad{I1181} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_begin( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1181} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1182} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'k' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1182} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'w' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'i' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1183} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'd' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1183} ) && 0 ) )
                               &&
                                  (   ( $pad{I1184} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1184} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1185} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'p' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1185} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'o' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1186} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'd' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1186} ) && 0 ) )
                               &&
                                  (   ( $pad{I1187} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1187} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1188} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'f' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1188} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'o' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1189} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'r' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1189} ) && 0 ) )
                               &&
                                  (   ( $pad{I1190} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1190} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1191} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'h' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1191} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'a' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'd' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1192} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq '1' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1192} ) && 0 ) )
                               &&
                                  (   ( $pad{I1193} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1193} ) && 0 ) )
                               )
                             )
                           )
## </alt>
 ||    ( ( $pos = $pad{I1171} ) && 0 ) )
                       || ( $bool = 1 )
                       ) 
                     ) ||    ( ( $pos = $pad{I1169} ) && 0 ) )
                 || ( $bool = 1 )
                 ) 
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1165} ) or 1 )
               &&                 (   ( $pad{I1194} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1194} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1164} ) && 0 ) )
       && do { while (
        (   ( $pad{I1164} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1165} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1166} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '#' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1166} ) && 0 ) )
               &&
                 do { while (
                  (   ( $pad{I1167} = $pos or 1 ) && 
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
 ||    ( ( $pos = $pad{I1167} ) && 0 ) )) {}; $bool = 1 }
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1165} ) or 1 )
               &&                (
                  (   ( $pad{I1168} = $pos or 1 ) && 
                   ## <perl5>
                   ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                       ? ( $pos += length( $1 ) or 1 )
                       : 0
                   )
                   ## </perl5>
 ||    ( ( $pos = $pad{I1168} ) && 0 ) )
               &&
                 (
                  (   ( $pad{I1169} = $pos or 1 ) && 
                     (
                        (   ( $pad{I1170} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '=' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1170} ) && 0 ) )
                     &&
                       (
                        (   ( $pad{I1171} = $pos or 1 ) && 
                           ## <alt>
                           (
                               ( $pad{I1172} = $pos or 1 )
                               && (
                               (
                                  (   ( $pad{I1173} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'b' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1173} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'g' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'i' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1174} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'n' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1174} ) && 0 ) )
                               &&
                                  (   ( $pad{I1175} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1175} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'E' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'N' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1176} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'D' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1176} ) && 0 ) )
                               &&
                                 do { while (
                                  (   ( $pad{I1177} = $pos or 1 ) && 
                                     ## <perl5>
                                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                                         ? ( $pos += length( $1 ) or 1 )
                                         : 0
                                     )
                                     ## </perl5>
 ||    ( ( $pos = $pad{I1177} ) && 0 ) )) {}; $bool = 1 }
                               &&
                                 do { while (
                                  (   ( $pad{I1178} = $pos or 1 ) && 
                                     ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1178} ) && 0 ) )) {}; $bool = 1 }
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1179} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'b' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1179} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'g' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'i' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1180} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'n' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1180} ) && 0 ) )
                               &&
                                  (   ( $pad{I1181} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_begin( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1181} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1182} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'k' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1182} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'w' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'i' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1183} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'd' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1183} ) && 0 ) )
                               &&
                                  (   ( $pad{I1184} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1184} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1185} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'p' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1185} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'o' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1186} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'd' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1186} ) && 0 ) )
                               &&
                                  (   ( $pad{I1187} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1187} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1188} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'f' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1188} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'o' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1189} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'r' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1189} ) && 0 ) )
                               &&
                                  (   ( $pad{I1190} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1190} ) && 0 ) )
                               )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                               &&                                (
                                  (   ( $pad{I1191} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq 'h' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1191} ) && 0 ) )
                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'a' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'd' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>

                               &&
                                  (   ( $pad{I1192} = $pos or 1 ) && 
                                   ## <constant>
                                   ( ( substr( $s, $pos, 1 ) eq '1' )
                                       ? ( $pos += 1 or 1 )
                                       : 0
                                   )
                                   ## </constant>
 ||    ( ( $pos = $pad{I1192} ) && 0 ) )
                               &&
                                  (   ( $pad{I1193} = $pos or 1 ) && 
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match = 
                                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   } ||    ( ( $pos = $pad{I1193} ) && 0 ) )
                               )
                             )
                           )
## </alt>
 ||    ( ( $pos = $pad{I1171} ) && 0 ) )
                       || ( $bool = 1 )
                       ) 
                     ) ||    ( ( $pos = $pad{I1169} ) && 0 ) )
                 || ( $bool = 1 )
                 ) 
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1165} ) or 1 )
               &&                 (   ( $pad{I1194} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1194} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1164} ) && 0 ) )) {}; $bool = 1 }
       ) ;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for ws
 # Data::Bind->sub_signature
#  (\&ws, );
;
*ident = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
        (   ( $pad{I1195} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1196} = $pos or 1 )
               && (
                (   ( $pad{I1197} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1197} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1196} ) or 1 )
               &&                 (   ( $pad{I1198} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '_' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1198} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1196} ) or 1 )
               &&                 (   ( $pad{I1199} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 2 ) eq '::' )
                     ? ( $pos += 2 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1199} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1195} ) && 0 ) )
       && do { while (
        (   ( $pad{I1195} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1196} = $pos or 1 )
               && (
                (   ( $pad{I1197} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1197} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1196} ) or 1 )
               &&                 (   ( $pad{I1198} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '_' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1198} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1196} ) or 1 )
               &&                 (   ( $pad{I1199} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 2 ) eq '::' )
                     ? ( $pos += 2 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1199} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1195} ) && 0 ) )) {}; $bool = 1 }
       ) ;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for ident
 # Data::Bind->sub_signature
#  (\&ident, );
;
*special_char = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1200} = $pos or 1 )
           && (
           (
              (   ( $pad{I1201} = $pos or 1 ) && 
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       ## <alt>
                       (
                           ( $pad{I1202} = $pos or 1 )
                           && (
                            (   ( $pad{I1203} = $pos or 1 ) && 
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq 'c' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
 ||    ( ( $pos = $pad{I1203} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1202} ) or 1 )
                           &&                             (   ( $pad{I1204} = $pos or 1 ) && 
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq 'C' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
 ||    ( ( $pos = $pad{I1204} ) && 0 ) )
                         )
                       )
## </alt>
;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1201} ) && 0 ) )
           &&
              (   ( $pad{I1205} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '[' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1205} ) && 0 ) )
           &&
              (   ( $pad{I1206} = $pos or 1 ) && 
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       (
                        (   ( $pad{I1207} = $pos or 1 ) && 
                           ## <alt>
                           (
                               ( $pad{I1208} = $pos or 1 )
                               && (
                               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                           $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'alnum'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            }
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                ## <perl5>
                               ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                                   ? ( $pos += length( $1 ) or 1 )
                                   : 0
                               )
                               ## </perl5>

                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1209} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq ';' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1209} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1210} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq '(' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1210} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1211} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq ')' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1211} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1212} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq '-' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1212} ) && 0 ) )
                             )
                           )
## </alt>
 ||    ( ( $pos = $pad{I1207} ) && 0 ) )
                       && do { while (
                        (   ( $pad{I1207} = $pos or 1 ) && 
                           ## <alt>
                           (
                               ( $pad{I1208} = $pos or 1 )
                               && (
                               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                           $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'alnum'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            }
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                ## <perl5>
                               ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                                   ? ( $pos += length( $1 ) or 1 )
                                   : 0
                               )
                               ## </perl5>

                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1209} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq ';' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1209} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1210} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq '(' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1210} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1211} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq ')' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1211} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1208} ) or 1 )
                               &&                                 (   ( $pad{I1212} = $pos or 1 ) && 
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq '-' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
 ||    ( ( $pos = $pad{I1212} ) && 0 ) )
                             )
                           )
## </alt>
 ||    ( ( $pos = $pad{I1207} ) && 0 ) )) {}; $bool = 1 }
                       ) ;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 1 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1206} ) && 0 ) )
           &&
              (   ( $pad{I1213} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1213} ) && 0 ) )
           &&
              (   ( $pad{I1214} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'special_char' => '\\' . $::_V6_MATCH_->[0] . $::_V6_MATCH_->[1]});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1214} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1200} ) or 1 )
           &&            (
              (   ( $pad{I1215} = $pos or 1 ) && 
               ## <alt>
               (
                   ( $pad{I1216} = $pos or 1 )
                   && (
                    (   ( $pad{I1217} = $pos or 1 ) && 
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq 'x' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
 ||    ( ( $pos = $pad{I1217} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1216} ) or 1 )
                   &&                     (   ( $pad{I1218} = $pos or 1 ) && 
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq 'X' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
 ||    ( ( $pos = $pad{I1218} ) && 0 ) )
                 )
               )
## </alt>
 ||    ( ( $pos = $pad{I1215} ) && 0 ) )
           &&
             (
              (   ( $pad{I1219} = $pos or 1 ) && 
                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                             $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'xdigit'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1219} ) && 0 ) )
             && do { while (
              (   ( $pad{I1219} = $pos or 1 ) && 
                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                             $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'xdigit'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1219} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1220} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'special_char' => '\\' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1220} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1200} ) or 1 )
           &&            (
              (   ( $pad{I1221} = $pos or 1 ) && 
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       ## <alt>
                       (
                           ( $pad{I1222} = $pos or 1 )
                           && (
                            (   ( $pad{I1223} = $pos or 1 ) && 
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq 'x' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
 ||    ( ( $pos = $pad{I1223} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1222} ) or 1 )
                           &&                             (   ( $pad{I1224} = $pos or 1 ) && 
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq 'X' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
 ||    ( ( $pos = $pad{I1224} ) && 0 ) )
                         )
                       )
## </alt>
;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1221} ) && 0 ) )
           &&
              (   ( $pad{I1225} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '[' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1225} ) && 0 ) )
           &&
              (   ( $pad{I1226} = $pos or 1 ) && 
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       (
                        (   ( $pad{I1227} = $pos or 1 ) && 
                           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                       $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'xdigit'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1227} ) && 0 ) )
                       && do { while (
                        (   ( $pad{I1227} = $pos or 1 ) && 
                           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                       $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'xdigit'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1227} ) && 0 ) )) {}; $bool = 1 }
                       ) ;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 1 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1226} ) && 0 ) )
           &&
              (   ( $pad{I1228} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1228} ) && 0 ) )
           &&
              (   ( $pad{I1229} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'special_char' => '\\' . $::_V6_MATCH_->[0] . $::_V6_MATCH_->[1]});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1229} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1200} ) or 1 )
           &&            (
              (   ( $pad{I1230} = $pos or 1 ) && 
               ## <alt>
               (
                   ( $pad{I1231} = $pos or 1 )
                   && (
                    (   ( $pad{I1232} = $pos or 1 ) && 
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq 'o' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
 ||    ( ( $pos = $pad{I1232} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1231} ) or 1 )
                   &&                     (   ( $pad{I1233} = $pos or 1 ) && 
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq 'O' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
 ||    ( ( $pos = $pad{I1233} ) && 0 ) )
                 )
               )
## </alt>
 ||    ( ( $pos = $pad{I1230} ) && 0 ) )
           &&
             (
              (   ( $pad{I1234} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1234} ) && 0 ) )
             && do { while (
              (   ( $pad{I1234} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1234} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1235} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'special_char' => '\\' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1235} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1200} ) or 1 )
           &&            (
              (   ( $pad{I1236} = $pos or 1 ) && 
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       ## <alt>
                       (
                           ( $pad{I1237} = $pos or 1 )
                           && (
                            (   ( $pad{I1238} = $pos or 1 ) && 
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq 'o' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
 ||    ( ( $pos = $pad{I1238} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1237} ) or 1 )
                           &&                             (   ( $pad{I1239} = $pos or 1 ) && 
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq 'O' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
 ||    ( ( $pos = $pad{I1239} ) && 0 ) )
                         )
                       )
## </alt>
;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1236} ) && 0 ) )
           &&
              (   ( $pad{I1240} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '[' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1240} ) && 0 ) )
           &&
              (   ( $pad{I1241} = $pos or 1 ) && 
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       (
                        (   ( $pad{I1242} = $pos or 1 ) && 
                           ## <perl5>
                           ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                               ? ( $pos += length( $1 ) or 1 )
                               : 0
                           )
                           ## </perl5>
 ||    ( ( $pos = $pad{I1242} ) && 0 ) )
                       && do { while (
                        (   ( $pad{I1242} = $pos or 1 ) && 
                           ## <perl5>
                           ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                               ? ( $pos += length( $1 ) or 1 )
                               : 0
                           )
                           ## </perl5>
 ||    ( ( $pos = $pad{I1242} ) && 0 ) )) {}; $bool = 1 }
                       ) ;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 1 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1241} ) && 0 ) )
           &&
              (   ( $pad{I1243} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1243} ) && 0 ) )
           &&
              (   ( $pad{I1244} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'special_char' => '\\' . $::_V6_MATCH_->[0] . $::_V6_MATCH_->[1]});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1244} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1200} ) or 1 )
           &&            (
              (   ( $pad{I1245} = $pos or 1 ) && 
               ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1245} ) && 0 ) )
           &&
              (   ( $pad{I1246} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'special_char' => '\\' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1246} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for special_char
 # Data::Bind->sub_signature
#  (\&special_char, );
;
*literal = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       do { while (
        (   ( $pad{I1247} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1248} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1249} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1249} ) && 0 ) )
               &&
                  (   ( $pad{I1250} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1250} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1248} ) or 1 )
               &&                 (   ( $pad{I1251} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\']))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1251} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1247} ) && 0 ) )) {}; $bool = 1 };
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for literal
 # Data::Bind->sub_signature
#  (\&literal, );
;
*double_quoted = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       do { while (
        (   ( $pad{I1252} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1253} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1254} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1254} ) && 0 ) )
               &&
                  (   ( $pad{I1255} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1255} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1253} ) or 1 )
               &&                 (   ( $pad{I1256} = $pos or 1 ) && 
                 do{
                my $match = 
          do {
            our $I1257;
            our $I1257_sizes;
            unless ( $I1257 ) {
                my $hash = \%Pugs::Grammar::Rule::variables;
                my %sizes = map { length($_) => 1 } keys %$hash;
                $I1257_sizes = [ sort { $b <=> $a } keys %sizes ];
                $I1257 = $hash;
            }
            my $match = 0;
            my $key;
            for ( @$I1257_sizes ) {
                $key = ( $pos <= length( $s )
                            ? substr( $s, $pos, $_ )
                            : '' );
                if ( exists $I1257->{$key} ) {
                    #$named{KEY} = $key;
                    #$::_V6_MATCH_ = $m;
                    #print "m: ", Dumper( $::_V6_MATCH_->data )
                    #    if ( $key eq 'until' );
                    $match = Pugs::Runtime::Regex::preprocess_hash( $I1257, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                    last if $match;
                }
            }
            if ( $match ) {
                $pos = $match->to;
                #print "match: $key at $pos = ", Dumper( $match->data );
                $bool = 1;
            }; # else { $bool = 0 }
            $match;
          };
                if ( $match ) { push @{$named{'Pugs::Grammar::Rule::variables'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1256} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1253} ) or 1 )
               &&                 (   ( $pad{I1258} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\"]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1258} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1252} ) && 0 ) )) {}; $bool = 1 };
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for double_quoted
 # Data::Bind->sub_signature
#  (\&double_quoted, );
;
*metasyntax = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
        (   ( $pad{I1259} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1260} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1261} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1261} ) && 0 ) )
               &&
                  (   ( $pad{I1262} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1262} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1263} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1263} ) && 0 ) )
               &&
                  (   ( $pad{I1264} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1264} ) && 0 ) )
               &&
                  (   ( $pad{I1265} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1265} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1266} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1266} ) && 0 ) )
               &&
                  (   ( $pad{I1267} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1267} ) && 0 ) )
               &&
                  (   ( $pad{I1268} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1268} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1269} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '{' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1269} ) && 0 ) )
               &&
                  (   ( $pad{I1270} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1270} ) && 0 ) )
               &&
                  (   ( $pad{I1271} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '}' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1271} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1272} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '<' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1272} ) && 0 ) )
               &&
                  (   ( $pad{I1273} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1273} ) && 0 ) )
               &&
                  (   ( $pad{I1274} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1274} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                 (   ( $pad{I1275} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\>]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1275} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1259} ) && 0 ) )
       && do { while (
        (   ( $pad{I1259} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1260} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1261} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1261} ) && 0 ) )
               &&
                  (   ( $pad{I1262} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1262} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1263} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1263} ) && 0 ) )
               &&
                  (   ( $pad{I1264} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1264} ) && 0 ) )
               &&
                  (   ( $pad{I1265} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1265} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1266} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1266} ) && 0 ) )
               &&
                  (   ( $pad{I1267} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1267} ) && 0 ) )
               &&
                  (   ( $pad{I1268} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1268} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1269} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '{' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1269} ) && 0 ) )
               &&
                  (   ( $pad{I1270} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1270} ) && 0 ) )
               &&
                  (   ( $pad{I1271} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '}' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1271} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                (
                  (   ( $pad{I1272} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '<' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1272} ) && 0 ) )
               &&
                  (   ( $pad{I1273} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1273} ) && 0 ) )
               &&
                  (   ( $pad{I1274} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1274} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1260} ) or 1 )
               &&                 (   ( $pad{I1275} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\>]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1275} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1259} ) && 0 ) )) {}; $bool = 1 }
       ) ;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for metasyntax
 # Data::Bind->sub_signature
#  (\&metasyntax, );
;
*char_range = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
        (   ( $pad{I1276} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1277} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1278} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1278} ) && 0 ) )
               &&
                  (   ( $pad{I1279} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1279} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1277} ) or 1 )
               &&                 (   ( $pad{I1280} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\]]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1280} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1276} ) && 0 ) )
       && do { while (
        (   ( $pad{I1276} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1277} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1278} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1278} ) && 0 ) )
               &&
                  (   ( $pad{I1279} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1279} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1277} ) or 1 )
               &&                 (   ( $pad{I1280} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\]]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1280} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1276} ) && 0 ) )) {}; $bool = 1 }
       ) ;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for char_range
 # Data::Bind->sub_signature
#  (\&char_range, );
;
*char_class = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1281} = $pos or 1 )
           && (
           (
            (   ( $pad{I1282} = $pos or 1 ) && 
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match = 
                           $grammar->alpha( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               } ||    ( ( $pos = $pad{I1282} ) && 0 ) )
           && do { while (
            (   ( $pad{I1282} = $pos or 1 ) && 
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match = 
                           $grammar->alpha( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               } ||    ( ( $pos = $pad{I1282} ) && 0 ) )) {}; $bool = 1 }
           ) 
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1281} ) or 1 )
           &&            (
              (   ( $pad{I1283} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '[' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1283} ) && 0 ) )
           &&
              (   ( $pad{I1284} = $pos or 1 ) && 
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match = 
                           $grammar->char_range( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               } ||    ( ( $pos = $pad{I1284} ) && 0 ) )
           &&
              (   ( $pad{I1285} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1285} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for char_class
 # Data::Bind->sub_signature
#  (\&char_class, );
;
*string_code = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
        (   ( $pad{I1286} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1287} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1288} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1288} ) && 0 ) )
               &&
                  (   ( $pad{I1289} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1289} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1290} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1290} ) && 0 ) )
               &&
                  (   ( $pad{I1291} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1291} ) && 0 ) )
               &&
                  (   ( $pad{I1292} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1292} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1293} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1293} ) && 0 ) )
               &&
                  (   ( $pad{I1294} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1294} ) && 0 ) )
               &&
                  (   ( $pad{I1295} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1295} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1296} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '{' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1296} ) && 0 ) )
               &&
                  (   ( $pad{I1297} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1298} = $pos or 1 )
                       && (
                        (   ( $pad{I1299} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1299} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1298} ) or 1 )
                       &&                         (   ( $pad{I1300} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1300} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1297} ) && 0 ) )
               &&
                  (   ( $pad{I1301} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '}' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1301} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1302} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '(' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1302} ) && 0 ) )
               &&
                  (   ( $pad{I1303} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1304} = $pos or 1 )
                       && (
                        (   ( $pad{I1305} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1305} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1304} ) or 1 )
                       &&                         (   ( $pad{I1306} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1306} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1303} ) && 0 ) )
               &&
                  (   ( $pad{I1307} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ')' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1307} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1308} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '<' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1308} ) && 0 ) )
               &&
                  (   ( $pad{I1309} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1310} = $pos or 1 )
                       && (
                        (   ( $pad{I1311} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1311} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1310} ) or 1 )
                       &&                         (   ( $pad{I1312} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1312} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1309} ) && 0 ) )
               &&
                  (   ( $pad{I1313} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1313} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1314} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1315} = $pos or 1 )
                       && (
                        (   ( $pad{I1316} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1316} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1315} ) or 1 )
                       &&                         (   ( $pad{I1317} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '>' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1317} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1315} ) or 1 )
                       &&                         (   ( $pad{I1318} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '=' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1318} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1315} ) or 1 )
                       &&                         (   ( $pad{I1319} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '-' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1319} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1314} ) && 0 ) )
               &&
                  (   ( $pad{I1320} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1320} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                 (   ( $pad{I1321} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1321} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                 (   ( $pad{I1322} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\}\)\>]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1322} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1286} ) && 0 ) )
       && do { while (
        (   ( $pad{I1286} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1287} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1288} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1288} ) && 0 ) )
               &&
                  (   ( $pad{I1289} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1289} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1290} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1290} ) && 0 ) )
               &&
                  (   ( $pad{I1291} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1291} ) && 0 ) )
               &&
                  (   ( $pad{I1292} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1292} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1293} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1293} ) && 0 ) )
               &&
                  (   ( $pad{I1294} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1294} ) && 0 ) )
               &&
                  (   ( $pad{I1295} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '"' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1295} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1296} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '{' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1296} ) && 0 ) )
               &&
                  (   ( $pad{I1297} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1298} = $pos or 1 )
                       && (
                        (   ( $pad{I1299} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1299} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1298} ) or 1 )
                       &&                         (   ( $pad{I1300} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1300} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1297} ) && 0 ) )
               &&
                  (   ( $pad{I1301} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '}' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1301} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1302} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '(' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1302} ) && 0 ) )
               &&
                  (   ( $pad{I1303} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1304} = $pos or 1 )
                       && (
                        (   ( $pad{I1305} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1305} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1304} ) or 1 )
                       &&                         (   ( $pad{I1306} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1306} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1303} ) && 0 ) )
               &&
                  (   ( $pad{I1307} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ')' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1307} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1308} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '<' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1308} ) && 0 ) )
               &&
                  (   ( $pad{I1309} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1310} = $pos or 1 )
                       && (
                        (   ( $pad{I1311} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1311} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1310} ) or 1 )
                       &&                         (   ( $pad{I1312} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1312} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1309} ) && 0 ) )
               &&
                  (   ( $pad{I1313} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1313} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                (
                  (   ( $pad{I1314} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1315} = $pos or 1 )
                       && (
                        (   ( $pad{I1316} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1316} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1315} ) or 1 )
                       &&                         (   ( $pad{I1317} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '>' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1317} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1315} ) or 1 )
                       &&                         (   ( $pad{I1318} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '=' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1318} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1315} ) or 1 )
                       &&                         (   ( $pad{I1319} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '-' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1319} ) && 0 ) )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1314} ) && 0 ) )
               &&
                  (   ( $pad{I1320} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1320} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                 (   ( $pad{I1321} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1321} ) && 0 ) )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1287} ) or 1 )
               &&                 (   ( $pad{I1322} = $pos or 1 ) && 
                 ## <perl5>
                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\}\)\>]))\X)/ )
                     ? ( $pos += length( $1 ) or 1 )
                     : 0
                 )
                 ## </perl5>
 ||    ( ( $pos = $pad{I1322} ) && 0 ) )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1286} ) && 0 ) )) {}; $bool = 1 }
       ) ;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for string_code
 # Data::Bind->sub_signature
#  (\&string_code, );
;
*parsed_code = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1323} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1323} ) && 0 ) )
       &&
          (   ( $pad{I1324} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return('{' . $::_V6_MATCH_ . '}');
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1324} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for parsed_code
 # Data::Bind->sub_signature
#  (\&parsed_code, );
;
*named_capture_body = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1325} = $pos or 1 )
           && (
           (
              (   ( $pad{I1326} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '(' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1326} ) && 0 ) )
           &&
              (   ( $pad{I1327} = $pos or 1 ) && 
               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1327} ) && 0 ) )
           &&
              (   ( $pad{I1328} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ')' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1328} ) && 0 ) )
           &&
              (   ( $pad{I1329} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'capturing_group' => ${ $::_V6_MATCH_->{'rule'}}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1329} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1325} ) or 1 )
           &&            (
              (   ( $pad{I1330} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '[' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1330} ) && 0 ) )
           &&
              (   ( $pad{I1331} = $pos or 1 ) && 
               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1331} ) && 0 ) )
           &&
              (   ( $pad{I1332} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1332} ) && 0 ) )
           &&
              (   ( $pad{I1333} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'rule'}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1333} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1325} ) or 1 )
           &&            (
              (   ( $pad{I1334} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '<' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1334} ) && 0 ) )
           &&
              (   ( $pad{I1335} = $pos or 1 ) && 
               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                           $grammar->parse_metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'parse_metasyntax'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1335} ) && 0 ) )
           &&
              (   ( $pad{I1336} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'parse_metasyntax'}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1336} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1325} ) or 1 )
           &&            (
              (   ( $pad{I1337} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq chr(39) )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1337} ) && 0 ) )
           &&
              (   ( $pad{I1338} = $pos or 1 ) && 
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match = 
                           $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               } ||    ( ( $pos = $pad{I1338} ) && 0 ) )
           &&
              (   ( $pad{I1339} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq chr(39) )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1339} ) && 0 ) )
           &&
              (   ( $pad{I1340} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'metasyntax' => {'metasyntax' =>  "" . ${$::_V6_MATCH_}}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1340} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1325} ) or 1 )
           &&             (   ( $pad{I1341} = $pos or 1 ) && 
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { die("invalid alias syntax");
return( undef );
 } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1341} ) && 0 ) )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for named_capture_body
 # Data::Bind->sub_signature
#  (\&named_capture_body, );
;
*parse_metasyntax = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1342} = $pos or 1 ) && 
           do{
                my $from = $pos;
                my $bool =                    ## <alt>
                   (
                       ( $pad{I1343} = $pos or 1 )
                       && (
                        (   ( $pad{I1344} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '!' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1344} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1343} ) or 1 )
                       &&                         (   ( $pad{I1345} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq '?' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1345} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1343} ) or 1 )
                       &&                         (   ( $pad{I1346} = $pos or 1 ) && 
 1 # null constant
 ||    ( ( $pos = $pad{I1346} ) && 0 ) )
                     )
                   )
## </alt>
;
                my $match = Pugs::Runtime::Match->new(
                    { str => \$s, from => \$from, match => [], named => {}, bool => \1, to => \(0+$pos), capture => undef }
                ); $named{'modifier'} = $match;
                $bool
            } ||    ( ( $pos = $pad{I1342} ) && 0 ) )
       &&
          (   ( $pad{I1347} = $pos or 1 ) && 
           ## <alt>
           (
               ( $pad{I1348} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1349} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '{' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1349} ) && 0 ) )
               &&
                  (   ( $pad{I1350} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'parsed_code'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1350} ) && 0 ) )
               &&
                  (   ( $pad{I1351} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 2 ) eq '}>' )
                       ? ( $pos += 2 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1351} ) && 0 ) )
               &&
                  (   ( $pad{I1352} = $pos or 1 ) && 
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'closure' => {'closure' => ${ $::_V6_MATCH_->{'parsed_code'}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1352} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1348} ) or 1 )
               &&                (
                  (   ( $pad{I1353} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1353} ) && 0 ) )
               &&
                 (
                  (   ( $pad{I1354} = $pos or 1 ) && 
                     do{
                         my $hash = do {
                           my $bool = 1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool = 0 unless
                             (
                                (   ( $pad{I1355} = $pos or 1 ) && 
                                 ## <perl5>
                                 ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                     ? ( $pos += length( $1 ) or 1 )
                                     : 0
                                 )
                                 ## </perl5>
 ||    ( ( $pos = $pad{I1355} ) && 0 ) )
                             &&
                                (   ( $pad{I1356} = $pos or 1 ) && 
                                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                             $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1356} ) && 0 ) )
                             );
                           { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                         };
                         my $bool = ${$hash->{'bool'}};
                         if ( $bool ) {
                             push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                         }
                         $bool;
                     } ||    ( ( $pos = $pad{I1354} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1354} = $pos or 1 ) && 
                     do{
                         my $hash = do {
                           my $bool = 1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool = 0 unless
                             (
                                (   ( $pad{I1355} = $pos or 1 ) && 
                                 ## <perl5>
                                 ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                     ? ( $pos += length( $1 ) or 1 )
                                     : 0
                                 )
                                 ## </perl5>
 ||    ( ( $pos = $pad{I1355} ) && 0 ) )
                             &&
                                (   ( $pad{I1356} = $pos or 1 ) && 
                                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                             $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1356} ) && 0 ) )
                             );
                           { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                         };
                         my $bool = ${$hash->{'bool'}};
                         if ( $bool ) {
                             push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                         }
                         $bool;
                     } ||    ( ( $pos = $pad{I1354} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1357} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1357} ) && 0 ) )
               &&
                  (   ( $pad{I1358} = $pos or 1 ) && 
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { if(${ $::_V6_MATCH_->{'modifier'}} eq '!') { return({'negate' => {'char_class' => ['+' .  $::_V6_MATCH_->{'char_class'},
@{($::_V6_MATCH_->[0])}]}});
 } 
;
return({'char_class' => ['+' .  $::_V6_MATCH_->{'char_class'},
@{($::_V6_MATCH_->[0])}]});
 } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1358} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1348} ) or 1 )
               &&                (
                  (   ( $pad{I1359} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->ident( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'ident'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1359} ) && 0 ) )
               &&
                  (   ( $pad{I1360} = $pos or 1 ) && 
                   ## <alt>
                   (
                       ( $pad{I1361} = $pos or 1 )
                       && (
                       (
                          (   ( $pad{I1362} = $pos or 1 ) && 
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match = 
                                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           } ||    ( ( $pos = $pad{I1362} ) && 0 ) )
                       &&
                          (   ( $pad{I1363} = $pos or 1 ) && 
                           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1363} ) && 0 ) )
                       &&
                          (   ( $pad{I1364} = $pos or 1 ) && 
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '>' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
 ||    ( ( $pos = $pad{I1364} ) && 0 ) )
                       &&
                          (   ( $pad{I1365} = $pos or 1 ) && 
                           do { 
                             local $::_V6_SUCCEED = 1;
                             $::_V6_MATCH_ = $m;
                             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { if((${ $::_V6_MATCH_->{'ident'}} eq 'before' || ${ $::_V6_MATCH_->{'ident'}} eq 'after')) { return({${ $::_V6_MATCH_->{'ident'}} => {'rule' => ${ $::_V6_MATCH_->{'rule'}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } 
;
return({'metasyntax' => {'metasyntax' => ${ $::_V6_MATCH_->{'ident'}}, 'rule' => ${ $::_V6_MATCH_->{'rule'}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } }->( $m ) ); 
                             $bool = $::_V6_SUCCEED;
                             $::_V6_MATCH_ = $m if $bool; 
                             return $m if $bool; 
                           } ||    ( ( $pos = $pad{I1365} ) && 0 ) )
                       )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1361} ) or 1 )
                       &&                        (
                          (   ( $pad{I1366} = $pos or 1 ) && 
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq ':' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
 ||    ( ( $pos = $pad{I1366} ) && 0 ) )
                       &&
                         (
                          (   ( $pad{I1367} = $pos or 1 ) && 
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match = 
                                         $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             } ||    ( ( $pos = $pad{I1367} ) && 0 ) )
                         || ( $bool = 1 )
                         ) 
                       &&
                          (   ( $pad{I1368} = $pos or 1 ) && 
                           do{
                my $from = $pos;
                my $bool =                                    do { while (
                                    (   ( $pad{I1369} = $pos or 1 ) && 
                                       ## <alt>
                                       (
                                           ( $pad{I1370} = $pos or 1 )
                                           && (
                                           (
                                              (   ( $pad{I1371} = $pos or 1 ) && 
                                               ## <constant>
                                               ( ( substr( $s, $pos, 1 ) eq '\\' )
                                                   ? ( $pos += 1 or 1 )
                                                   : 0
                                               )
                                               ## </constant>
 ||    ( ( $pos = $pad{I1371} ) && 0 ) )
                                           &&
                                              (   ( $pad{I1372} = $pos or 1 ) && 
                                               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                                           $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'special_char'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1372} ) && 0 ) )
                                           )
                                           )
                                         || (
                                           ( ( $bool = 1 ) && ( $pos = $pad{I1370} ) or 1 )
                                           &&                                             (   ( $pad{I1373} = $pos or 1 ) && 
                                             do{
                my $match = 
          do {
            our $I1374;
            our $I1374_sizes;
            unless ( $I1374 ) {
                my $hash = \%Pugs::Grammar::Rule::variables;
                my %sizes = map { length($_) => 1 } keys %$hash;
                $I1374_sizes = [ sort { $b <=> $a } keys %sizes ];
                $I1374 = $hash;
            }
            my $match = 0;
            my $key;
            for ( @$I1374_sizes ) {
                $key = ( $pos <= length( $s )
                            ? substr( $s, $pos, $_ )
                            : '' );
                if ( exists $I1374->{$key} ) {
                    #$named{KEY} = $key;
                    #$::_V6_MATCH_ = $m;
                    #print "m: ", Dumper( $::_V6_MATCH_->data )
                    #    if ( $key eq 'until' );
                    $match = Pugs::Runtime::Regex::preprocess_hash( $I1374, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                    last if $match;
                }
            }
            if ( $match ) {
                $pos = $match->to;
                #print "match: $key at $pos = ", Dumper( $match->data );
                $bool = 1;
            }; # else { $bool = 0 }
            $match;
          };
                if ( $match ) { push @{$named{'Pugs::Grammar::Rule::variables'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1373} ) && 0 ) )
                                           )
                                         || (
                                           ( ( $bool = 1 ) && ( $pos = $pad{I1370} ) or 1 )
                                           &&                                             (   ( $pad{I1375} = $pos or 1 ) && 
                                             ## <perl5>
                                             ( ( substr( $s, $pos ) =~ m/^((?:(?![\>]))\X)/ )
                                                 ? ( $pos += length( $1 ) or 1 )
                                                 : 0
                                             )
                                             ## </perl5>
 ||    ( ( $pos = $pad{I1375} ) && 0 ) )
                                         )
                                       )
## </alt>
 ||    ( ( $pos = $pad{I1369} ) && 0 ) )) {}; $bool = 1 };
                my $match = Pugs::Runtime::Match->new(
                    { str => \$s, from => \$from, match => [], named => {}, bool => \1, to => \(0+$pos), capture => undef }
                ); $named{'str'} = $match;
                $bool
            } ||    ( ( $pos = $pad{I1368} ) && 0 ) )
                       &&
                          (   ( $pad{I1376} = $pos or 1 ) && 
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '>' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
 ||    ( ( $pos = $pad{I1376} ) && 0 ) )
                       &&
                          (   ( $pad{I1377} = $pos or 1 ) && 
                           do { 
                             local $::_V6_SUCCEED = 1;
                             $::_V6_MATCH_ = $m;
                             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { if((${ $::_V6_MATCH_->{'ident'}} eq 'before' || ${ $::_V6_MATCH_->{'ident'}} eq 'after')) { return({${ $::_V6_MATCH_->{'ident'}} => {'rule' => {'metasyntax' => {'metasyntax' => '\'' . ${ $::_V6_MATCH_->{'str'}} . '\''}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } 
;
return({'metasyntax' => {'metasyntax' => ${ $::_V6_MATCH_->{'ident'}}, 'string' => ${ $::_V6_MATCH_->{'str'}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } }->( $m ) ); 
                             $bool = $::_V6_SUCCEED;
                             $::_V6_MATCH_ = $m if $bool; 
                             return $m if $bool; 
                           } ||    ( ( $pos = $pad{I1377} ) && 0 ) )
                       )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1361} ) or 1 )
                       &&                        (
                          (   ( $pad{I1378} = $pos or 1 ) && 
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '(' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
 ||    ( ( $pos = $pad{I1378} ) && 0 ) )
                       &&
                          (   ( $pad{I1379} = $pos or 1 ) && 
                           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                       $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'parsed_code'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1379} ) && 0 ) )
                       &&
                          (   ( $pad{I1380} = $pos or 1 ) && 
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq ')' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
 ||    ( ( $pos = $pad{I1380} ) && 0 ) )
                       &&
                          (   ( $pad{I1381} = $pos or 1 ) && 
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '>' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
 ||    ( ( $pos = $pad{I1381} ) && 0 ) )
                       &&
                          (   ( $pad{I1382} = $pos or 1 ) && 
                           do { 
                             local $::_V6_SUCCEED = 1;
                             $::_V6_MATCH_ = $m;
                             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'call' => {'method' => ${ $::_V6_MATCH_->{'ident'}}, 'params' => ${ $::_V6_MATCH_->{'parsed_code'}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } }->( $m ) ); 
                             $bool = $::_V6_SUCCEED;
                             $::_V6_MATCH_ = $m if $bool; 
                             return $m if $bool; 
                           } ||    ( ( $pos = $pad{I1382} ) && 0 ) )
                       )
                     )
                   )
## </alt>
 ||    ( ( $pos = $pad{I1360} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1348} ) or 1 )
               &&                (
                  (   ( $pad{I1383} = $pos or 1 ) && 
                   do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                               $grammar->metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'metasyntax'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1383} ) && 0 ) )
               &&
                  (   ( $pad{I1384} = $pos or 1 ) && 
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '>' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
 ||    ( ( $pos = $pad{I1384} ) && 0 ) )
               &&
                  (   ( $pad{I1385} = $pos or 1 ) && 
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'metasyntax' => {'metasyntax' =>  "" . ${ $::_V6_MATCH_->{'metasyntax'}}, 'modifier' => ${ $::_V6_MATCH_->{'modifier'}}}});
 } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1385} ) && 0 ) )
               )
             )
           )
## </alt>
 ||    ( ( $pos = $pad{I1347} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for parse_metasyntax
 # Data::Bind->sub_signature
#  (\&parse_metasyntax, );
;
%variables = ('$<' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1386} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ident( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'ident'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1386} ) && 0 ) )
       &&
          (   ( $pad{I1387} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq '>' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1387} ) && 0 ) )
       &&
          (   ( $pad{I1388} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'match_variable' => '$' .  $::_V6_MATCH_->{'ident'}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1388} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'$' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1389} = $pos or 1 )
           && (
           (
             (
              (   ( $pad{I1390} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1390} ) && 0 ) )
             && do { while (
              (   ( $pad{I1390} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1390} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1391} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'match_variable' => '$' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1391} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1389} ) or 1 )
           &&            (
             (
              (   ( $pad{I1392} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '^' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1392} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
              (   ( $pad{I1393} = $pos or 1 ) && 
                 ## <alt>
                 (
                     ( $pad{I1394} = $pos or 1 )
                     && (
                      (   ( $pad{I1395} = $pos or 1 ) && 
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match = 
                                   $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       } ||    ( ( $pos = $pad{I1395} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1394} ) or 1 )
                     &&                       (   ( $pad{I1396} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '_' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1396} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1394} ) or 1 )
                     &&                      (
                        (   ( $pad{I1397} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1397} ) && 0 ) )
                     &&
                        (   ( $pad{I1398} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1398} ) && 0 ) )
                     )
                   )
                 )
## </alt>
 ||    ( ( $pos = $pad{I1393} ) && 0 ) )
             && do { while (
              (   ( $pad{I1393} = $pos or 1 ) && 
                 ## <alt>
                 (
                     ( $pad{I1394} = $pos or 1 )
                     && (
                      (   ( $pad{I1395} = $pos or 1 ) && 
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match = 
                                   $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       } ||    ( ( $pos = $pad{I1395} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1394} ) or 1 )
                     &&                       (   ( $pad{I1396} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '_' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1396} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1394} ) or 1 )
                     &&                      (
                        (   ( $pad{I1397} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1397} ) && 0 ) )
                     &&
                        (   ( $pad{I1398} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1398} ) && 0 ) )
                     )
                   )
                 )
## </alt>
 ||    ( ( $pos = $pad{I1393} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1399} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'variable' => '$' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1399} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'@' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1400} = $pos or 1 )
           && (
           (
             (
              (   ( $pad{I1401} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1401} ) && 0 ) )
             && do { while (
              (   ( $pad{I1401} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1401} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1402} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'match_variable' => '@' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1402} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1400} ) or 1 )
           &&            (
             (
              (   ( $pad{I1403} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '^' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1403} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
              (   ( $pad{I1404} = $pos or 1 ) && 
                 ## <alt>
                 (
                     ( $pad{I1405} = $pos or 1 )
                     && (
                      (   ( $pad{I1406} = $pos or 1 ) && 
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match = 
                                   $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       } ||    ( ( $pos = $pad{I1406} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1405} ) or 1 )
                     &&                       (   ( $pad{I1407} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '_' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1407} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1405} ) or 1 )
                     &&                      (
                        (   ( $pad{I1408} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1408} ) && 0 ) )
                     &&
                        (   ( $pad{I1409} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1409} ) && 0 ) )
                     )
                   )
                 )
## </alt>
 ||    ( ( $pos = $pad{I1404} ) && 0 ) )
             && do { while (
              (   ( $pad{I1404} = $pos or 1 ) && 
                 ## <alt>
                 (
                     ( $pad{I1405} = $pos or 1 )
                     && (
                      (   ( $pad{I1406} = $pos or 1 ) && 
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match = 
                                   $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       } ||    ( ( $pos = $pad{I1406} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1405} ) or 1 )
                     &&                       (   ( $pad{I1407} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '_' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1407} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1405} ) or 1 )
                     &&                      (
                        (   ( $pad{I1408} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1408} ) && 0 ) )
                     &&
                        (   ( $pad{I1409} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1409} ) && 0 ) )
                     )
                   )
                 )
## </alt>
 ||    ( ( $pos = $pad{I1404} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1410} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'variable' => '@' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1410} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'%' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1411} = $pos or 1 )
           && (
           (
             (
              (   ( $pad{I1412} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1412} ) && 0 ) )
             && do { while (
              (   ( $pad{I1412} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1412} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1413} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'match_variable' => '%' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1413} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1411} ) or 1 )
           &&            (
             (
              (   ( $pad{I1414} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '^' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1414} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
              (   ( $pad{I1415} = $pos or 1 ) && 
                 ## <alt>
                 (
                     ( $pad{I1416} = $pos or 1 )
                     && (
                      (   ( $pad{I1417} = $pos or 1 ) && 
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match = 
                                   $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       } ||    ( ( $pos = $pad{I1417} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1416} ) or 1 )
                     &&                       (   ( $pad{I1418} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '_' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1418} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1416} ) or 1 )
                     &&                      (
                        (   ( $pad{I1419} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1419} ) && 0 ) )
                     &&
                        (   ( $pad{I1420} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1420} ) && 0 ) )
                     )
                   )
                 )
## </alt>
 ||    ( ( $pos = $pad{I1415} ) && 0 ) )
             && do { while (
              (   ( $pad{I1415} = $pos or 1 ) && 
                 ## <alt>
                 (
                     ( $pad{I1416} = $pos or 1 )
                     && (
                      (   ( $pad{I1417} = $pos or 1 ) && 
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match = 
                                   $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       } ||    ( ( $pos = $pad{I1417} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1416} ) or 1 )
                     &&                       (   ( $pad{I1418} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '_' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1418} ) && 0 ) )
                     )
                   || (
                     ( ( $bool = 1 ) && ( $pos = $pad{I1416} ) or 1 )
                     &&                      (
                        (   ( $pad{I1419} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1419} ) && 0 ) )
                     &&
                        (   ( $pad{I1420} = $pos or 1 ) && 
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq ':' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
 ||    ( ( $pos = $pad{I1420} ) && 0 ) )
                     )
                   )
                 )
## </alt>
 ||    ( ( $pos = $pad{I1415} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1421} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'variable' => '%' . $::_V6_MATCH_});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1421} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
);
%rule_terms = ('{*}' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1422} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'metasyntax' => {'metasyntax' => 'null'}});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1422} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'\'' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1423} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1423} ) && 0 ) )
       &&
          (   ( $pad{I1424} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq chr(39) )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1424} ) && 0 ) )
       &&
          (   ( $pad{I1425} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'metasyntax' => {'metasyntax' => '\'' . ${$::_V6_MATCH_}}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1425} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'(' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1426} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1426} ) && 0 ) )
       &&
          (   ( $pad{I1427} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq ')' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1427} ) && 0 ) )
       &&
          (   ( $pad{I1428} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'capturing_group' => ${ $::_V6_MATCH_->{'rule'}}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1428} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'<(' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1429} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1429} ) && 0 ) )
       &&
          (   ( $pad{I1430} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 2 ) eq ')>' )
               ? ( $pos += 2 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1430} ) && 0 ) )
       &&
          (   ( $pad{I1431} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'capture_as_result' => ${ $::_V6_MATCH_->{'rule'}}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1431} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'<+' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1432} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1432} ) && 0 ) )
       &&
         do { while (
          (   ( $pad{I1433} = $pos or 1 ) && 
             do{
                 my $hash = do {
                   my $bool = 1;
                   my $from = $pos;
                   my @match;
                   my %named;
                   $bool = 0 unless
                     (
                        (   ( $pad{I1434} = $pos or 1 ) && 
                         ## <perl5>
                         ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                             ? ( $pos += length( $1 ) or 1 )
                             : 0
                         )
                         ## </perl5>
 ||    ( ( $pos = $pad{I1434} ) && 0 ) )
                     &&
                        (   ( $pad{I1435} = $pos or 1 ) && 
                         do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                     $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1435} ) && 0 ) )
                     );
                   { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                 };
                 my $bool = ${$hash->{'bool'}};
                 if ( $bool ) {
                     push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                 }
                 $bool;
             } ||    ( ( $pos = $pad{I1433} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1436} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq '>' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1436} ) && 0 ) )
       &&
          (   ( $pad{I1437} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'char_class' => ['+' .  $::_V6_MATCH_->{'char_class'},
@{($::_V6_MATCH_->[0])}]});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1437} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'<-' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1438} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1438} ) && 0 ) )
       &&
         do { while (
          (   ( $pad{I1439} = $pos or 1 ) && 
             do{
                 my $hash = do {
                   my $bool = 1;
                   my $from = $pos;
                   my @match;
                   my %named;
                   $bool = 0 unless
                     (
                        (   ( $pad{I1440} = $pos or 1 ) && 
                         ## <perl5>
                         ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                             ? ( $pos += length( $1 ) or 1 )
                             : 0
                         )
                         ## </perl5>
 ||    ( ( $pos = $pad{I1440} ) && 0 ) )
                     &&
                        (   ( $pad{I1441} = $pos or 1 ) && 
                         do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                     $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1441} ) && 0 ) )
                     );
                   { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                 };
                 my $bool = ${$hash->{'bool'}};
                 if ( $bool ) {
                     push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                 }
                 $bool;
             } ||    ( ( $pos = $pad{I1439} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1442} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq '>' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1442} ) && 0 ) )
       &&
          (   ( $pad{I1443} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'char_class' => ['-' .  $::_V6_MATCH_->{'char_class'},
@{($::_V6_MATCH_->[0])}]});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1443} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'<[' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1444} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->char_range( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_range'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1444} ) && 0 ) )
       &&
          (   ( $pad{I1445} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq ']' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1445} ) && 0 ) )
       &&
         do { while (
          (   ( $pad{I1446} = $pos or 1 ) && 
             do{
                 my $hash = do {
                   my $bool = 1;
                   my $from = $pos;
                   my @match;
                   my %named;
                   $bool = 0 unless
                     (
                        (   ( $pad{I1447} = $pos or 1 ) && 
                         ## <perl5>
                         ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                             ? ( $pos += length( $1 ) or 1 )
                             : 0
                         )
                         ## </perl5>
 ||    ( ( $pos = $pad{I1447} ) && 0 ) )
                     &&
                        (   ( $pad{I1448} = $pos or 1 ) && 
                         do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                     $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'char_class'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1448} ) && 0 ) )
                     );
                   { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                 };
                 my $bool = ${$hash->{'bool'}};
                 if ( $bool ) {
                     push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                 }
                 $bool;
             } ||    ( ( $pos = $pad{I1446} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1449} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq '>' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1449} ) && 0 ) )
       &&
          (   ( $pad{I1450} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'char_class' => ['+[' .  $::_V6_MATCH_->{'char_range'} . ']',
@{($::_V6_MATCH_->[0])}]});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1450} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'<' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1451} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->parse_metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'parse_metasyntax'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1451} ) && 0 ) )
       &&
          (   ( $pad{I1452} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'parse_metasyntax'}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1452} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'{' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1453} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'parsed_code'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1453} ) && 0 ) )
       &&
          (   ( $pad{I1454} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq '}' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1454} ) && 0 ) )
       &&
          (   ( $pad{I1455} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'closure' => {'closure' => ${ $::_V6_MATCH_->{'parsed_code'}}, 'modifier' => 'plain'}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1455} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'\\' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1456} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'special_char'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1456} ) && 0 ) )
       &&
          (   ( $pad{I1457} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'special_char'}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1457} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'.' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1458} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'dot' => 1});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1458} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'[' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1459} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1459} ) && 0 ) )
       &&
          (   ( $pad{I1460} = $pos or 1 ) && 
           ## <constant>
           ( ( substr( $s, $pos, 1 ) eq ']' )
               ? ( $pos += 1 or 1 )
               : 0
           )
           ## </constant>
 ||    ( ( $pos = $pad{I1460} ) && 0 ) )
       &&
          (   ( $pad{I1461} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'rule'}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1461} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':::' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1462} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => ':::'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1462} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':?' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1463} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => ':?'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1463} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':+' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1464} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => ':+'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1464} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'::' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1465} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '::'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1465} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1466} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => ':'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1466} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'$$' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1467} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '$$'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1467} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'$' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1468} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '$'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1468} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'^^' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1469} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '^^'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1469} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'^' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1470} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '^'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1470} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'>>' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1471} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '>>'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1471} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'»' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1472} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '>>'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1472} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'<<' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1473} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '<<'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1473} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
'«' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
        (   ( $pad{I1474} = $pos or 1 ) && 
         do { 
           local $::_V6_SUCCEED = 1;
           $::_V6_MATCH_ = $m;
           $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'colon' => '<<'});
 } }->( $m ) ); 
           $bool = $::_V6_SUCCEED;
           $::_V6_MATCH_ = $m if $bool; 
           return $m if $bool; 
         } ||    ( ( $pos = $pad{I1474} ) && 0 ) );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':i' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1475} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1475} ) && 0 ) )
       &&
          (   ( $pad{I1476} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1476} ) && 0 ) )
       &&
          (   ( $pad{I1477} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => {'modifier' => 'ignorecase', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1477} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':ignorecase' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1478} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1478} ) && 0 ) )
       &&
          (   ( $pad{I1479} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1479} ) && 0 ) )
       &&
          (   ( $pad{I1480} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => {'modifier' => 'ignorecase', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1480} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':s' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1481} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1481} ) && 0 ) )
       &&
          (   ( $pad{I1482} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1482} ) && 0 ) )
       &&
          (   ( $pad{I1483} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'sigspace', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1483} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':sigspace' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1484} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1484} ) && 0 ) )
       &&
          (   ( $pad{I1485} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1485} ) && 0 ) )
       &&
          (   ( $pad{I1486} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'sigspace', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1486} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':P5' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1487} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1487} ) && 0 ) )
       &&
          (   ( $pad{I1488} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1488} ) && 0 ) )
       &&
          (   ( $pad{I1489} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'Perl5', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1489} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':Perl5' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1490} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1490} ) && 0 ) )
       &&
          (   ( $pad{I1491} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1491} ) && 0 ) )
       &&
          (   ( $pad{I1492} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'Perl5', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1492} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':bytes' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1493} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1493} ) && 0 ) )
       &&
          (   ( $pad{I1494} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1494} ) && 0 ) )
       &&
          (   ( $pad{I1495} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'bytes', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1495} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':codes' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1496} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1496} ) && 0 ) )
       &&
          (   ( $pad{I1497} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1497} ) && 0 ) )
       &&
          (   ( $pad{I1498} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'codes', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1498} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':graphs' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1499} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1499} ) && 0 ) )
       &&
          (   ( $pad{I1500} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1500} ) && 0 ) )
       &&
          (   ( $pad{I1501} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'graphs', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1501} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
,
':langs' => ## <global>
do { my $rule; $rule = sub {
  
              my $s       = $_[0] || '';
              my $grammar = $_[1] || __PACKAGE__;
              $_[3] = $_[2]; 
              $_[2] = undef;
            
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1502} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                my $bool = (!$match != 1);
                $pos = $match->to if $bool;
                $match;
           } ||    ( ( $pos = $pad{I1502} ) && 0 ) )
       &&
          (   ( $pad{I1503} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'rule'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1503} ) && 0 ) )
       &&
          (   ( $pad{I1504} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'modifier' => 'langs', 'rule'=>${ $::_V6_MATCH_->{ qw(rule) }}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1504} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
);
*term = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1505} = $pos or 1 )
           && (
           (
              (   ( $pad{I1506} = $pos or 1 ) && 
               do{
                my $match = 
          do {
            our $I1507;
            our $I1507_sizes;
            unless ( $I1507 ) {
                my $hash = \%Pugs::Grammar::Rule::variables;
                my %sizes = map { length($_) => 1 } keys %$hash;
                $I1507_sizes = [ sort { $b <=> $a } keys %sizes ];
                $I1507 = $hash;
            }
            my $match = 0;
            my $key;
            for ( @$I1507_sizes ) {
                $key = ( $pos <= length( $s )
                            ? substr( $s, $pos, $_ )
                            : '' );
                if ( exists $I1507->{$key} ) {
                    #$named{KEY} = $key;
                    #$::_V6_MATCH_ = $m;
                    #print "m: ", Dumper( $::_V6_MATCH_->data )
                    #    if ( $key eq 'until' );
                    $match = Pugs::Runtime::Regex::preprocess_hash( $I1507, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                    last if $match;
                }
            }
            if ( $match ) {
                $pos = $match->to;
                #print "match: $key at $pos = ", Dumper( $match->data );
                $bool = 1;
            }; # else { $bool = 0 }
            $match;
          };
                if ( $match ) { $named{'Pugs::Grammar::Rule::variables'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1506} ) && 0 ) )
           &&
              (   ( $pad{I1508} = $pos or 1 ) && 
               ## <alt>
               (
                   ( $pad{I1509} = $pos or 1 )
                   && (
                   (
                     (
                      (   ( $pad{I1510} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1510} ) && 0 ) )
                     || ( $bool = 1 )
                     ) 
                   &&
                      (   ( $pad{I1511} = $pos or 1 ) && 
                       ## <constant>
                       ( ( substr( $s, $pos, 2 ) eq ':=' )
                           ? ( $pos += 2 or 1 )
                           : 0
                       )
                       ## </constant>
 ||    ( ( $pos = $pad{I1511} ) && 0 ) )
                   &&
                     (
                      (   ( $pad{I1512} = $pos or 1 ) && 
                         do {
                              my $prior = $::_V6_PRIOR_;
                              my $match = 
                                     $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                              $::_V6_PRIOR_ = $prior;
                              my $bool = (!$match != 1);
                              $pos = $match->to if $bool;
                              $match;
                         } ||    ( ( $pos = $pad{I1512} ) && 0 ) )
                     || ( $bool = 1 )
                     ) 
                   &&
                      (   ( $pad{I1513} = $pos or 1 ) && 
                       do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                                   $grammar->named_capture_body( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'named_capture_body'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1513} ) && 0 ) )
                   &&
                      (   ( $pad{I1514} = $pos or 1 ) && 
                       do { 
                         local $::_V6_SUCCEED = 1;
                         $::_V6_MATCH_ = $m;
                         $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'named_capture' => {'rule' => ${ $::_V6_MATCH_->{'named_capture_body'}}, 'ident' => ${ $::_V6_MATCH_->{'Pugs::Grammar::Rule::variables'}}}});
 } }->( $m ) ); 
                         $bool = $::_V6_SUCCEED;
                         $::_V6_MATCH_ = $m if $bool; 
                         return $m if $bool; 
                       } ||    ( ( $pos = $pad{I1514} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1509} ) or 1 )
                   &&                     (   ( $pad{I1515} = $pos or 1 ) && 
                     do { 
                       local $::_V6_SUCCEED = 1;
                       $::_V6_MATCH_ = $m;
                       $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'Pugs::Grammar::Rule::variables'}});
 } }->( $m ) ); 
                       $bool = $::_V6_SUCCEED;
                       $::_V6_MATCH_ = $m if $bool; 
                       return $m if $bool; 
                     } ||    ( ( $pos = $pad{I1515} ) && 0 ) )
                 )
               )
## </alt>
 ||    ( ( $pos = $pad{I1508} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1505} ) or 1 )
           &&            (
              (   ( $pad{I1516} = $pos or 1 ) && 
               do{
                my $match = 
          do {
            our $I1517;
            our $I1517_sizes;
            unless ( $I1517 ) {
                my $hash = \%Pugs::Grammar::Rule::rule_terms;
                my %sizes = map { length($_) => 1 } keys %$hash;
                $I1517_sizes = [ sort { $b <=> $a } keys %sizes ];
                $I1517 = $hash;
            }
            my $match = 0;
            my $key;
            for ( @$I1517_sizes ) {
                $key = ( $pos <= length( $s )
                            ? substr( $s, $pos, $_ )
                            : '' );
                if ( exists $I1517->{$key} ) {
                    #$named{KEY} = $key;
                    #$::_V6_MATCH_ = $m;
                    #print "m: ", Dumper( $::_V6_MATCH_->data )
                    #    if ( $key eq 'until' );
                    $match = Pugs::Runtime::Regex::preprocess_hash( $I1517, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                    last if $match;
                }
            }
            if ( $match ) {
                $pos = $match->to;
                #print "match: $key at $pos = ", Dumper( $match->data );
                $bool = 1;
            }; # else { $bool = 0 }
            $match;
          };
                if ( $match ) { $named{'Pugs::Grammar::Rule::rule_terms'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1516} ) && 0 ) )
           &&
              (   ( $pad{I1518} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return(${ $::_V6_MATCH_->{'Pugs::Grammar::Rule::rule_terms'}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1518} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1505} ) or 1 )
           &&            (
              (   ( $pad{I1519} = $pos or 1 ) && 
               ## <perl5>
               ( ( substr( $s, $pos ) =~ m/^((?:(?![\]\}\)\>\:\?\+\*\|\&]))\X)/ )
                   ? ( $pos += length( $1 ) or 1 )
                   : 0
               )
               ## </perl5>
 ||    ( ( $pos = $pad{I1519} ) && 0 ) )
           &&
              (   ( $pad{I1520} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'constant' => ${$::_V6_MATCH_}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1520} ) && 0 ) )
           )
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for term
 # Data::Bind->sub_signature
#  (\&term, );
;
*quant = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       ## <alt>
       (
           ( $pad{I1521} = $pos or 1 )
           && (
           (
              (   ( $pad{I1522} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 2 ) eq '**' )
                   ? ( $pos += 2 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1522} ) && 0 ) )
           &&
             (
              (   ( $pad{I1523} = $pos or 1 ) && 
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match = 
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 } ||    ( ( $pos = $pad{I1523} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
              (   ( $pad{I1524} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '{' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1524} ) && 0 ) )
           &&
              (   ( $pad{I1525} = $pos or 1 ) && 
               do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                           $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'parsed_code'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1525} ) && 0 ) )
           &&
              (   ( $pad{I1526} = $pos or 1 ) && 
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '}' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
 ||    ( ( $pos = $pad{I1526} ) && 0 ) )
           &&
              (   ( $pad{I1527} = $pos or 1 ) && 
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { return({'closure' => ${ $::_V6_MATCH_->{'parsed_code'}}});
 } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1527} ) && 0 ) )
           )
           )
         || (
           ( ( $bool = 1 ) && ( $pos = $pad{I1521} ) or 1 )
           &&            (
            (   ( $pad{I1528} = $pos or 1 ) && 
               ## <perl5>
               ( ( substr( $s, $pos ) =~ m/^((?:(?=[\?\*\+]))\X)/ )
                   ? ( $pos += length( $1 ) or 1 )
                   : 0
               )
               ## </perl5>
 ||    ( ( $pos = $pad{I1528} ) && 0 ) )
           || ( $bool = 1 )
           ) 
         )
       )
## </alt>
;
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for quant
 # Data::Bind->sub_signature
#  (\&quant, );
;
*quantifier = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
          (   ( $pad{I1529} = $pos or 1 ) && 
           do{
                my $match = Pugs::Runtime::Match->new( do {
                    my $bool = 1;
                    my $from = $pos;
                    my @match;
                    my %named;
                    $bool = 0 unless                    do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1530} = $pos or 1 ) && 
                               do {
                                    my $prior = $::_V6_PRIOR_;
                                    my $match = 
                                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                    $::_V6_PRIOR_ = $prior;
                                    my $bool = (!$match != 1);
                                    $pos = $match->to if $bool;
                                    $match;
                               } ||    ( ( $pos = $pad{I1530} ) && 0 ) )
                           || ( $bool = 1 )
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   };
                    { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                } );
                if ( $match ) { $named{'ws1'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1529} ) && 0 ) )
       &&
          (   ( $pad{I1531} = $pos or 1 ) && 
           do{
               my $pos1 = $pos;
               do {
                 my $pos = $pos1;
                 my $from = $pos;
                 my @match;
                 my %named;
                 $bool =                      do{
                         my $pos1 = $pos;
                         do {
                           my $pos = $pos1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool = 0 unless
                                (   ( $pad{I1532} = $pos or 1 ) && 
                                 ## <perl5>
                                 ( ( substr( $s, $pos ) =~ m/^((?:(?=[\}\]\)]))\X)/ )
                                     ? ( $pos += length( $1 ) or 1 )
                                     : 0
                                 )
                                 ## </perl5>
 ||    ( ( $pos = $pad{I1532} ) && 0 ) );
                           $bool;
                         };
                     } ? 0 : 1;
                 $bool;
               };
           } ||    ( ( $pos = $pad{I1531} ) && 0 ) )
       &&
          (   ( $pad{I1533} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->term( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'term'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1533} ) && 0 ) )
       &&
          (   ( $pad{I1534} = $pos or 1 ) && 
           do{
                my $match = Pugs::Runtime::Match->new( do {
                    my $bool = 1;
                    my $from = $pos;
                    my @match;
                    my %named;
                    $bool = 0 unless                    do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1535} = $pos or 1 ) && 
                               do {
                                    my $prior = $::_V6_PRIOR_;
                                    my $match = 
                                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                    $::_V6_PRIOR_ = $prior;
                                    my $bool = (!$match != 1);
                                    $pos = $match->to if $bool;
                                    $match;
                               } ||    ( ( $pos = $pad{I1535} ) && 0 ) )
                           || ( $bool = 1 )
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   };
                    { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                } );
                if ( $match ) { $named{'ws2'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1534} ) && 0 ) )
       &&
          (   ( $pad{I1536} = $pos or 1 ) && 
           do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                       $grammar->quant( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { $named{'quant'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1536} ) && 0 ) )
       &&
          (   ( $pad{I1537} = $pos or 1 ) && 
           do{
                my $match = Pugs::Runtime::Match->new( do {
                    my $bool = 1;
                    my $from = $pos;
                    my @match;
                    my %named;
                    $bool = 0 unless                    do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1538} = $pos or 1 ) && 
                               ## <perl5>
                               ( ( substr( $s, $pos ) =~ m/^((?:(?=[\?\+]))\X)/ )
                                   ? ( $pos += length( $1 ) or 1 )
                                   : 0
                               )
                               ## </perl5>
 ||    ( ( $pos = $pad{I1538} ) && 0 ) )
                           || ( $bool = 1 )
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   };
                    { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                } );
                if ( $match ) { $named{'greedy'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1537} ) && 0 ) )
       &&
          (   ( $pad{I1539} = $pos or 1 ) && 
           do{
                my $match = Pugs::Runtime::Match->new( do {
                    my $bool = 1;
                    my $from = $pos;
                    my @match;
                    my %named;
                    $bool = 0 unless                    do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1540} = $pos or 1 ) && 
                               do {
                                    my $prior = $::_V6_PRIOR_;
                                    my $match = 
                                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                    $::_V6_PRIOR_ = $prior;
                                    my $bool = (!$match != 1);
                                    $pos = $match->to if $bool;
                                    $match;
                               } ||    ( ( $pos = $pad{I1540} ) && 0 ) )
                           || ( $bool = 1 )
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   };
                    { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                } );
                if ( $match ) { $named{'ws3'} = $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1539} ) && 0 ) )
       &&
          (   ( $pad{I1541} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { if((${ $::_V6_MATCH_->{'quant'}} eq '' && (${ $::_V6_MATCH_->{'greedy'}} eq '' && (${ $::_V6_MATCH_->{'ws1'}} eq '' && (${ $::_V6_MATCH_->{'ws2'}} eq '' && ${ $::_V6_MATCH_->{'ws3'}} eq ''))))) { return(${ $::_V6_MATCH_->{'term'}});
 } 
;
return({'quant' => {'term' => ${ $::_V6_MATCH_->{'term'}}, 'quant' => ${ $::_V6_MATCH_->{'quant'}}, 'greedy' => ${ $::_V6_MATCH_->{'greedy'}}, 'ws1' => ${ $::_V6_MATCH_->{'ws1'}}, 'ws2' => ${ $::_V6_MATCH_->{'ws2'}}, 'ws3' => ${ $::_V6_MATCH_->{'ws3'}}}});
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1541} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for quantifier
 # Data::Bind->sub_signature
#  (\&quantifier, );
;
*concat = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
         (
          (   ( $pad{I1542} = $pos or 1 ) && 
             do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                         $grammar->quantifier( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'quantifier'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1542} ) && 0 ) )
         && do { while (
          (   ( $pad{I1542} = $pos or 1 ) && 
             do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                         $grammar->quantifier( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'quantifier'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1542} ) && 0 ) )) {}; $bool = 1 }
         ) 
       &&
          (   ( $pad{I1543} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { 
        my @a = map {  $_->()  }  @{ $::_V6_MATCH_->{'quantifier'} };
        return { concat => \@a ,}  if scalar @a > 1;
        return $a[0];;
 # use v6
;
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1543} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for concat
 # Data::Bind->sub_signature
#  (\&concat, );
;
*conjunctive1 = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
         (
          (   ( $pad{I1544} = $pos or 1 ) && 
             (
               (
                (   ( $pad{I1545} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1545} ) && 0 ) )
               || ( $bool = 1 )
               ) 
             &&
                (   ( $pad{I1546} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '&' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1546} ) && 0 ) )
             &&
                (   ( $pad{I1547} = $pos or 1 ) && 
                 do{
                     my $pos1 = $pos;
                     do {
                       my $pos = $pos1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool =                            do{
                               my $pos1 = $pos;
                               do {
                                 my $pos = $pos1;
                                 my $from = $pos;
                                 my @match;
                                 my %named;
                                 $bool = 0 unless
                                      (   ( $pad{I1548} = $pos or 1 ) && 
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq '&' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
 ||    ( ( $pos = $pad{I1548} ) && 0 ) );
                                 $bool;
                               };
                           } ? 0 : 1;
                       $bool;
                     };
                 } ||    ( ( $pos = $pad{I1547} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1544} ) && 0 ) )
         || ( $bool = 1 )
         ) 
       &&
         (
           (   ( $pad{I1549} = $pos or 1 ) && 
             do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                         $grammar->concat( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'concat'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1549} ) && 0 ) )
         ) 
       &&
         do { while (
          (   ( $pad{I1550} = $pos or 1 ) && 
             (
                (   ( $pad{I1551} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '&' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1551} ) && 0 ) )
             &&
                (   ( $pad{I1552} = $pos or 1 ) && 
                 do{
                     my $pos1 = $pos;
                     do {
                       my $pos = $pos1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool =                            do{
                               my $pos1 = $pos;
                               do {
                                 my $pos = $pos1;
                                 my $from = $pos;
                                 my @match;
                                 my %named;
                                 $bool = 0 unless
                                      (   ( $pad{I1553} = $pos or 1 ) && 
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq '&' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
 ||    ( ( $pos = $pad{I1553} ) && 0 ) );
                                 $bool;
                               };
                           } ? 0 : 1;
                       $bool;
                     };
                 } ||    ( ( $pos = $pad{I1552} ) && 0 ) )
             &&
                (   ( $pad{I1554} = $pos or 1 ) && 
                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                             $grammar->concat( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'concat'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1554} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1550} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1555} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { 
        my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'concat'} };
        return { conjunctive1 => \@a ,}  if scalar @a > 1;
        return $a[0];;
 # use v6
;
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1555} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for conjunctive1
 # Data::Bind->sub_signature
#  (\&conjunctive1, );
;
*disjunctive1 = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
         (
          (   ( $pad{I1556} = $pos or 1 ) && 
             (
               (
                (   ( $pad{I1557} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1557} ) && 0 ) )
               || ( $bool = 1 )
               ) 
             &&
                (   ( $pad{I1558} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '|' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1558} ) && 0 ) )
             &&
                (   ( $pad{I1559} = $pos or 1 ) && 
                 do{
                     my $pos1 = $pos;
                     do {
                       my $pos = $pos1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool =                            do{
                               my $pos1 = $pos;
                               do {
                                 my $pos = $pos1;
                                 my $from = $pos;
                                 my @match;
                                 my %named;
                                 $bool = 0 unless
                                      (   ( $pad{I1560} = $pos or 1 ) && 
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq '|' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
 ||    ( ( $pos = $pad{I1560} ) && 0 ) );
                                 $bool;
                               };
                           } ? 0 : 1;
                       $bool;
                     };
                 } ||    ( ( $pos = $pad{I1559} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1556} ) && 0 ) )
         || ( $bool = 1 )
         ) 
       &&
         (
           (   ( $pad{I1561} = $pos or 1 ) && 
             do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                         $grammar->conjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'conjunctive1'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1561} ) && 0 ) )
         ) 
       &&
         do { while (
          (   ( $pad{I1562} = $pos or 1 ) && 
             (
                (   ( $pad{I1563} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '|' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1563} ) && 0 ) )
             &&
                (   ( $pad{I1564} = $pos or 1 ) && 
                 do{
                     my $pos1 = $pos;
                     do {
                       my $pos = $pos1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool =                            do{
                               my $pos1 = $pos;
                               do {
                                 my $pos = $pos1;
                                 my $from = $pos;
                                 my @match;
                                 my %named;
                                 $bool = 0 unless
                                      (   ( $pad{I1565} = $pos or 1 ) && 
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq '|' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
 ||    ( ( $pos = $pad{I1565} ) && 0 ) );
                                 $bool;
                               };
                           } ? 0 : 1;
                       $bool;
                     };
                 } ||    ( ( $pos = $pad{I1564} ) && 0 ) )
             &&
                (   ( $pad{I1566} = $pos or 1 ) && 
                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                             $grammar->conjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'conjunctive1'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1566} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1562} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1567} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { 
        my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'conjunctive1'} };
        return { alt1 => \@a ,}  if scalar @a > 1;
        return $a[0];;
 # use v6
;
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1567} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for disjunctive1
 # Data::Bind->sub_signature
#  (\&disjunctive1, );
;
*conjunctive = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
         (
          (   ( $pad{I1568} = $pos or 1 ) && 
             (
               (
                (   ( $pad{I1569} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1569} ) && 0 ) )
               || ( $bool = 1 )
               ) 
             &&
                (   ( $pad{I1570} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '&' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1570} ) && 0 ) )
             &&
                (   ( $pad{I1571} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '&' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1571} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1568} ) && 0 ) )
         || ( $bool = 1 )
         ) 
       &&
         (
           (   ( $pad{I1572} = $pos or 1 ) && 
             do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                         $grammar->disjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'disjunctive1'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1572} ) && 0 ) )
         ) 
       &&
         do { while (
          (   ( $pad{I1573} = $pos or 1 ) && 
             (
                (   ( $pad{I1574} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '&' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1574} ) && 0 ) )
             &&
                (   ( $pad{I1575} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '&' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1575} ) && 0 ) )
             &&
                (   ( $pad{I1576} = $pos or 1 ) && 
                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                             $grammar->disjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'disjunctive1'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1576} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1573} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1577} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { 
        my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'disjunctive1'} };
        return { conjunctive => \@a ,}  if scalar @a > 1;
        return $a[0];;
 # use v6
;
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1577} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for conjunctive
 # Data::Bind->sub_signature
#  (\&conjunctive, );
;
*rule = ## <global>
do { my $rule; $rule = sub {
  
              $_[3] = $_[2]; 
              eval{ $_[2] = undef };
              my $grammar
             = $_[0];
  my $s = $_[1];
  no warnings 'substr', 'uninitialized', 'syntax';
  my %pad;
  my $m;
  for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
        ? $_[3]{p}
        : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
    my %index;
    my @match;
    my %named;
    my $bool = 1;
    $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
    $m = Pugs::Runtime::Match->new( {
      str => \$s, from => \(0+$pos), to => \($pos),
      bool => \$bool, match => \@match, named => \%named, capture => undef,
    } );
    {
      my $prior = $::_V6_PRIOR_;
      local $::_V6_PRIOR_ = $prior;
      $bool = 0 unless
       (
         (
          (   ( $pad{I1578} = $pos or 1 ) && 
             (
               (
                (   ( $pad{I1579} = $pos or 1 ) && 
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match = 
                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   } ||    ( ( $pos = $pad{I1579} ) && 0 ) )
               || ( $bool = 1 )
               ) 
             &&
                (   ( $pad{I1580} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '|' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1580} ) && 0 ) )
             &&
                (   ( $pad{I1581} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '|' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1581} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1578} ) && 0 ) )
         || ( $bool = 1 )
         ) 
       &&
         (
           (   ( $pad{I1582} = $pos or 1 ) && 
             do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                         $grammar->conjunctive( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'conjunctive'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1582} ) && 0 ) )
         ) 
       &&
         do { while (
          (   ( $pad{I1583} = $pos or 1 ) && 
             (
                (   ( $pad{I1584} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '|' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1584} ) && 0 ) )
             &&
                (   ( $pad{I1585} = $pos or 1 ) && 
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '|' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
 ||    ( ( $pos = $pad{I1585} ) && 0 ) )
             &&
                (   ( $pad{I1586} = $pos or 1 ) && 
                 do {
                my $prior = $::_V6_PRIOR_;
                my $match = 
                             $grammar->conjunctive( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                $::_V6_PRIOR_ = $prior;
                if ( $match ) { push @{$named{'conjunctive'}}, $match;
                    $pos = $match->to;
                    1
                }
                else { 0 }
            } ||    ( ( $pos = $pad{I1586} ) && 0 ) )
             ) ||    ( ( $pos = $pad{I1583} ) && 0 ) )) {}; $bool = 1 }
       &&
          (   ( $pad{I1587} = $pos or 1 ) && 
           do { 
             local $::_V6_SUCCEED = 1;
             $::_V6_MATCH_ = $m;
             $m->data->{capture} = \( sub { my $_V6_SELF = shift;  { 
        my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'conjunctive'} };
        return { alt => \@a ,}  if scalar @a > 1;
        return $a[0];;
 # use v6
;
 } }->( $m ) ); 
             $bool = $::_V6_SUCCEED;
             $::_V6_MATCH_ = $m if $bool; 
             return $m if $bool; 
           } ||    ( ( $pos = $pad{I1587} ) && 0 ) )
       );
    }
    if ( $bool ) {
      my $prior = $::_V6_PRIOR_;
      $::_V6_PRIOR_ = sub {
        local $main::_V6_PRIOR_ = $prior;
        $rule->(@_);
      };
      #warn "pos2 = ", $pos, "\n";
      pos($_[1]) = $pos if $_[3]{continue};
      last;
    }
  } # /for
  $::_V6_MATCH_ = $m;
  return $m;
} }
## </global>
;## Signature for rule
 # Data::Bind->sub_signature
#  (\&rule, );
;

; 1;

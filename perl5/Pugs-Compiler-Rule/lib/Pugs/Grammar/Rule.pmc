# !!! DO NOT EDIT !!!
# This file was generated by Pugs::Compiler::Rule's compile_p6grammar.pl
# script from examples/Grammar.grammar at Mon Jul  9 14:14:13 2007

use strict;
use warnings;

package Pugs::Grammar::Rule;

#use base 'Pugs::Grammar::Base';

use Pugs::Runtime::Match;
use Pugs::Runtime::Regex;

# Code block from grammar spec


use utf8;
no strict 'refs';
no warnings 'redefine';
no warnings 'once';

#use Pugs::Runtime::Match;

our %rule_terms;
our %variables;



# token pod_begin
*pod_begin =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1042} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1043} = $pos or 1 ) &&
                   ## <perl5>
                   ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                       ? ( $pos += length( $1 ) or 1 )
                       : 0
                   )
                   ## </perl5>
     ||    ( ( $pos = $pad{I1043} ) && 0 ) )
               &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '=' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
    
               &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq 'e' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
    
               &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq 'n' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
    
               &&
                  (   ( $pad{I1044} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq 'd' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1044} ) && 0 ) )
               &&
                 do { while (
                  (   ( $pad{I1045} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1045} ) && 0 ) )) {}; $bool = 1 }
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1042} ) or 1 )
               &&            (
                  (   ( $pad{I1046} = $pos or 1 ) &&
                   ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1046} ) && 0 ) )
               &&
                 do { while (
                  (   ( $pad{I1047} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1047} ) && 0 ) )) {}; $bool = 1 }
               &&
                  (   ( $pad{I1048} = $pos or 1 ) &&
                   ## <metasyntax>
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match =
                               $grammar->pod_begin( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   }
                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1048} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token pod_other
*pod_other =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1049} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1050} = $pos or 1 ) &&
                   ## <perl5>
                   ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                       ? ( $pos += length( $1 ) or 1 )
                       : 0
                   )
                   ## </perl5>
     ||    ( ( $pos = $pad{I1050} ) && 0 ) )
               &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq '=' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
    
               &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq 'c' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
    
               &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq 'u' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
    
               &&
                  (   ( $pad{I1051} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq 't' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1051} ) && 0 ) )
               &&
                 do { while (
                  (   ( $pad{I1052} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1052} ) && 0 ) )) {}; $bool = 1 }
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1049} ) or 1 )
               &&            (
                  (   ( $pad{I1053} = $pos or 1 ) &&
                   ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1053} ) && 0 ) )
               &&
                 do { while (
                  (   ( $pad{I1054} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1054} ) && 0 ) )) {}; $bool = 1 }
               &&
                  (   ( $pad{I1055} = $pos or 1 ) &&
                   ## <metasyntax>
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match =
                               $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   }
                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1055} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token ws
*ws =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
            (   ( $pad{I1056} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1057} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1058} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '#' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1058} ) && 0 ) )
                   &&
                     do { while (
                      (   ( $pad{I1059} = $pos or 1 ) &&
                         ## <perl5>
                         ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                             ? ( $pos += length( $1 ) or 1 )
                             : 0
                         )
                         ## </perl5>
     ||    ( ( $pos = $pad{I1059} ) && 0 ) )) {}; $bool = 1 }
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1057} ) or 1 )
                   &&                (
                      (   ( $pad{I1060} = $pos or 1 ) &&
                       ## <perl5>
                       ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                           ? ( $pos += length( $1 ) or 1 )
                           : 0
                       )
                       ## </perl5>
     ||    ( ( $pos = $pad{I1060} ) && 0 ) )
                   &&
                     (
                      (   ( $pad{I1061} = $pos or 1 ) &&
                         (
                            (   ( $pad{I1062} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '=' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1062} ) && 0 ) )
                         &&
                           (
                            (   ( $pad{I1063} = $pos or 1 ) &&
                               ## <alt>
                               (
                                   ( $pad{I1064} = $pos or 1 )
                                   && (
                                   (
                                      (   ( $pad{I1065} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'b' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1065} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'e' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'g' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'i' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1066} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'n' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1066} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1067} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1067} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'E' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'N' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1068} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'D' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1068} ) && 0 ) )
                                   &&
                                     do { while (
                                      (   ( $pad{I1069} = $pos or 1 ) &&
                                         ## <perl5>
                                         ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                                             ? ( $pos += length( $1 ) or 1 )
                                             : 0
                                         )
                                         ## </perl5>
     ||    ( ( $pos = $pad{I1069} ) && 0 ) )) {}; $bool = 1 }
                                   &&
                                     do { while (
                                      (   ( $pad{I1070} = $pos or 1 ) &&
                                         ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1070} ) && 0 ) )) {}; $bool = 1 }
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1071} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'b' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1071} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'e' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'g' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'i' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1072} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'n' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1072} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1073} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_begin( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1073} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1074} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'k' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1074} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'w' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'i' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1075} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'd' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1075} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1076} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1076} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1077} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'p' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1077} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'o' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1078} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'd' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1078} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1079} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1079} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1080} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'f' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1080} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'o' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1081} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'r' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1081} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1082} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1082} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1083} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'h' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1083} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'e' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'a' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'd' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1084} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq '1' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1084} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1085} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1085} ) && 0 ) )
                                   )
                                 )
                               )
    ## </alt>
     ||    ( ( $pos = $pad{I1063} ) && 0 ) )
                           || ( $bool = 1 )
                           ) 
                         ) ||    ( ( $pos = $pad{I1061} ) && 0 ) )
                     || ( $bool = 1 )
                     ) 
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1057} ) or 1 )
                   &&                 (   ( $pad{I1086} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1086} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1056} ) && 0 ) )
           && do { while (
            (   ( $pad{I1056} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1057} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1058} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '#' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1058} ) && 0 ) )
                   &&
                     do { while (
                      (   ( $pad{I1059} = $pos or 1 ) &&
                         ## <perl5>
                         ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                             ? ( $pos += length( $1 ) or 1 )
                             : 0
                         )
                         ## </perl5>
     ||    ( ( $pos = $pad{I1059} ) && 0 ) )) {}; $bool = 1 }
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1057} ) or 1 )
                   &&                (
                      (   ( $pad{I1060} = $pos or 1 ) &&
                       ## <perl5>
                       ( ( substr( $s, $pos ) =~ m/^((?:\n\r?|\r\n?))/ )
                           ? ( $pos += length( $1 ) or 1 )
                           : 0
                       )
                       ## </perl5>
     ||    ( ( $pos = $pad{I1060} ) && 0 ) )
                   &&
                     (
                      (   ( $pad{I1061} = $pos or 1 ) &&
                         (
                            (   ( $pad{I1062} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '=' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1062} ) && 0 ) )
                         &&
                           (
                            (   ( $pad{I1063} = $pos or 1 ) &&
                               ## <alt>
                               (
                                   ( $pad{I1064} = $pos or 1 )
                                   && (
                                   (
                                      (   ( $pad{I1065} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'b' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1065} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'e' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'g' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'i' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1066} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'n' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1066} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1067} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1067} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'E' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'N' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1068} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'D' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1068} ) && 0 ) )
                                   &&
                                     do { while (
                                      (   ( $pad{I1069} = $pos or 1 ) &&
                                         ## <perl5>
                                         ( ( substr( $s, $pos ) =~ m/^((?!\n\r?|\r\n?).)/ )
                                             ? ( $pos += length( $1 ) or 1 )
                                             : 0
                                         )
                                         ## </perl5>
     ||    ( ( $pos = $pad{I1069} ) && 0 ) )) {}; $bool = 1 }
                                   &&
                                     do { while (
                                      (   ( $pad{I1070} = $pos or 1 ) &&
                                         ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1070} ) && 0 ) )) {}; $bool = 1 }
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1071} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'b' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1071} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'e' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'g' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'i' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1072} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'n' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1072} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1073} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_begin( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1073} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1074} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'k' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1074} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'w' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'i' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1075} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'd' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1075} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1076} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1076} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1077} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'p' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1077} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'o' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1078} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'd' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1078} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1079} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1079} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1080} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'f' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1080} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'o' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1081} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'r' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1081} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1082} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1082} ) && 0 ) )
                                   )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1064} ) or 1 )
                                   &&                                (
                                      (   ( $pad{I1083} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq 'h' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1083} ) && 0 ) )
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'e' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'a' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq 'd' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
    
                                   &&
                                      (   ( $pad{I1084} = $pos or 1 ) &&
                                       ## <constant>
                                       ( ( substr( $s, $pos, 1 ) eq '1' )
                                           ? ( $pos += 1 or 1 )
                                           : 0
                                       )
                                       ## </constant>
     ||    ( ( $pos = $pad{I1084} ) && 0 ) )
                                   &&
                                      (   ( $pad{I1085} = $pos or 1 ) &&
                                       ## <metasyntax>
                                       do {
                                            my $prior = $::_V6_PRIOR_;
                                            my $match =
                                                   $grammar->pod_other( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                            $::_V6_PRIOR_ = $prior;
                                            my $bool = (!$match != 1);
                                            $pos = $match->to if $bool;
                                            $match;
                                       }
                                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1085} ) && 0 ) )
                                   )
                                 )
                               )
    ## </alt>
     ||    ( ( $pos = $pad{I1063} ) && 0 ) )
                           || ( $bool = 1 )
                           ) 
                         ) ||    ( ( $pos = $pad{I1061} ) && 0 ) )
                     || ( $bool = 1 )
                     ) 
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1057} ) or 1 )
                   &&                 (   ( $pad{I1086} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1086} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1056} ) && 0 ) )) {}; $bool = 1 }
           ) ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token ident
*ident =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
            (   ( $pad{I1087} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1088} = $pos or 1 )
                   && (
                    (   ( $pad{I1089} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1089} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1088} ) or 1 )
                   &&                 (   ( $pad{I1090} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '_' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1090} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1088} ) or 1 )
                   &&                 (   ( $pad{I1091} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 2 ) eq '::' )
                         ? ( $pos += 2 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1091} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1087} ) && 0 ) )
           && do { while (
            (   ( $pad{I1087} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1088} = $pos or 1 )
                   && (
                    (   ( $pad{I1089} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1089} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1088} ) or 1 )
                   &&                 (   ( $pad{I1090} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '_' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1090} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1088} ) or 1 )
                   &&                 (   ( $pad{I1091} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 2 ) eq '::' )
                         ? ( $pos += 2 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1091} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1087} ) && 0 ) )) {}; $bool = 1 }
           ) ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token alnum
*alnum =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1092} = $pos or 1 ) &&
             ## <perl5>
             ( ( substr( $s, $pos ) =~ m/^((?:(?=[0-9a-zA-Z]))\X)/ )
                 ? ( $pos += length( $1 ) or 1 )
                 : 0
             )
             ## </perl5>
     ||    ( ( $pos = $pad{I1092} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token alpha
*alpha =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1093} = $pos or 1 ) &&
             ## <perl5>
             ( ( substr( $s, $pos ) =~ m/^((?:(?=[a-zA-Z]))\X)/ )
                 ? ( $pos += length( $1 ) or 1 )
                 : 0
             )
             ## </perl5>
     ||    ( ( $pos = $pad{I1093} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token digit
*digit =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1094} = $pos or 1 ) &&
             ## <perl5>
             ( ( substr( $s, $pos ) =~ m/^((?:(?=[0-9]))\X)/ )
                 ? ( $pos += length( $1 ) or 1 )
                 : 0
             )
             ## </perl5>
     ||    ( ( $pos = $pad{I1094} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token special_char
*special_char =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1095} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1096} = $pos or 1 ) &&
                   do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           ## <alt>
                           (
                               ( $pad{I1097} = $pos or 1 )
                               && (
                                (   ( $pad{I1098} = $pos or 1 ) &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'c' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
     ||    ( ( $pos = $pad{I1098} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1097} ) or 1 )
                               &&                             (   ( $pad{I1099} = $pos or 1 ) &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'C' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
     ||    ( ( $pos = $pad{I1099} ) && 0 ) )
                             )
                           )
    ## </alt>
    ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   } ||    ( ( $pos = $pad{I1096} ) && 0 ) )
               &&
                  (   ( $pad{I1100} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '[' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1100} ) && 0 ) )
               &&
                  (   ( $pad{I1101} = $pos or 1 ) &&
                   do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1102} = $pos or 1 ) &&
                               ## <alt>
                               (
                                   ( $pad{I1103} = $pos or 1 )
                                   && (
                                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                               $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'alnum'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                }
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                ## <perl5>
                                   ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                                       ? ( $pos += length( $1 ) or 1 )
                                       : 0
                                   )
                                   ## </perl5>
    
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1104} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq ';' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1104} ) && 0 ) )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1105} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq '(' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1105} ) && 0 ) )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1106} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq ')' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1106} ) && 0 ) )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1107} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq '-' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1107} ) && 0 ) )
                                 )
                               )
    ## </alt>
     ||    ( ( $pos = $pad{I1102} ) && 0 ) )
                           && do { while (
                            (   ( $pad{I1102} = $pos or 1 ) &&
                               ## <alt>
                               (
                                   ( $pad{I1103} = $pos or 1 )
                                   && (
                                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                               $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'alnum'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                }
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                ## <perl5>
                                   ( ( substr( $s, $pos ) =~ m/^(\s)/ )
                                       ? ( $pos += length( $1 ) or 1 )
                                       : 0
                                   )
                                   ## </perl5>
    
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1104} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq ';' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1104} ) && 0 ) )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1105} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq '(' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1105} ) && 0 ) )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1106} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq ')' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1106} ) && 0 ) )
                                   )
                                 || (
                                   ( ( $bool = 1 ) && ( $pos = $pad{I1103} ) or 1 )
                                   &&                                 (   ( $pad{I1107} = $pos or 1 ) &&
                                     ## <constant>
                                     ( ( substr( $s, $pos, 1 ) eq '-' )
                                         ? ( $pos += 1 or 1 )
                                         : 0
                                     )
                                     ## </constant>
     ||    ( ( $pos = $pad{I1107} ) && 0 ) )
                                 )
                               )
    ## </alt>
     ||    ( ( $pos = $pad{I1102} ) && 0 ) )) {}; $bool = 1 }
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 1 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   } ||    ( ( $pos = $pad{I1101} ) && 0 ) )
               &&
                  (   ( $pad{I1108} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ']' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1108} ) && 0 ) )
               &&
                  (   ( $pad{I1109} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { special_char => '\\' . $_[0]->[0] . $_[0]->[1] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1109} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1095} ) or 1 )
               &&            (
                  (   ( $pad{I1110} = $pos or 1 ) &&
                   ## <alt>
                   (
                       ( $pad{I1111} = $pos or 1 )
                       && (
                        (   ( $pad{I1112} = $pos or 1 ) &&
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq 'x' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
     ||    ( ( $pos = $pad{I1112} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1111} ) or 1 )
                       &&                     (   ( $pad{I1113} = $pos or 1 ) &&
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq 'X' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
     ||    ( ( $pos = $pad{I1113} ) && 0 ) )
                     )
                   )
    ## </alt>
     ||    ( ( $pos = $pad{I1110} ) && 0 ) )
               &&
                 (
                  (   ( $pad{I1114} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'xdigit'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1114} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1114} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'xdigit'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1114} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1115} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { special_char => '\\' . $_[0] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1115} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1095} ) or 1 )
               &&            (
                  (   ( $pad{I1116} = $pos or 1 ) &&
                   do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           ## <alt>
                           (
                               ( $pad{I1117} = $pos or 1 )
                               && (
                                (   ( $pad{I1118} = $pos or 1 ) &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'x' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
     ||    ( ( $pos = $pad{I1118} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1117} ) or 1 )
                               &&                             (   ( $pad{I1119} = $pos or 1 ) &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'X' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
     ||    ( ( $pos = $pad{I1119} ) && 0 ) )
                             )
                           )
    ## </alt>
    ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   } ||    ( ( $pos = $pad{I1116} ) && 0 ) )
               &&
                  (   ( $pad{I1120} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '[' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1120} ) && 0 ) )
               &&
                  (   ( $pad{I1121} = $pos or 1 ) &&
                   do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1122} = $pos or 1 ) &&
                               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                           $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'xdigit'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1122} ) && 0 ) )
                           && do { while (
                            (   ( $pad{I1122} = $pos or 1 ) &&
                               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                           $grammar->xdigit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'xdigit'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1122} ) && 0 ) )) {}; $bool = 1 }
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 1 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   } ||    ( ( $pos = $pad{I1121} ) && 0 ) )
               &&
                  (   ( $pad{I1123} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ']' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1123} ) && 0 ) )
               &&
                  (   ( $pad{I1124} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { special_char => '\\' . $_[0]->[0] . $_[0]->[1] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1124} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1095} ) or 1 )
               &&            (
                  (   ( $pad{I1125} = $pos or 1 ) &&
                   ## <alt>
                   (
                       ( $pad{I1126} = $pos or 1 )
                       && (
                        (   ( $pad{I1127} = $pos or 1 ) &&
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq 'o' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
     ||    ( ( $pos = $pad{I1127} ) && 0 ) )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1126} ) or 1 )
                       &&                     (   ( $pad{I1128} = $pos or 1 ) &&
                         ## <constant>
                         ( ( substr( $s, $pos, 1 ) eq 'O' )
                             ? ( $pos += 1 or 1 )
                             : 0
                         )
                         ## </constant>
     ||    ( ( $pos = $pad{I1128} ) && 0 ) )
                     )
                   )
    ## </alt>
     ||    ( ( $pos = $pad{I1125} ) && 0 ) )
               &&
                 (
                  (   ( $pad{I1129} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1129} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1129} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1129} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1130} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { special_char => '\\' . $_[0] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1130} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1095} ) or 1 )
               &&            (
                  (   ( $pad{I1131} = $pos or 1 ) &&
                   do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           ## <alt>
                           (
                               ( $pad{I1132} = $pos or 1 )
                               && (
                                (   ( $pad{I1133} = $pos or 1 ) &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'o' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
     ||    ( ( $pos = $pad{I1133} ) && 0 ) )
                               )
                             || (
                               ( ( $bool = 1 ) && ( $pos = $pad{I1132} ) or 1 )
                               &&                             (   ( $pad{I1134} = $pos or 1 ) &&
                                 ## <constant>
                                 ( ( substr( $s, $pos, 1 ) eq 'O' )
                                     ? ( $pos += 1 or 1 )
                                     : 0
                                 )
                                 ## </constant>
     ||    ( ( $pos = $pad{I1134} ) && 0 ) )
                             )
                           )
    ## </alt>
    ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   } ||    ( ( $pos = $pad{I1131} ) && 0 ) )
               &&
                  (   ( $pad{I1135} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '[' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1135} ) && 0 ) )
               &&
                  (   ( $pad{I1136} = $pos or 1 ) &&
                   do{
                       my $hash = do {
                         my $bool = 1;
                         my $from = $pos;
                         my @match;
                         my %named;
                         $bool = 0 unless
                           (
                            (   ( $pad{I1137} = $pos or 1 ) &&
                               ## <perl5>
                               ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                                   ? ( $pos += length( $1 ) or 1 )
                                   : 0
                               )
                               ## </perl5>
     ||    ( ( $pos = $pad{I1137} ) && 0 ) )
                           && do { while (
                            (   ( $pad{I1137} = $pos or 1 ) &&
                               ## <perl5>
                               ( ( substr( $s, $pos ) =~ m/^(\d)/ )
                                   ? ( $pos += length( $1 ) or 1 )
                                   : 0
                               )
                               ## </perl5>
     ||    ( ( $pos = $pad{I1137} ) && 0 ) )) {}; $bool = 1 }
                           ) ;
                         { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                       };
                       my $bool = ${$hash->{'bool'}};
                       $match[ 1 ] = Pugs::Runtime::Match->new( $hash );
                       $bool;
                   } ||    ( ( $pos = $pad{I1136} ) && 0 ) )
               &&
                  (   ( $pad{I1138} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ']' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1138} ) && 0 ) )
               &&
                  (   ( $pad{I1139} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { special_char => '\\' . $_[0]->[0] . $_[0]->[1] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1139} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1095} ) or 1 )
               &&            (
                  (   ( $pad{I1140} = $pos or 1 ) &&
                   ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1140} ) && 0 ) )
               &&
                  (   ( $pad{I1141} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { special_char => '\\' . $_[0] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1141} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token literal
*literal =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           do { while (
            (   ( $pad{I1142} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1143} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1144} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1144} ) && 0 ) )
                   &&
                      (   ( $pad{I1145} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1145} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1143} ) or 1 )
                   &&                 (   ( $pad{I1146} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\']))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1146} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1142} ) && 0 ) )) {}; $bool = 1 };
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token double_quoted
*double_quoted =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           do { while (
            (   ( $pad{I1147} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1148} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1149} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1149} ) && 0 ) )
                   &&
                      (   ( $pad{I1150} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1150} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1148} ) or 1 )
                   &&                 (   ( $pad{I1151} = $pos or 1 ) &&
                     do{
                    my $match =           ## <variable>
              do {
                our $I1152;
                our $I1152_sizes;
                unless ( $I1152 ) {
                    my $hash = \%Pugs::Grammar::Rule::variables;
                    my %sizes = map { length($_) => 1 } keys %$hash;
                    $I1152_sizes = [ sort { $b <=> $a } keys %sizes ];
                    $I1152 = $hash;
                }
                my $match = 0;
                my $key;
                for ( @$I1152_sizes ) {
                    $key = ( $pos <= length( $s )
                                ? substr( $s, $pos, $_ )
                                : '' );
                    if ( exists $I1152->{$key} ) {
                        #$named{KEY} = $key;
                        #$::_V6_MATCH_ = $m;
                        #print "m: ", Dumper( $::_V6_MATCH_->data )
                        #    if ( $key eq 'until' );
                        $match = Pugs::Runtime::Regex::preprocess_hash( $I1152, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                        last if $match;
                    }
                }
                if ( $match ) {
                    $pos = $match->to;
                    #print "match: $key at $pos = ", Dumper( $match->data );
                    $bool = 1;
                }; # else { $bool = 0 }
                $match;
              }
              ## </variable>
    ;
                    if ( $match ) { push @{$named{'Pugs::Grammar::Rule::variables'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1151} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1148} ) or 1 )
                   &&                 (   ( $pad{I1153} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\"]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1153} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1147} ) && 0 ) )) {}; $bool = 1 };
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token metasyntax
*metasyntax =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
            (   ( $pad{I1154} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1155} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1156} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1156} ) && 0 ) )
                   &&
                      (   ( $pad{I1157} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1157} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1158} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1158} ) && 0 ) )
                   &&
                      (   ( $pad{I1159} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1159} ) && 0 ) )
                   &&
                      (   ( $pad{I1160} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1160} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1161} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1161} ) && 0 ) )
                   &&
                      (   ( $pad{I1162} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1162} ) && 0 ) )
                   &&
                      (   ( $pad{I1163} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1163} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1164} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '{' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1164} ) && 0 ) )
                   &&
                      (   ( $pad{I1165} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1165} ) && 0 ) )
                   &&
                      (   ( $pad{I1166} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '}' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1166} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1167} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '<' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1167} ) && 0 ) )
                   &&
                      (   ( $pad{I1168} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1168} ) && 0 ) )
                   &&
                      (   ( $pad{I1169} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1169} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                 (   ( $pad{I1170} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\>]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1170} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1154} ) && 0 ) )
           && do { while (
            (   ( $pad{I1154} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1155} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1156} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1156} ) && 0 ) )
                   &&
                      (   ( $pad{I1157} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1157} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1158} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1158} ) && 0 ) )
                   &&
                      (   ( $pad{I1159} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1159} ) && 0 ) )
                   &&
                      (   ( $pad{I1160} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1160} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1161} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1161} ) && 0 ) )
                   &&
                      (   ( $pad{I1162} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1162} ) && 0 ) )
                   &&
                      (   ( $pad{I1163} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1163} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1164} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '{' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1164} ) && 0 ) )
                   &&
                      (   ( $pad{I1165} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1165} ) && 0 ) )
                   &&
                      (   ( $pad{I1166} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '}' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1166} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                (
                      (   ( $pad{I1167} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '<' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1167} ) && 0 ) )
                   &&
                      (   ( $pad{I1168} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1168} ) && 0 ) )
                   &&
                      (   ( $pad{I1169} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1169} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1155} ) or 1 )
                   &&                 (   ( $pad{I1170} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\>]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1170} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1154} ) && 0 ) )) {}; $bool = 1 }
           ) ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token char_range
*char_range =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
            (   ( $pad{I1171} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1172} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1173} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1173} ) && 0 ) )
                   &&
                      (   ( $pad{I1174} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1174} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                   &&                 (   ( $pad{I1175} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\]]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1175} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1171} ) && 0 ) )
           && do { while (
            (   ( $pad{I1171} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1172} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1173} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1173} ) && 0 ) )
                   &&
                      (   ( $pad{I1174} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1174} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1172} ) or 1 )
                   &&                 (   ( $pad{I1175} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\]]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1175} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1171} ) && 0 ) )) {}; $bool = 1 }
           ) ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token char_class
*char_class =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1176} = $pos or 1 )
               && (
               (
                (   ( $pad{I1177} = $pos or 1 ) &&
                   ## <metasyntax>
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match =
                               $grammar->alpha( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   }
                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1177} ) && 0 ) )
               && do { while (
                (   ( $pad{I1177} = $pos or 1 ) &&
                   ## <metasyntax>
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match =
                               $grammar->alpha( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   }
                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1177} ) && 0 ) )) {}; $bool = 1 }
               ) 
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1176} ) or 1 )
               &&            (
                  (   ( $pad{I1178} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '[' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1178} ) && 0 ) )
               &&
                  (   ( $pad{I1179} = $pos or 1 ) &&
                   ## <metasyntax>
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match =
                               $grammar->char_range( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   }
                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1179} ) && 0 ) )
               &&
                  (   ( $pad{I1180} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ']' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1180} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token string_code
*string_code =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
            (   ( $pad{I1181} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1182} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1183} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1183} ) && 0 ) )
                   &&
                      (   ( $pad{I1184} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1184} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1185} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1185} ) && 0 ) )
                   &&
                      (   ( $pad{I1186} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1186} ) && 0 ) )
                   &&
                      (   ( $pad{I1187} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1187} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1188} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1188} ) && 0 ) )
                   &&
                      (   ( $pad{I1189} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1189} ) && 0 ) )
                   &&
                      (   ( $pad{I1190} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1190} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1191} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '{' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1191} ) && 0 ) )
                   &&
                      (   ( $pad{I1192} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1193} = $pos or 1 )
                           && (
                            (   ( $pad{I1194} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1194} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1193} ) or 1 )
                           &&                         (   ( $pad{I1195} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1195} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1192} ) && 0 ) )
                   &&
                      (   ( $pad{I1196} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '}' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1196} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1197} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '(' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1197} ) && 0 ) )
                   &&
                      (   ( $pad{I1198} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1199} = $pos or 1 )
                           && (
                            (   ( $pad{I1200} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1200} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1199} ) or 1 )
                           &&                         (   ( $pad{I1201} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1201} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1198} ) && 0 ) )
                   &&
                      (   ( $pad{I1202} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq ')' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1202} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1203} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '<' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1203} ) && 0 ) )
                   &&
                      (   ( $pad{I1204} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1205} = $pos or 1 )
                           && (
                            (   ( $pad{I1206} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1206} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1205} ) or 1 )
                           &&                         (   ( $pad{I1207} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1207} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1204} ) && 0 ) )
                   &&
                      (   ( $pad{I1208} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1208} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1209} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1210} = $pos or 1 )
                           && (
                            (   ( $pad{I1211} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1211} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1210} ) or 1 )
                           &&                         (   ( $pad{I1212} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '>' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1212} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1210} ) or 1 )
                           &&                         (   ( $pad{I1213} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '=' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1213} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1210} ) or 1 )
                           &&                         (   ( $pad{I1214} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '-' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1214} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1209} ) && 0 ) )
                   &&
                      (   ( $pad{I1215} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1215} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                 (   ( $pad{I1216} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1216} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                 (   ( $pad{I1217} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\}\)\>]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1217} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1181} ) && 0 ) )
           && do { while (
            (   ( $pad{I1181} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1182} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1183} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '\\' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1183} ) && 0 ) )
                   &&
                      (   ( $pad{I1184} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1184} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1185} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1185} ) && 0 ) )
                   &&
                      (   ( $pad{I1186} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1186} ) && 0 ) )
                   &&
                      (   ( $pad{I1187} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq chr(39) )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1187} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1188} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1188} ) && 0 ) )
                   &&
                      (   ( $pad{I1189} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->double_quoted( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1189} ) && 0 ) )
                   &&
                      (   ( $pad{I1190} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '"' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1190} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1191} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '{' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1191} ) && 0 ) )
                   &&
                      (   ( $pad{I1192} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1193} = $pos or 1 )
                           && (
                            (   ( $pad{I1194} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1194} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1193} ) or 1 )
                           &&                         (   ( $pad{I1195} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1195} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1192} ) && 0 ) )
                   &&
                      (   ( $pad{I1196} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '}' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1196} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1197} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '(' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1197} ) && 0 ) )
                   &&
                      (   ( $pad{I1198} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1199} = $pos or 1 )
                           && (
                            (   ( $pad{I1200} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1200} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1199} ) or 1 )
                           &&                         (   ( $pad{I1201} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1201} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1198} ) && 0 ) )
                   &&
                      (   ( $pad{I1202} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq ')' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1202} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1203} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '<' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1203} ) && 0 ) )
                   &&
                      (   ( $pad{I1204} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1205} = $pos or 1 )
                           && (
                            (   ( $pad{I1206} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1206} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1205} ) or 1 )
                           &&                         (   ( $pad{I1207} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1207} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1204} ) && 0 ) )
                   &&
                      (   ( $pad{I1208} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1208} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                (
                      (   ( $pad{I1209} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1210} = $pos or 1 )
                           && (
                            (   ( $pad{I1211} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1211} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1210} ) or 1 )
                           &&                         (   ( $pad{I1212} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '>' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1212} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1210} ) or 1 )
                           &&                         (   ( $pad{I1213} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '=' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1213} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1210} ) or 1 )
                           &&                         (   ( $pad{I1214} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '-' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1214} ) && 0 ) )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1209} ) && 0 ) )
                   &&
                      (   ( $pad{I1215} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1215} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                 (   ( $pad{I1216} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1216} ) && 0 ) )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1182} ) or 1 )
                   &&                 (   ( $pad{I1217} = $pos or 1 ) &&
                     ## <perl5>
                     ( ( substr( $s, $pos ) =~ m/^((?:(?![\}\)\>]))\X)/ )
                         ? ( $pos += length( $1 ) or 1 )
                         : 0
                     )
                     ## </perl5>
     ||    ( ( $pos = $pad{I1217} ) && 0 ) )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1181} ) && 0 ) )) {}; $bool = 1 }
           ) ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token parsed_code
*parsed_code =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1218} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->string_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1218} ) && 0 ) )
           &&
              (   ( $pad{I1219} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return '{' . $_[0] . '}'; }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1219} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token named_capture_body
*named_capture_body =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1220} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1221} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '(' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1221} ) && 0 ) )
               &&
                  (   ( $pad{I1222} = $pos or 1 ) &&
                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                               $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1222} ) && 0 ) )
               &&
                  (   ( $pad{I1223} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ')' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1223} ) && 0 ) )
               &&
                  (   ( $pad{I1224} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { capturing_group => ${ $_[0]->{qw(rule)} } }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1224} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1220} ) or 1 )
               &&            (
                  (   ( $pad{I1225} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '[' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1225} ) && 0 ) )
               &&
                  (   ( $pad{I1226} = $pos or 1 ) &&
                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                               $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1226} ) && 0 ) )
               &&
                  (   ( $pad{I1227} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq ']' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1227} ) && 0 ) )
               &&
                  (   ( $pad{I1228} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return ${ $_[0]->{qw(rule)} } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1228} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1220} ) or 1 )
               &&            (
                  (   ( $pad{I1229} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '<' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1229} ) && 0 ) )
               &&
                  (   ( $pad{I1230} = $pos or 1 ) &&
                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                               $grammar->parse_metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'parse_metasyntax'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1230} ) && 0 ) )
               &&
                  (   ( $pad{I1231} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return ${ $_[0]->{qw(parse_metasyntax)} } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1231} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1220} ) or 1 )
               &&            (
                  (   ( $pad{I1232} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1232} ) && 0 ) )
               &&
                  (   ( $pad{I1233} = $pos or 1 ) &&
                   ## <metasyntax>
                   do {
                        my $prior = $::_V6_PRIOR_;
                        my $match =
                               $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                        $::_V6_PRIOR_ = $prior;
                        my $bool = (!$match != 1);
                        $pos = $match->to if $bool;
                        $match;
                   }
                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1233} ) && 0 ) )
               &&
                  (   ( $pad{I1234} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq chr(39) )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1234} ) && 0 ) )
               &&
                  (   ( $pad{I1235} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { metasyntax => { metasyntax => "${$_[0]}" } }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1235} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1220} ) or 1 )
               &&             (   ( $pad{I1236} = $pos or 1 ) &&
                 do { 
                   local $::_V6_SUCCEED = 1;
                   $::_V6_MATCH_ = $m;
                   sub { die "invalid alias syntax"; }->( $m );
                   1;
                 } ||    ( ( $pos = $pad{I1236} ) && 0 ) )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token parse_metasyntax
*parse_metasyntax =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1237} = $pos or 1 ) &&
               do{
                    my $from = $pos;
                    my $bool =                    ## <alt>
                       (
                           ( $pad{I1238} = $pos or 1 )
                           && (
                            (   ( $pad{I1239} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '!' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1239} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1238} ) or 1 )
                           &&                         (   ( $pad{I1240} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq '?' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1240} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1238} ) or 1 )
                           &&                         (   ( $pad{I1241} = $pos or 1 ) &&
     1 # null constant
     ||    ( ( $pos = $pad{I1241} ) && 0 ) )
                         )
                       )
    ## </alt>
    ;
                    my $match = Pugs::Runtime::Match->new(
                        { str => \$s, from => \$from, match => [], named => {}, bool => \1, to => \(0+$pos), capture => undef }
                    ); $named{'modifier'} = $match;
                    $bool
                } ||    ( ( $pos = $pad{I1237} ) && 0 ) )
           &&
              (   ( $pad{I1242} = $pos or 1 ) &&
               ## <alt>
               (
                   ( $pad{I1243} = $pos or 1 )
                   && (
                   (
                      (   ( $pad{I1244} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '{' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1244} ) && 0 ) )
                   &&
                      (   ( $pad{I1245} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'parsed_code'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1245} ) && 0 ) )
                   &&
                      (   ( $pad{I1246} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 2 ) eq '}>' )
                           ? ( $pos += 2 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1246} ) && 0 ) )
                   &&
                      (   ( $pad{I1247} = $pos or 1 ) &&
                       do { 
                         local $::_V6_SUCCEED = 1;
                         $::_V6_MATCH_ = $m;
                         $m->data->{capture} = \( sub { return { closure => {
                closure  => ${ $_[0]->{qw(parsed_code)} },
                modifier => ${ $_[0]->{qw(modifier)} },
            } } }->( $m ) ); 
                         $bool = $::_V6_SUCCEED;
                         $::_V6_MATCH_ = $m if $bool; 
                         return $m if $bool; 
                       } ||    ( ( $pos = $pad{I1247} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1243} ) or 1 )
                   &&                (
                      (   ( $pad{I1248} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1248} ) && 0 ) )
                   &&
                     (
                      (   ( $pad{I1249} = $pos or 1 ) &&
                         do{
                             my $hash = do {
                               my $bool = 1;
                               my $from = $pos;
                               my @match;
                               my %named;
                               $bool = 0 unless
                                 (
                                    (   ( $pad{I1250} = $pos or 1 ) &&
                                     ## <perl5>
                                     ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                         ? ( $pos += length( $1 ) or 1 )
                                         : 0
                                     )
                                     ## </perl5>
     ||    ( ( $pos = $pad{I1250} ) && 0 ) )
                                 &&
                                    (   ( $pad{I1251} = $pos or 1 ) &&
                                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                                 $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1251} ) && 0 ) )
                                 );
                               { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                             };
                             my $bool = ${$hash->{'bool'}};
                             if ( $bool ) {
                                 push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                             }
                             $bool;
                         } ||    ( ( $pos = $pad{I1249} ) && 0 ) )
                     && do { while (
                      (   ( $pad{I1249} = $pos or 1 ) &&
                         do{
                             my $hash = do {
                               my $bool = 1;
                               my $from = $pos;
                               my @match;
                               my %named;
                               $bool = 0 unless
                                 (
                                    (   ( $pad{I1250} = $pos or 1 ) &&
                                     ## <perl5>
                                     ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                         ? ( $pos += length( $1 ) or 1 )
                                         : 0
                                     )
                                     ## </perl5>
     ||    ( ( $pos = $pad{I1250} ) && 0 ) )
                                 &&
                                    (   ( $pad{I1251} = $pos or 1 ) &&
                                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                                 $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1251} ) && 0 ) )
                                 );
                               { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                             };
                             my $bool = ${$hash->{'bool'}};
                             if ( $bool ) {
                                 push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                             }
                             $bool;
                         } ||    ( ( $pos = $pad{I1249} ) && 0 ) )) {}; $bool = 1 }
                     ) 
                   &&
                      (   ( $pad{I1252} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1252} ) && 0 ) )
                   &&
                      (   ( $pad{I1253} = $pos or 1 ) &&
                       do { 
                         local $::_V6_SUCCEED = 1;
                         $::_V6_MATCH_ = $m;
                         $m->data->{capture} = \( sub {
                if ( ${ $_[0]->{qw(modifier)} } eq '!' ) {
                  return {
                    negate => {
                      char_class => [
                        '+' . $_[0]->{qw(char_class)},
                        @{$_[0]->[0]},   # TODO - stringify
                  ] } }
                }
                return {
                  char_class => [
                    '+' . $_[0]->{qw(char_class)},
                    @{$_[0]->[0]},   # TODO - stringify
                ] }
            }->( $m ) ); 
                         $bool = $::_V6_SUCCEED;
                         $::_V6_MATCH_ = $m if $bool; 
                         return $m if $bool; 
                       } ||    ( ( $pos = $pad{I1253} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1243} ) or 1 )
                   &&                (
                      (   ( $pad{I1254} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->ident( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'ident'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1254} ) && 0 ) )
                   &&
                      (   ( $pad{I1255} = $pos or 1 ) &&
                       ## <alt>
                       (
                           ( $pad{I1256} = $pos or 1 )
                           && (
                           (
                              (   ( $pad{I1257} = $pos or 1 ) &&
                               ## <metasyntax>
                               do {
                                    my $prior = $::_V6_PRIOR_;
                                    my $match =
                                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                    $::_V6_PRIOR_ = $prior;
                                    my $bool = (!$match != 1);
                                    $pos = $match->to if $bool;
                                    $match;
                               }
                               ## </metasyntax>
     ||    ( ( $pos = $pad{I1257} ) && 0 ) )
                           &&
                              (   ( $pad{I1258} = $pos or 1 ) &&
                               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1258} ) && 0 ) )
                           &&
                              (   ( $pad{I1259} = $pos or 1 ) &&
                               ## <constant>
                               ( ( substr( $s, $pos, 1 ) eq '>' )
                                   ? ( $pos += 1 or 1 )
                                   : 0
                               )
                               ## </constant>
     ||    ( ( $pos = $pad{I1259} ) && 0 ) )
                           &&
                              (   ( $pad{I1260} = $pos or 1 ) &&
                               do { 
                                 local $::_V6_SUCCEED = 1;
                                 $::_V6_MATCH_ = $m;
                                 $m->data->{capture} = \( sub {
                if  ( ${ $_[0]->{qw(ident)} } eq 'before'
                   || ${ $_[0]->{qw(ident)} } eq 'after'
                    ) {
                    return { ${ $_[0]->{qw(ident)} } => { rule => ${ $_[0]->{qw(rule)} }, modifier => ${ $_[0]->{qw(modifier)} } } }
                }
                return { metasyntax => {
                    metasyntax => ${ $_[0]->{qw(ident)} },
                    rule       => ${ $_[0]->{qw(rule)} },
                    modifier   => ${ $_[0]->{qw(modifier)} },
                } }
              }->( $m ) ); 
                                 $bool = $::_V6_SUCCEED;
                                 $::_V6_MATCH_ = $m if $bool; 
                                 return $m if $bool; 
                               } ||    ( ( $pos = $pad{I1260} ) && 0 ) )
                           )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1256} ) or 1 )
                           &&                        (
                              (   ( $pad{I1261} = $pos or 1 ) &&
                               ## <constant>
                               ( ( substr( $s, $pos, 1 ) eq ':' )
                                   ? ( $pos += 1 or 1 )
                                   : 0
                               )
                               ## </constant>
     ||    ( ( $pos = $pad{I1261} ) && 0 ) )
                           &&
                             (
                              (   ( $pad{I1262} = $pos or 1 ) &&
                                 ## <metasyntax>
                                 do {
                                      my $prior = $::_V6_PRIOR_;
                                      my $match =
                                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                      $::_V6_PRIOR_ = $prior;
                                      my $bool = (!$match != 1);
                                      $pos = $match->to if $bool;
                                      $match;
                                 }
                                 ## </metasyntax>
     ||    ( ( $pos = $pad{I1262} ) && 0 ) )
                             || ( $bool = 1 )
                             ) 
                           &&
                              (   ( $pad{I1263} = $pos or 1 ) &&
                               do{
                    my $from = $pos;
                    my $bool =                                    do { while (
                                        (   ( $pad{I1264} = $pos or 1 ) &&
                                           ## <alt>
                                           (
                                               ( $pad{I1265} = $pos or 1 )
                                               && (
                                               (
                                                  (   ( $pad{I1266} = $pos or 1 ) &&
                                                   ## <constant>
                                                   ( ( substr( $s, $pos, 1 ) eq '\\' )
                                                       ? ( $pos += 1 or 1 )
                                                       : 0
                                                   )
                                                   ## </constant>
     ||    ( ( $pos = $pad{I1266} ) && 0 ) )
                                               &&
                                                  (   ( $pad{I1267} = $pos or 1 ) &&
                                                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                                               $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'special_char'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1267} ) && 0 ) )
                                               )
                                               )
                                             || (
                                               ( ( $bool = 1 ) && ( $pos = $pad{I1265} ) or 1 )
                                               &&                                             (   ( $pad{I1268} = $pos or 1 ) &&
                                                 do{
                    my $match =           ## <variable>
              do {
                our $I1269;
                our $I1269_sizes;
                unless ( $I1269 ) {
                    my $hash = \%Pugs::Grammar::Rule::variables;
                    my %sizes = map { length($_) => 1 } keys %$hash;
                    $I1269_sizes = [ sort { $b <=> $a } keys %sizes ];
                    $I1269 = $hash;
                }
                my $match = 0;
                my $key;
                for ( @$I1269_sizes ) {
                    $key = ( $pos <= length( $s )
                                ? substr( $s, $pos, $_ )
                                : '' );
                    if ( exists $I1269->{$key} ) {
                        #$named{KEY} = $key;
                        #$::_V6_MATCH_ = $m;
                        #print "m: ", Dumper( $::_V6_MATCH_->data )
                        #    if ( $key eq 'until' );
                        $match = Pugs::Runtime::Regex::preprocess_hash( $I1269, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                        last if $match;
                    }
                }
                if ( $match ) {
                    $pos = $match->to;
                    #print "match: $key at $pos = ", Dumper( $match->data );
                    $bool = 1;
                }; # else { $bool = 0 }
                $match;
              }
              ## </variable>
    ;
                    if ( $match ) { push @{$named{'Pugs::Grammar::Rule::variables'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1268} ) && 0 ) )
                                               )
                                             || (
                                               ( ( $bool = 1 ) && ( $pos = $pad{I1265} ) or 1 )
                                               &&                                             (   ( $pad{I1270} = $pos or 1 ) &&
                                                 ## <perl5>
                                                 ( ( substr( $s, $pos ) =~ m/^((?:(?![\>]))\X)/ )
                                                     ? ( $pos += length( $1 ) or 1 )
                                                     : 0
                                                 )
                                                 ## </perl5>
     ||    ( ( $pos = $pad{I1270} ) && 0 ) )
                                             )
                                           )
    ## </alt>
     ||    ( ( $pos = $pad{I1264} ) && 0 ) )) {}; $bool = 1 };
                    my $match = Pugs::Runtime::Match->new(
                        { str => \$s, from => \$from, match => [], named => {}, bool => \1, to => \(0+$pos), capture => undef }
                    ); $named{'str'} = $match;
                    $bool
                } ||    ( ( $pos = $pad{I1263} ) && 0 ) )
                           &&
                              (   ( $pad{I1271} = $pos or 1 ) &&
                               ## <constant>
                               ( ( substr( $s, $pos, 1 ) eq '>' )
                                   ? ( $pos += 1 or 1 )
                                   : 0
                               )
                               ## </constant>
     ||    ( ( $pos = $pad{I1271} ) && 0 ) )
                           &&
                              (   ( $pad{I1272} = $pos or 1 ) &&
                               do { 
                                 local $::_V6_SUCCEED = 1;
                                 $::_V6_MATCH_ = $m;
                                 $m->data->{capture} = \( sub {
                if  ( ${ $_[0]->{qw(ident)} } eq 'before'
                   || ${ $_[0]->{qw(ident)} } eq 'after'
                    ) {
                    return { ${ $_[0]->{qw(ident)} } => {
                        rule     => { metasyntax => {
                            metasyntax => '\'' . ${ $_[0]->{qw(str)} } . '\''
                        } },
                        modifier => ${ $_[0]->{qw(modifier)} },
                    } }
                }
                return { metasyntax => {
                    metasyntax => ${ $_[0]->{qw(ident)} },
                    string   => ${ $_[0]->{qw(str)} },
                    modifier => ${ $_[0]->{qw(modifier)} },
                } }
              }->( $m ) ); 
                                 $bool = $::_V6_SUCCEED;
                                 $::_V6_MATCH_ = $m if $bool; 
                                 return $m if $bool; 
                               } ||    ( ( $pos = $pad{I1272} ) && 0 ) )
                           )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1256} ) or 1 )
                           &&                        (
                              (   ( $pad{I1273} = $pos or 1 ) &&
                               ## <constant>
                               ( ( substr( $s, $pos, 1 ) eq '(' )
                                   ? ( $pos += 1 or 1 )
                                   : 0
                               )
                               ## </constant>
     ||    ( ( $pos = $pad{I1273} ) && 0 ) )
                           &&
                              (   ( $pad{I1274} = $pos or 1 ) &&
                               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                           $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'parsed_code'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1274} ) && 0 ) )
                           &&
                              (   ( $pad{I1275} = $pos or 1 ) &&
                               ## <constant>
                               ( ( substr( $s, $pos, 1 ) eq ')' )
                                   ? ( $pos += 1 or 1 )
                                   : 0
                               )
                               ## </constant>
     ||    ( ( $pos = $pad{I1275} ) && 0 ) )
                           &&
                              (   ( $pad{I1276} = $pos or 1 ) &&
                               ## <constant>
                               ( ( substr( $s, $pos, 1 ) eq '>' )
                                   ? ( $pos += 1 or 1 )
                                   : 0
                               )
                               ## </constant>
     ||    ( ( $pos = $pad{I1276} ) && 0 ) )
                           &&
                              (   ( $pad{I1277} = $pos or 1 ) &&
                               do { 
                                 local $::_V6_SUCCEED = 1;
                                 $::_V6_MATCH_ = $m;
                                 $m->data->{capture} = \( sub { return { call => {
                  method   => ${ $_[0]->{qw(ident)} },
                  params   => ${ $_[0]->{qw(parsed_code)} },
                  modifier => ${ $_[0]->{qw(modifier)} },
              } } }->( $m ) ); 
                                 $bool = $::_V6_SUCCEED;
                                 $::_V6_MATCH_ = $m if $bool; 
                                 return $m if $bool; 
                               } ||    ( ( $pos = $pad{I1277} ) && 0 ) )
                           )
                         )
                       )
    ## </alt>
     ||    ( ( $pos = $pad{I1255} ) && 0 ) )
                   )
                   )
                 || (
                   ( ( $bool = 1 ) && ( $pos = $pad{I1243} ) or 1 )
                   &&                (
                      (   ( $pad{I1278} = $pos or 1 ) &&
                       do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                   $grammar->metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'metasyntax'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1278} ) && 0 ) )
                   &&
                      (   ( $pad{I1279} = $pos or 1 ) &&
                       ## <constant>
                       ( ( substr( $s, $pos, 1 ) eq '>' )
                           ? ( $pos += 1 or 1 )
                           : 0
                       )
                       ## </constant>
     ||    ( ( $pos = $pad{I1279} ) && 0 ) )
                   &&
                      (   ( $pad{I1280} = $pos or 1 ) &&
                       do { 
                         local $::_V6_SUCCEED = 1;
                         $::_V6_MATCH_ = $m;
                         $m->data->{capture} = \( sub { return { metasyntax => {
                  metasyntax => "${ $_[0]->{qw(metasyntax)} }",
                  modifier   => ${ $_[0]->{qw(modifier)} },
            } } }->( $m ) ); 
                         $bool = $::_V6_SUCCEED;
                         $::_V6_MATCH_ = $m if $bool; 
                         return $m if $bool; 
                       } ||    ( ( $pos = $pad{I1280} ) && 0 ) )
                   )
                 )
               )
    ## </alt>
     ||    ( ( $pos = $pad{I1242} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token var1
*var1 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1281} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ident( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'ident'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1281} ) && 0 ) )
           &&
              (   ( $pad{I1282} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '>' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1282} ) && 0 ) )
           &&
              (   ( $pad{I1283} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { match_variable => '$' . $_[0]->{ident} }; }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1283} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $variables{'$<'} = sub { var1($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token var2
*var2 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1284} = $pos or 1 )
               && (
               (
                 (
                  (   ( $pad{I1285} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1285} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1285} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1285} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1286} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { match_variable => '$' . $_[0] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1286} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1284} ) or 1 )
               &&            (
                 (
                  (   ( $pad{I1287} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '^' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1287} ) && 0 ) )
                 || ( $bool = 1 )
                 ) 
               &&
                 (
                  (   ( $pad{I1288} = $pos or 1 ) &&
                     ## <alt>
                     (
                         ( $pad{I1289} = $pos or 1 )
                         && (
                          (   ( $pad{I1290} = $pos or 1 ) &&
                           ## <metasyntax>
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match =
                                       $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           }
                           ## </metasyntax>
     ||    ( ( $pos = $pad{I1290} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1289} ) or 1 )
                         &&                       (   ( $pad{I1291} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '_' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1291} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1289} ) or 1 )
                         &&                      (
                            (   ( $pad{I1292} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1292} ) && 0 ) )
                         &&
                            (   ( $pad{I1293} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1293} ) && 0 ) )
                         )
                       )
                     )
    ## </alt>
     ||    ( ( $pos = $pad{I1288} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1288} = $pos or 1 ) &&
                     ## <alt>
                     (
                         ( $pad{I1289} = $pos or 1 )
                         && (
                          (   ( $pad{I1290} = $pos or 1 ) &&
                           ## <metasyntax>
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match =
                                       $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           }
                           ## </metasyntax>
     ||    ( ( $pos = $pad{I1290} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1289} ) or 1 )
                         &&                       (   ( $pad{I1291} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '_' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1291} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1289} ) or 1 )
                         &&                      (
                            (   ( $pad{I1292} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1292} ) && 0 ) )
                         &&
                            (   ( $pad{I1293} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1293} ) && 0 ) )
                         )
                       )
                     )
    ## </alt>
     ||    ( ( $pos = $pad{I1288} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1294} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { variable => '$' . $_[0] }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1294} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $variables{'$'} = sub { var2($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token var3
*var3 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1295} = $pos or 1 )
               && (
               (
                 (
                  (   ( $pad{I1296} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1296} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1296} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1296} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1297} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { match_variable => '@' . $_[0] } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1297} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1295} ) or 1 )
               &&            (
                 (
                  (   ( $pad{I1298} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '^' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1298} ) && 0 ) )
                 || ( $bool = 1 )
                 ) 
               &&
                 (
                  (   ( $pad{I1299} = $pos or 1 ) &&
                     ## <alt>
                     (
                         ( $pad{I1300} = $pos or 1 )
                         && (
                          (   ( $pad{I1301} = $pos or 1 ) &&
                           ## <metasyntax>
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match =
                                       $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           }
                           ## </metasyntax>
     ||    ( ( $pos = $pad{I1301} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1300} ) or 1 )
                         &&                       (   ( $pad{I1302} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '_' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1302} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1300} ) or 1 )
                         &&                      (
                            (   ( $pad{I1303} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1303} ) && 0 ) )
                         &&
                            (   ( $pad{I1304} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1304} ) && 0 ) )
                         )
                       )
                     )
    ## </alt>
     ||    ( ( $pos = $pad{I1299} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1299} = $pos or 1 ) &&
                     ## <alt>
                     (
                         ( $pad{I1300} = $pos or 1 )
                         && (
                          (   ( $pad{I1301} = $pos or 1 ) &&
                           ## <metasyntax>
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match =
                                       $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           }
                           ## </metasyntax>
     ||    ( ( $pos = $pad{I1301} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1300} ) or 1 )
                         &&                       (   ( $pad{I1302} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '_' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1302} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1300} ) or 1 )
                         &&                      (
                            (   ( $pad{I1303} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1303} ) && 0 ) )
                         &&
                            (   ( $pad{I1304} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1304} ) && 0 ) )
                         )
                       )
                     )
    ## </alt>
     ||    ( ( $pos = $pad{I1299} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1305} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { variable => '@' . $_[0] } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1305} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $variables{'@'} = sub { var3($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token var4
*var4 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1306} = $pos or 1 )
               && (
               (
                 (
                  (   ( $pad{I1307} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1307} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1307} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->digit( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1307} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1308} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { match_variable => '%' . $_[0] } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1308} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1306} ) or 1 )
               &&            (
                 (
                  (   ( $pad{I1309} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '^' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1309} ) && 0 ) )
                 || ( $bool = 1 )
                 ) 
               &&
                 (
                  (   ( $pad{I1310} = $pos or 1 ) &&
                     ## <alt>
                     (
                         ( $pad{I1311} = $pos or 1 )
                         && (
                          (   ( $pad{I1312} = $pos or 1 ) &&
                           ## <metasyntax>
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match =
                                       $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           }
                           ## </metasyntax>
     ||    ( ( $pos = $pad{I1312} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1311} ) or 1 )
                         &&                       (   ( $pad{I1313} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '_' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1313} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1311} ) or 1 )
                         &&                      (
                            (   ( $pad{I1314} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1314} ) && 0 ) )
                         &&
                            (   ( $pad{I1315} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1315} ) && 0 ) )
                         )
                       )
                     )
    ## </alt>
     ||    ( ( $pos = $pad{I1310} ) && 0 ) )
                 && do { while (
                  (   ( $pad{I1310} = $pos or 1 ) &&
                     ## <alt>
                     (
                         ( $pad{I1311} = $pos or 1 )
                         && (
                          (   ( $pad{I1312} = $pos or 1 ) &&
                           ## <metasyntax>
                           do {
                                my $prior = $::_V6_PRIOR_;
                                my $match =
                                       $grammar->alnum( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                $::_V6_PRIOR_ = $prior;
                                my $bool = (!$match != 1);
                                $pos = $match->to if $bool;
                                $match;
                           }
                           ## </metasyntax>
     ||    ( ( $pos = $pad{I1312} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1311} ) or 1 )
                         &&                       (   ( $pad{I1313} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 1 ) eq '_' )
                               ? ( $pos += 1 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1313} ) && 0 ) )
                         )
                       || (
                         ( ( $bool = 1 ) && ( $pos = $pad{I1311} ) or 1 )
                         &&                      (
                            (   ( $pad{I1314} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1314} ) && 0 ) )
                         &&
                            (   ( $pad{I1315} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 1 ) eq ':' )
                                 ? ( $pos += 1 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1315} ) && 0 ) )
                         )
                       )
                     )
    ## </alt>
     ||    ( ( $pos = $pad{I1310} ) && 0 ) )) {}; $bool = 1 }
                 ) 
               &&
                  (   ( $pad{I1316} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { variable => '%' . $_[0] } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1316} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $variables{'%'} = sub { var4($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term1
*term1 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1317} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { metasyntax => { metasyntax => 'null' } } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1317} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'{*}'} = sub { term1($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term2
*term2 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1318} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->literal( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1318} ) && 0 ) )
           &&
              (   ( $pad{I1319} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq chr(39) )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1319} ) && 0 ) )
           &&
              (   ( $pad{I1320} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                    metasyntax => {
                        metasyntax => '\'' . ${$_[0]}
                    }
                };
            }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1320} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'\''} = sub { term2($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term3
*term3 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1321} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1321} ) && 0 ) )
           &&
              (   ( $pad{I1322} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ')' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1322} ) && 0 ) )
           &&
              (   ( $pad{I1323} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { capturing_group => ${ $_[0]->{qw(rule)} } }; }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1323} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'('} = sub { term3($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term4
*term4 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1324} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1324} ) && 0 ) )
           &&
              (   ( $pad{I1325} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 2 ) eq ')>' )
                   ? ( $pos += 2 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1325} ) && 0 ) )
           &&
              (   ( $pad{I1326} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { capture_as_result => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1326} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'<('} = sub { term4($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term5
*term5 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1327} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1327} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1328} = $pos or 1 ) &&
                 do{
                     my $hash = do {
                       my $bool = 1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool = 0 unless
                         (
                            (   ( $pad{I1329} = $pos or 1 ) &&
                             ## <perl5>
                             ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                 ? ( $pos += length( $1 ) or 1 )
                                 : 0
                             )
                             ## </perl5>
     ||    ( ( $pos = $pad{I1329} ) && 0 ) )
                         &&
                            (   ( $pad{I1330} = $pos or 1 ) &&
                             do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                         $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1330} ) && 0 ) )
                         );
                       { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                     };
                     my $bool = ${$hash->{'bool'}};
                     if ( $bool ) {
                         push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                     }
                     $bool;
                 } ||    ( ( $pos = $pad{I1328} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1331} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '>' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1331} ) && 0 ) )
           &&
              (   ( $pad{I1332} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                char_class => [
                    '+' . $_[0]->{qw(char_class)},
                    @{$_[0]->[0]},   # TODO - stringify
                ] }
            }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1332} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'<+'} = sub { term5($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term6
*term6 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1333} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1333} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1334} = $pos or 1 ) &&
                 do{
                     my $hash = do {
                       my $bool = 1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool = 0 unless
                         (
                            (   ( $pad{I1335} = $pos or 1 ) &&
                             ## <perl5>
                             ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                 ? ( $pos += length( $1 ) or 1 )
                                 : 0
                             )
                             ## </perl5>
     ||    ( ( $pos = $pad{I1335} ) && 0 ) )
                         &&
                            (   ( $pad{I1336} = $pos or 1 ) &&
                             do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                         $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1336} ) && 0 ) )
                         );
                       { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                     };
                     my $bool = ${$hash->{'bool'}};
                     if ( $bool ) {
                         push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                     }
                     $bool;
                 } ||    ( ( $pos = $pad{I1334} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1337} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '>' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1337} ) && 0 ) )
           &&
              (   ( $pad{I1338} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                char_class => [
                    '-' . $_[0]->{qw(char_class)},
                    @{$_[0]->[0]},   # TODO - stringify
                ] }
            }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1338} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'<-'} = sub { term6($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term7
*term7 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1339} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->char_range( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_range'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1339} ) && 0 ) )
           &&
              (   ( $pad{I1340} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1340} ) && 0 ) )
           &&
             do { while (
              (   ( $pad{I1341} = $pos or 1 ) &&
                 do{
                     my $hash = do {
                       my $bool = 1;
                       my $from = $pos;
                       my @match;
                       my %named;
                       $bool = 0 unless
                         (
                            (   ( $pad{I1342} = $pos or 1 ) &&
                             ## <perl5>
                             ( ( substr( $s, $pos ) =~ m/^((?:(?=[+-]))\X)/ )
                                 ? ( $pos += length( $1 ) or 1 )
                                 : 0
                             )
                             ## </perl5>
     ||    ( ( $pos = $pad{I1342} ) && 0 ) )
                         &&
                            (   ( $pad{I1343} = $pos or 1 ) &&
                             do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                         $grammar->char_class( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'char_class'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1343} ) && 0 ) )
                         );
                       { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                     };
                     my $bool = ${$hash->{'bool'}};
                     if ( $bool ) {
                         push @{ $match[ 0 ] }, Pugs::Runtime::Match->new( $hash );
                     }
                     $bool;
                 } ||    ( ( $pos = $pad{I1341} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1344} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '>' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1344} ) && 0 ) )
           &&
              (   ( $pad{I1345} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                char_class => [
                    '+[' . $_[0]->{qw(char_range)} . ']',
                    @{$_[0]->[0]},   # TODO - stringify
                ] }
            }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1345} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'<['} = sub { term7($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term8
*term8 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1346} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->parse_metasyntax( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'parse_metasyntax'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1346} ) && 0 ) )
           &&
              (   ( $pad{I1347} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return ${ $_[0]->{qw(parse_metasyntax)} } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1347} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'<'} = sub { term8($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term9
*term9 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1348} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'parsed_code'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1348} ) && 0 ) )
           &&
              (   ( $pad{I1349} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '}' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1349} ) && 0 ) )
           &&
              (   ( $pad{I1350} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { closure => {
                closure => ${ $_[0]->{qw(parsed_code)} },
                modifier => 'plain',
            } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1350} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'{'} = sub { term9($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term10
*term10 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1351} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->special_char( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'special_char'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1351} ) && 0 ) )
           &&
              (   ( $pad{I1352} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return ${ $_[0]->{qw(special_char)} } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1352} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'\\'} = sub { term10($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term11
*term11 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1353} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { 'dot' => 1 } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1353} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'.'} = sub { term11($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term12
*term12 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1354} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1354} ) && 0 ) )
           &&
              (   ( $pad{I1355} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ']' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1355} ) && 0 ) )
           &&
              (   ( $pad{I1356} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return ${ $_[0]->{qw(rule)} } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1356} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'['} = sub { term12($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term13
*term13 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1357} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => ':::' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1357} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':::'} = sub { term13($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term14
*term14 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1358} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => ':?' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1358} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':?'} = sub { term14($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term15
*term15 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1359} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => ':+' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1359} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':+'} = sub { term15($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term16
*term16 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1360} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '::' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1360} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'::'} = sub { term16($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term17
*term17 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1361} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => ':'  } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1361} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':'} = sub { term17($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term18
*term18 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1362} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '$$' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1362} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'$$'} = sub { term18($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term19
*term19 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1363} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '$'  } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1363} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'$'} = sub { term19($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term20
*term20 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1364} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '^^' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1364} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'^^'} = sub { term20($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term21
*term21 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1365} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '^'  } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1365} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'^'} = sub { term21($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term22
*term22 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1366} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '>>' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1366} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'>>'} = sub { term22($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# Code block from grammar spec
 $rule_terms{'»'} = sub { term22($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term24
*term24 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
            (   ( $pad{I1367} = $pos or 1 ) &&
             do { 
               local $::_V6_SUCCEED = 1;
               $::_V6_MATCH_ = $m;
               $m->data->{capture} = \( sub { return { colon => '<<' } }->( $m ) ); 
               $bool = $::_V6_SUCCEED;
               $::_V6_MATCH_ = $m if $bool; 
               return $m if $bool; 
             } ||    ( ( $pos = $pad{I1367} ) && 0 ) );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{'<<'} = sub { term24($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# Code block from grammar spec
 $rule_terms{'«'} = sub { term24($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term26
*term26 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1368} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1368} ) && 0 ) )
           &&
              (   ( $pad{I1369} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1369} ) && 0 ) )
           &&
              (   ( $pad{I1370} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                modifier => {
                    modifier => 'ignorecase',
                    rule => ${ $_[0]->{qw(rule)} }
                    }
                }
            }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1370} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':i'} = sub { term26($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# Code block from grammar spec
 $rule_terms{':ignorecase'} = sub { term26($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term28
*term28 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1371} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1371} ) && 0 ) )
           &&
              (   ( $pad{I1372} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1372} ) && 0 ) )
           &&
              (   ( $pad{I1373} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { modifier => 'sigspace', rule => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1373} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':s'} = sub { term28($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# Code block from grammar spec
 $rule_terms{':sigspace'} = sub { term28($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term30
*term30 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1374} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1374} ) && 0 ) )
           &&
              (   ( $pad{I1375} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1375} ) && 0 ) )
           &&
              (   ( $pad{I1376} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { modifier => 'Perl5', rule => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1376} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':P5'} = sub { term30($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# Code block from grammar spec
 $rule_terms{':Perl5'} = sub { term30($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term32
*term32 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1377} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1377} ) && 0 ) )
           &&
              (   ( $pad{I1378} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1378} ) && 0 ) )
           &&
              (   ( $pad{I1379} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { modifier => 'bytes', rule => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1379} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':bytes'} = sub { term32($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term33
*term33 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1380} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1380} ) && 0 ) )
           &&
              (   ( $pad{I1381} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1381} ) && 0 ) )
           &&
              (   ( $pad{I1382} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { modifier => 'codes', rule => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1382} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':codes'} = sub { term33($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term34
*term34 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1383} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1383} ) && 0 ) )
           &&
              (   ( $pad{I1384} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1384} ) && 0 ) )
           &&
              (   ( $pad{I1385} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { modifier => 'graphs', rule => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1385} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':graphs'} = sub { term34($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term35
*term35 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1386} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1386} ) && 0 ) )
           &&
              (   ( $pad{I1387} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1387} ) && 0 ) )
           &&
              (   ( $pad{I1388} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { modifier => 'langs',  rule => ${ $_[0]->{qw(rule)} } } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1388} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# Code block from grammar spec
 $rule_terms{':langs'} = sub { term35($_[1] || __PACKAGE__, $_[0] || '', $_[2], $_[3]) }; 

# token term
*term =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1389} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1390} = $pos or 1 ) &&
                   do{
                    my $match =           ## <variable>
              do {
                our $I1391;
                our $I1391_sizes;
                unless ( $I1391 ) {
                    my $hash = \%Pugs::Grammar::Rule::variables;
                    my %sizes = map { length($_) => 1 } keys %$hash;
                    $I1391_sizes = [ sort { $b <=> $a } keys %sizes ];
                    $I1391 = $hash;
                }
                my $match = 0;
                my $key;
                for ( @$I1391_sizes ) {
                    $key = ( $pos <= length( $s )
                                ? substr( $s, $pos, $_ )
                                : '' );
                    if ( exists $I1391->{$key} ) {
                        #$named{KEY} = $key;
                        #$::_V6_MATCH_ = $m;
                        #print "m: ", Dumper( $::_V6_MATCH_->data )
                        #    if ( $key eq 'until' );
                        $match = Pugs::Runtime::Regex::preprocess_hash( $I1391, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                        last if $match;
                    }
                }
                if ( $match ) {
                    $pos = $match->to;
                    #print "match: $key at $pos = ", Dumper( $match->data );
                    $bool = 1;
                }; # else { $bool = 0 }
                $match;
              }
              ## </variable>
    ;
                    if ( $match ) { $named{'Pugs::Grammar::Rule::variables'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1390} ) && 0 ) )
               &&
                  (   ( $pad{I1392} = $pos or 1 ) &&
                   ## <alt>
                   (
                       ( $pad{I1393} = $pos or 1 )
                       && (
                       (
                         (
                          (   ( $pad{I1394} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1394} ) && 0 ) )
                         || ( $bool = 1 )
                         ) 
                       &&
                          (   ( $pad{I1395} = $pos or 1 ) &&
                           ## <constant>
                           ( ( substr( $s, $pos, 2 ) eq ':=' )
                               ? ( $pos += 2 or 1 )
                               : 0
                           )
                           ## </constant>
     ||    ( ( $pos = $pad{I1395} ) && 0 ) )
                       &&
                         (
                          (   ( $pad{I1396} = $pos or 1 ) &&
                             ## <metasyntax>
                             do {
                                  my $prior = $::_V6_PRIOR_;
                                  my $match =
                                         $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                  $::_V6_PRIOR_ = $prior;
                                  my $bool = (!$match != 1);
                                  $pos = $match->to if $bool;
                                  $match;
                             }
                             ## </metasyntax>
     ||    ( ( $pos = $pad{I1396} ) && 0 ) )
                         || ( $bool = 1 )
                         ) 
                       &&
                          (   ( $pad{I1397} = $pos or 1 ) &&
                           do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                       $grammar->named_capture_body( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'named_capture_body'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1397} ) && 0 ) )
                       &&
                          (   ( $pad{I1398} = $pos or 1 ) &&
                           do { 
                             local $::_V6_SUCCEED = 1;
                             $::_V6_MATCH_ = $m;
                             $m->data->{capture} = \( sub {
                return { named_capture => {
                    rule =>  ${ $_[0]->{qw(named_capture_body)} },
                    ident => ${ $_[0]->{qw(Pugs::Grammar::Rule::variables)} },
                }, };
              }->( $m ) ); 
                             $bool = $::_V6_SUCCEED;
                             $::_V6_MATCH_ = $m if $bool; 
                             return $m if $bool; 
                           } ||    ( ( $pos = $pad{I1398} ) && 0 ) )
                       )
                       )
                     || (
                       ( ( $bool = 1 ) && ( $pos = $pad{I1393} ) or 1 )
                       &&                     (   ( $pad{I1399} = $pos or 1 ) &&
                         do { 
                           local $::_V6_SUCCEED = 1;
                           $::_V6_MATCH_ = $m;
                           $m->data->{capture} = \( sub {
                return ${ $_[0]->{qw(Pugs::Grammar::Rule::variables)} }
              }->( $m ) ); 
                           $bool = $::_V6_SUCCEED;
                           $::_V6_MATCH_ = $m if $bool; 
                           return $m if $bool; 
                         } ||    ( ( $pos = $pad{I1399} ) && 0 ) )
                     )
                   )
    ## </alt>
     ||    ( ( $pos = $pad{I1392} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1389} ) or 1 )
               &&            (
                  (   ( $pad{I1400} = $pos or 1 ) &&
                   do{
                    my $match =           ## <variable>
              do {
                our $I1401;
                our $I1401_sizes;
                unless ( $I1401 ) {
                    my $hash = \%Pugs::Grammar::Rule::rule_terms;
                    my %sizes = map { length($_) => 1 } keys %$hash;
                    $I1401_sizes = [ sort { $b <=> $a } keys %sizes ];
                    $I1401 = $hash;
                }
                my $match = 0;
                my $key;
                for ( @$I1401_sizes ) {
                    $key = ( $pos <= length( $s )
                                ? substr( $s, $pos, $_ )
                                : '' );
                    if ( exists $I1401->{$key} ) {
                        #$named{KEY} = $key;
                        #$::_V6_MATCH_ = $m;
                        #print "m: ", Dumper( $::_V6_MATCH_->data )
                        #    if ( $key eq 'until' );
                        $match = Pugs::Runtime::Regex::preprocess_hash( $I1401, $key )->( $s, $grammar, { p => ( $pos + $_ ), positionals => [ ], args => { KEY => $key } }, undef );
                        last if $match;
                    }
                }
                if ( $match ) {
                    $pos = $match->to;
                    #print "match: $key at $pos = ", Dumper( $match->data );
                    $bool = 1;
                }; # else { $bool = 0 }
                $match;
              }
              ## </variable>
    ;
                    if ( $match ) { $named{'Pugs::Grammar::Rule::rule_terms'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1400} ) && 0 ) )
               &&
                  (   ( $pad{I1402} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub {
                #print "term: ", Dumper( $_[0]->data );
                return ${ $_[0]->{qw(Pugs::Grammar::Rule::rule_terms)} }
            }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1402} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1389} ) or 1 )
               &&            (
                  (   ( $pad{I1403} = $pos or 1 ) &&
                   ## <perl5>
                   ( ( substr( $s, $pos ) =~ m/^((?:(?![\]\}\)\>\:\?\+\*\|\&]))\X)/ )
                       ? ( $pos += length( $1 ) or 1 )
                       : 0
                   )
                   ## </perl5>
     ||    ( ( $pos = $pad{I1403} ) && 0 ) )
               &&
                  (   ( $pad{I1404} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub {
                #print "constant: ", Dumper( $_[0]->data );
                return { 'constant' => ${$_[0]} }
            }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1404} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token quant
*quant =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1405} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1406} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 2 ) eq '**' )
                       ? ( $pos += 2 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1406} ) && 0 ) )
               &&
                 (
                  (   ( $pad{I1407} = $pos or 1 ) &&
                     ## <metasyntax>
                     do {
                          my $prior = $::_V6_PRIOR_;
                          my $match =
                                 $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                          $::_V6_PRIOR_ = $prior;
                          my $bool = (!$match != 1);
                          $pos = $match->to if $bool;
                          $match;
                     }
                     ## </metasyntax>
     ||    ( ( $pos = $pad{I1407} ) && 0 ) )
                 || ( $bool = 1 )
                 ) 
               &&
                  (   ( $pad{I1408} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '{' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1408} ) && 0 ) )
               &&
                  (   ( $pad{I1409} = $pos or 1 ) &&
                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                               $grammar->parsed_code( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'parsed_code'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1409} ) && 0 ) )
               &&
                  (   ( $pad{I1410} = $pos or 1 ) &&
                   ## <constant>
                   ( ( substr( $s, $pos, 1 ) eq '}' )
                       ? ( $pos += 1 or 1 )
                       : 0
                   )
                   ## </constant>
     ||    ( ( $pos = $pad{I1410} ) && 0 ) )
               &&
                  (   ( $pad{I1411} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return { closure => ${ $_[0]->{qw(parsed_code)} } } }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1411} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1405} ) or 1 )
               &&            (
                (   ( $pad{I1412} = $pos or 1 ) &&
                   ## <perl5>
                   ( ( substr( $s, $pos ) =~ m/^((?:(?=[\?\*\+]))\X)/ )
                       ? ( $pos += length( $1 ) or 1 )
                       : 0
                   )
                   ## </perl5>
     ||    ( ( $pos = $pad{I1412} ) && 0 ) )
               || ( $bool = 1 )
               ) 
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token quantifier
*quantifier =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1413} = $pos or 1 ) &&
               do{
                    my $match = Pugs::Runtime::Match->new( do {
                        my $bool = 1;
                        my $from = $pos;
                        my @match;
                        my %named;
                        $bool = 0 unless                    do{
                           my $hash = do {
                             my $bool = 1;
                             my $from = $pos;
                             my @match;
                             my %named;
                             $bool = 0 unless
                               (
                                (   ( $pad{I1414} = $pos or 1 ) &&
                                   ## <metasyntax>
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match =
                                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   }
                                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1414} ) && 0 ) )
                               || ( $bool = 1 )
                               ) ;
                             { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                           };
                           my $bool = ${$hash->{'bool'}};
                           $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                           $bool;
                       };
                        { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                    } );
                    if ( $match ) { $named{'ws1'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1413} ) && 0 ) )
           &&
              (   ( $pad{I1415} = $pos or 1 ) &&
               do{
                   my $pos1 = $pos;
                   do {
                     my $pos = $pos1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool =                      do{
                             my $pos1 = $pos;
                             do {
                               my $pos = $pos1;
                               my $from = $pos;
                               my @match;
                               my %named;
                               $bool = 0 unless
                                    (   ( $pad{I1416} = $pos or 1 ) &&
                                     ## <perl5>
                                     ( ( substr( $s, $pos ) =~ m/^((?:(?=[\}\]\)]))\X)/ )
                                         ? ( $pos += length( $1 ) or 1 )
                                         : 0
                                     )
                                     ## </perl5>
     ||    ( ( $pos = $pad{I1416} ) && 0 ) );
                               $bool;
                             };
                         } ? 0 : 1;
                     $bool;
                   };
               } ||    ( ( $pos = $pad{I1415} ) && 0 ) )
           &&
              (   ( $pad{I1417} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->term( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'term'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1417} ) && 0 ) )
           &&
              (   ( $pad{I1418} = $pos or 1 ) &&
               do{
                    my $match = Pugs::Runtime::Match->new( do {
                        my $bool = 1;
                        my $from = $pos;
                        my @match;
                        my %named;
                        $bool = 0 unless                    do{
                           my $hash = do {
                             my $bool = 1;
                             my $from = $pos;
                             my @match;
                             my %named;
                             $bool = 0 unless
                               (
                                (   ( $pad{I1419} = $pos or 1 ) &&
                                   ## <metasyntax>
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match =
                                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   }
                                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1419} ) && 0 ) )
                               || ( $bool = 1 )
                               ) ;
                             { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                           };
                           my $bool = ${$hash->{'bool'}};
                           $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                           $bool;
                       };
                        { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                    } );
                    if ( $match ) { $named{'ws2'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1418} ) && 0 ) )
           &&
              (   ( $pad{I1420} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->quant( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'quant'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1420} ) && 0 ) )
           &&
              (   ( $pad{I1421} = $pos or 1 ) &&
               do{
                    my $match = Pugs::Runtime::Match->new( do {
                        my $bool = 1;
                        my $from = $pos;
                        my @match;
                        my %named;
                        $bool = 0 unless                    do{
                           my $hash = do {
                             my $bool = 1;
                             my $from = $pos;
                             my @match;
                             my %named;
                             $bool = 0 unless
                               (
                                (   ( $pad{I1422} = $pos or 1 ) &&
                                   ## <perl5>
                                   ( ( substr( $s, $pos ) =~ m/^((?:(?=[\?\+]))\X)/ )
                                       ? ( $pos += length( $1 ) or 1 )
                                       : 0
                                   )
                                   ## </perl5>
     ||    ( ( $pos = $pad{I1422} ) && 0 ) )
                               || ( $bool = 1 )
                               ) ;
                             { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                           };
                           my $bool = ${$hash->{'bool'}};
                           $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                           $bool;
                       };
                        { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                    } );
                    if ( $match ) { $named{'greedy'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1421} ) && 0 ) )
           &&
              (   ( $pad{I1423} = $pos or 1 ) &&
               do{
                    my $match = Pugs::Runtime::Match->new( do {
                        my $bool = 1;
                        my $from = $pos;
                        my @match;
                        my %named;
                        $bool = 0 unless                    do{
                           my $hash = do {
                             my $bool = 1;
                             my $from = $pos;
                             my @match;
                             my %named;
                             $bool = 0 unless
                               (
                                (   ( $pad{I1424} = $pos or 1 ) &&
                                   ## <metasyntax>
                                   do {
                                        my $prior = $::_V6_PRIOR_;
                                        my $match =
                                               $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                                        $::_V6_PRIOR_ = $prior;
                                        my $bool = (!$match != 1);
                                        $pos = $match->to if $bool;
                                        $match;
                                   }
                                   ## </metasyntax>
     ||    ( ( $pos = $pad{I1424} ) && 0 ) )
                               || ( $bool = 1 )
                               ) ;
                             { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                           };
                           my $bool = ${$hash->{'bool'}};
                           $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                           $bool;
                       };
                        { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                    } );
                    if ( $match ) { $named{'ws3'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1423} ) && 0 ) )
           &&
              (   ( $pad{I1425} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub {
          if (
                   ${$_[0]->{'quant'}}  eq ''
                && ${$_[0]->{'greedy'}} eq ''
                && ${$_[0]->{'ws1'}}    eq ''
                && ${$_[0]->{'ws2'}}    eq ''
                && ${$_[0]->{'ws3'}}    eq ''
          ) {
              return ${$_[0]->{'term'}};
          }
          return {
            quant => {
                term    => ${$_[0]->{'term'}},
                quant   => ${$_[0]->{'quant'}},
                greedy  => ${$_[0]->{'greedy'}},
                ws1     => ${$_[0]->{'ws1'}},
                ws2     => ${$_[0]->{'ws2'}},
                ws3     => ${$_[0]->{'ws3'}},
            } }
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1425} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token concat
*concat =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1426} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->quantifier( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'quantifier'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1426} ) && 0 ) )
             && do { while (
              (   ( $pad{I1426} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->quantifier( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'quantifier'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1426} ) && 0 ) )) {}; $bool = 1 }
             ) 
           &&
              (   ( $pad{I1427} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub {
            my @a = map {  $_->()  }  @{ $::_V6_MATCH_->{'quantifier'} };
            return { concat => \@a }  if scalar @a > 1;
            return $a[0];
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1427} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token conjunctive1
*conjunctive1 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1428} = $pos or 1 ) &&
                 (
                   (
                    (   ( $pad{I1429} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1429} ) && 0 ) )
                   || ( $bool = 1 )
                   ) 
                 &&
                    (   ( $pad{I1430} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '&' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1430} ) && 0 ) )
                 &&
                    (   ( $pad{I1431} = $pos or 1 ) &&
                     do{
                         my $pos1 = $pos;
                         do {
                           my $pos = $pos1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool =                            do{
                                   my $pos1 = $pos;
                                   do {
                                     my $pos = $pos1;
                                     my $from = $pos;
                                     my @match;
                                     my %named;
                                     $bool = 0 unless
                                          (   ( $pad{I1432} = $pos or 1 ) &&
                                           ## <constant>
                                           ( ( substr( $s, $pos, 1 ) eq '&' )
                                               ? ( $pos += 1 or 1 )
                                               : 0
                                           )
                                           ## </constant>
     ||    ( ( $pos = $pad{I1432} ) && 0 ) );
                                     $bool;
                                   };
                               } ? 0 : 1;
                           $bool;
                         };
                     } ||    ( ( $pos = $pad{I1431} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1428} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
               (   ( $pad{I1433} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->concat( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'concat'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1433} ) && 0 ) )
             ) 
           &&
             do { while (
              (   ( $pad{I1434} = $pos or 1 ) &&
                 (
                    (   ( $pad{I1435} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '&' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1435} ) && 0 ) )
                 &&
                    (   ( $pad{I1436} = $pos or 1 ) &&
                     do{
                         my $pos1 = $pos;
                         do {
                           my $pos = $pos1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool =                            do{
                                   my $pos1 = $pos;
                                   do {
                                     my $pos = $pos1;
                                     my $from = $pos;
                                     my @match;
                                     my %named;
                                     $bool = 0 unless
                                          (   ( $pad{I1437} = $pos or 1 ) &&
                                           ## <constant>
                                           ( ( substr( $s, $pos, 1 ) eq '&' )
                                               ? ( $pos += 1 or 1 )
                                               : 0
                                           )
                                           ## </constant>
     ||    ( ( $pos = $pad{I1437} ) && 0 ) );
                                     $bool;
                                   };
                               } ? 0 : 1;
                           $bool;
                         };
                     } ||    ( ( $pos = $pad{I1436} ) && 0 ) )
                 &&
                    (   ( $pad{I1438} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->concat( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'concat'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1438} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1434} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1439} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub {
            my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'concat'} };
            return { conjunctive1 => \@a }  if scalar @a > 1;
            return $a[0];
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1439} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token disjunctive1
*disjunctive1 =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1440} = $pos or 1 ) &&
                 (
                   (
                    (   ( $pad{I1441} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1441} ) && 0 ) )
                   || ( $bool = 1 )
                   ) 
                 &&
                    (   ( $pad{I1442} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '|' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1442} ) && 0 ) )
                 &&
                    (   ( $pad{I1443} = $pos or 1 ) &&
                     do{
                         my $pos1 = $pos;
                         do {
                           my $pos = $pos1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool =                            do{
                                   my $pos1 = $pos;
                                   do {
                                     my $pos = $pos1;
                                     my $from = $pos;
                                     my @match;
                                     my %named;
                                     $bool = 0 unless
                                          (   ( $pad{I1444} = $pos or 1 ) &&
                                           ## <constant>
                                           ( ( substr( $s, $pos, 1 ) eq '|' )
                                               ? ( $pos += 1 or 1 )
                                               : 0
                                           )
                                           ## </constant>
     ||    ( ( $pos = $pad{I1444} ) && 0 ) );
                                     $bool;
                                   };
                               } ? 0 : 1;
                           $bool;
                         };
                     } ||    ( ( $pos = $pad{I1443} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1440} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
               (   ( $pad{I1445} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->conjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'conjunctive1'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1445} ) && 0 ) )
             ) 
           &&
             do { while (
              (   ( $pad{I1446} = $pos or 1 ) &&
                 (
                    (   ( $pad{I1447} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '|' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1447} ) && 0 ) )
                 &&
                    (   ( $pad{I1448} = $pos or 1 ) &&
                     do{
                         my $pos1 = $pos;
                         do {
                           my $pos = $pos1;
                           my $from = $pos;
                           my @match;
                           my %named;
                           $bool =                            do{
                                   my $pos1 = $pos;
                                   do {
                                     my $pos = $pos1;
                                     my $from = $pos;
                                     my @match;
                                     my %named;
                                     $bool = 0 unless
                                          (   ( $pad{I1449} = $pos or 1 ) &&
                                           ## <constant>
                                           ( ( substr( $s, $pos, 1 ) eq '|' )
                                               ? ( $pos += 1 or 1 )
                                               : 0
                                           )
                                           ## </constant>
     ||    ( ( $pos = $pad{I1449} ) && 0 ) );
                                     $bool;
                                   };
                               } ? 0 : 1;
                           $bool;
                         };
                     } ||    ( ( $pos = $pad{I1448} ) && 0 ) )
                 &&
                    (   ( $pad{I1450} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->conjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'conjunctive1'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1450} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1446} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1451} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub {
            my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'conjunctive1'} };
            return { alt1 => \@a }  if scalar @a > 1;
            return $a[0];
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1451} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token conjunctive
*conjunctive =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1452} = $pos or 1 ) &&
                 (
                   (
                    (   ( $pad{I1453} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1453} ) && 0 ) )
                   || ( $bool = 1 )
                   ) 
                 &&
                    (   ( $pad{I1454} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '&' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1454} ) && 0 ) )
                 &&
                    (   ( $pad{I1455} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '&' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1455} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1452} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
               (   ( $pad{I1456} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->disjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'disjunctive1'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1456} ) && 0 ) )
             ) 
           &&
             do { while (
              (   ( $pad{I1457} = $pos or 1 ) &&
                 (
                    (   ( $pad{I1458} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '&' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1458} ) && 0 ) )
                 &&
                    (   ( $pad{I1459} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '&' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1459} ) && 0 ) )
                 &&
                    (   ( $pad{I1460} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->disjunctive1( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'disjunctive1'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1460} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1457} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1461} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub {
            my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'disjunctive1'} };
            return { conjunctive => \@a }  if scalar @a > 1;
            return $a[0];
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1461} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token rule
*rule =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1462} = $pos or 1 ) &&
                 (
                   (
                    (   ( $pad{I1463} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1463} ) && 0 ) )
                   || ( $bool = 1 )
                   ) 
                 &&
                    (   ( $pad{I1464} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '|' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1464} ) && 0 ) )
                 &&
                    (   ( $pad{I1465} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '|' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1465} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1462} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             (
               (   ( $pad{I1466} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->conjunctive( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'conjunctive'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1466} ) && 0 ) )
             ) 
           &&
             do { while (
              (   ( $pad{I1467} = $pos or 1 ) &&
                 (
                    (   ( $pad{I1468} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '|' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1468} ) && 0 ) )
                 &&
                    (   ( $pad{I1469} = $pos or 1 ) &&
                     ## <constant>
                     ( ( substr( $s, $pos, 1 ) eq '|' )
                         ? ( $pos += 1 or 1 )
                         : 0
                     )
                     ## </constant>
     ||    ( ( $pos = $pad{I1469} ) && 0 ) )
                 &&
                    (   ( $pad{I1470} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->conjunctive( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'conjunctive'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1470} ) && 0 ) )
                 ) ||    ( ( $pos = $pad{I1467} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1471} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub {
            my @a = map {  $$_  }  @{ $::_V6_MATCH_->{'conjunctive'} };
            return { alt => \@a }  if scalar @a > 1;
            return $a[0];
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1471} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token named_regex
*named_regex =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1472} = $pos or 1 ) &&
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       ## <alt>
                       (
                           ( $pad{I1473} = $pos or 1 )
                           && (
                            (   ( $pad{I1474} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 5 ) eq 'token' )
                                 ? ( $pos += 5 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1474} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1473} ) or 1 )
                           &&                         (   ( $pad{I1475} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 5 ) eq 'regex' )
                                 ? ( $pos += 5 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1475} ) && 0 ) )
                           )
                         || (
                           ( ( $bool = 1 ) && ( $pos = $pad{I1473} ) or 1 )
                           &&                         (   ( $pad{I1476} = $pos or 1 ) &&
                             ## <constant>
                             ( ( substr( $s, $pos, 4 ) eq 'rule' )
                                 ? ( $pos += 4 or 1 )
                                 : 0
                             )
                             ## </constant>
     ||    ( ( $pos = $pad{I1476} ) && 0 ) )
                         )
                       )
    ## </alt>
    ;
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1472} ) && 0 ) )
           &&
              (   ( $pad{I1477} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1477} ) && 0 ) )
           &&
              (   ( $pad{I1478} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ident( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'ident'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1478} ) && 0 ) )
           &&
             (
              (   ( $pad{I1479} = $pos or 1 ) &&
                 ## <metasyntax>
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match =
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 }
                 ## </metasyntax>
     ||    ( ( $pos = $pad{I1479} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
              (   ( $pad{I1480} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '{' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1480} ) && 0 ) )
           &&
             (
              (   ( $pad{I1481} = $pos or 1 ) &&
                 ## <metasyntax>
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match =
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 }
                 ## </metasyntax>
     ||    ( ( $pos = $pad{I1481} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
              (   ( $pad{I1482} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->rule( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'rule'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1482} ) && 0 ) )
           &&
              (   ( $pad{I1483} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq '}' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1483} ) && 0 ) )
           &&
             (
              (   ( $pad{I1484} = $pos or 1 ) &&
                 ## <constant>
                 ( ( substr( $s, $pos, 1 ) eq ';' )
                     ? ( $pos += 1 or 1 )
                     : 0
                 )
                 ## </constant>
     ||    ( ( $pos = $pad{I1484} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
              (   ( $pad{I1485} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                type => ${ $_[0]->[0] },
                name => ${ $_[0]->{qw(ident)} },
                ast => ${ $_[0]->{qw(rule)} }
            };
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1485} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token verbatim
*verbatim =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
              (   ( $pad{I1486} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 2 ) eq '%{' )
                   ? ( $pos += 2 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1486} ) && 0 ) )
           &&
              (   ( $pad{I1487} = $pos or 1 ) &&
               do{
                   my $hash = do {
                     my $bool = 1;
                     my $from = $pos;
                     my @match;
                     my %named;
                     $bool = 0 unless
                       do { while (
                        (   ( $pad{I1488} = $pos or 1 ) &&
                           (
                              (   ( $pad{I1489} = $pos or 1 ) &&
                               do{
                                   my $pos1 = $pos;
                                   do {
                                     my $pos = $pos1;
                                     my $from = $pos;
                                     my @match;
                                     my %named;
                                     $bool =                                      do{
                                             my $pos1 = $pos;
                                             do {
                                               my $pos = $pos1;
                                               my $from = $pos;
                                               my @match;
                                               my %named;
                                               $bool = 0 unless
                                                   ## <constant>
                                                   ( ( substr( $s, $pos, 2 ) eq '%}' )
                                                       ? ( $pos += 2 or 1 )
                                                       : 0
                                                   )
                                                   ## </constant>
    ;
                                               $bool;
                                             };
                                         } ? 0 : 1;
                                     $bool;
                                   };
                               } ||    ( ( $pos = $pad{I1489} ) && 0 ) )
                           &&
                              (   ( $pad{I1490} = $pos or 1 ) &&
                               ( substr( $s, $pos++, 1 ) ne '' ) ||    ( ( $pos = $pad{I1490} ) && 0 ) )
                           ) ||    ( ( $pos = $pad{I1488} ) && 0 ) )) {}; $bool = 1 };
                     { str => \$s, from => \$from, match => \@match, named => \%named, bool => \$bool, to => \(0+$pos), capture => undef }
                   };
                   my $bool = ${$hash->{'bool'}};
                   $match[ 0 ] = Pugs::Runtime::Match->new( $hash );
                   $bool;
               } ||    ( ( $pos = $pad{I1487} ) && 0 ) )
           &&
              (   ( $pad{I1491} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 2 ) eq '%}' )
                   ? ( $pos += 2 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1491} ) && 0 ) )
           &&
              (   ( $pad{I1492} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                type => 'block',
                value => ${ $_[0]->[0] }
            };
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1492} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token item
*item =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           ## <alt>
           (
               ( $pad{I1493} = $pos or 1 )
               && (
               (
                  (   ( $pad{I1494} = $pos or 1 ) &&
                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                               $grammar->verbatim( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'verbatim'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1494} ) && 0 ) )
               &&
                  (   ( $pad{I1495} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return ${ $_[0]->{qw(verbatim)} }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1495} ) && 0 ) )
               )
               )
             || (
               ( ( $bool = 1 ) && ( $pos = $pad{I1493} ) or 1 )
               &&            (
                  (   ( $pad{I1496} = $pos or 1 ) &&
                   do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                               $grammar->named_regex( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'named_regex'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1496} ) && 0 ) )
               &&
                  (   ( $pad{I1497} = $pos or 1 ) &&
                   do { 
                     local $::_V6_SUCCEED = 1;
                     $::_V6_MATCH_ = $m;
                     $m->data->{capture} = \( sub { return ${ $_[0]->{qw(named_regex)} }; }->( $m ) ); 
                     $bool = $::_V6_SUCCEED;
                     $::_V6_MATCH_ = $m if $bool; 
                     return $m if $bool; 
                   } ||    ( ( $pos = $pad{I1497} ) && 0 ) )
               )
             )
           )
    ## </alt>
    ;
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token grammar
*grammar =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1498} = $pos or 1 ) &&
                 ## <metasyntax>
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match =
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 }
                 ## </metasyntax>
     ||    ( ( $pos = $pad{I1498} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
              (   ( $pad{I1499} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 7 ) eq 'grammar' )
                   ? ( $pos += 7 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1499} ) && 0 ) )
           &&
              (   ( $pad{I1500} = $pos or 1 ) &&
               ## <metasyntax>
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    my $bool = (!$match != 1);
                    $pos = $match->to if $bool;
                    $match;
               }
               ## </metasyntax>
     ||    ( ( $pos = $pad{I1500} ) && 0 ) )
           &&
              (   ( $pad{I1501} = $pos or 1 ) &&
               do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                           $grammar->ident( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { $named{'ident'} = $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1501} ) && 0 ) )
           &&
             (
              (   ( $pad{I1502} = $pos or 1 ) &&
                 ## <metasyntax>
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match =
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 }
                 ## </metasyntax>
     ||    ( ( $pos = $pad{I1502} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
              (   ( $pad{I1503} = $pos or 1 ) &&
               ## <constant>
               ( ( substr( $s, $pos, 1 ) eq ';' )
                   ? ( $pos += 1 or 1 )
                   : 0
               )
               ## </constant>
     ||    ( ( $pos = $pad{I1503} ) && 0 ) )
           &&
             (
              (   ( $pad{I1504} = $pos or 1 ) &&
                 ## <metasyntax>
                 do {
                      my $prior = $::_V6_PRIOR_;
                      my $match =
                             $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                      $::_V6_PRIOR_ = $prior;
                      my $bool = (!$match != 1);
                      $pos = $match->to if $bool;
                      $match;
                 }
                 ## </metasyntax>
     ||    ( ( $pos = $pad{I1504} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             do { while (
              (   ( $pad{I1505} = $pos or 1 ) &&
                 (
                    (   ( $pad{I1506} = $pos or 1 ) &&
                     do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                                 $grammar->item( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'item'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1506} ) && 0 ) )
                 &&
                   (
                    (   ( $pad{I1507} = $pos or 1 ) &&
                       ## <metasyntax>
                       do {
                            my $prior = $::_V6_PRIOR_;
                            my $match =
                                   $grammar->ws( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                            $::_V6_PRIOR_ = $prior;
                            my $bool = (!$match != 1);
                            $pos = $match->to if $bool;
                            $match;
                       }
                       ## </metasyntax>
     ||    ( ( $pos = $pad{I1507} ) && 0 ) )
                   || ( $bool = 1 )
                   ) 
                 ) ||    ( ( $pos = $pad{I1505} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1508} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return { ${ $_[0]->{qw(ident)} } => $_[0]->{qw(item)} } }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1508} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;

# token spec
*spec =
    ## <global>
    ## sigspace: 0
    ## ratchet: 1
    do { my $rule; $rule = sub {
      my $grammar = $_[0];
      my $s = $_[1];
      $_[3] = $_[2] unless defined $_[3]; # backwards compat
      no warnings 'substr', 'uninitialized', 'syntax';
      my %pad;
      my $m;
      for my $pos ( defined $_[3]{p} && ! $_[3]{continue}
            ? $_[3]{p}
            : ( ( $_[3]{p} || pos($_[1]) || 0 ) .. length( $s ) ) ) {
        my %index;
        my @match;
        my %named;
        my $bool = 1;
        $named{KEY} = $_[3]{KEY} if exists $_[3]{KEY};
        $m = Pugs::Runtime::Match->new( {
          str => \$s, from => \(0+$pos), to => \($pos),
          bool => \$bool, match => \@match, named => \%named, capture => undef,
        } );
        {
          my $prior = $::_V6_PRIOR_;
          local $::_V6_PRIOR_ = $prior;
          $bool = 0 unless
           (
             (
              (   ( $pad{I1509} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->verbatim( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'verbatim'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1509} ) && 0 ) )
             || ( $bool = 1 )
             ) 
           &&
             do { while (
              (   ( $pad{I1510} = $pos or 1 ) &&
                 do {
                    my $prior = $::_V6_PRIOR_;
                    my $match =
                             $grammar->grammar( $s, { p => $pos, positionals => [  ], args => {}, }, undef );
                    $::_V6_PRIOR_ = $prior;
                    if ( $match ) { push @{$named{'grammar'}}, $match;
                        $pos = $match->to;
                        1
                    }
                    else { 0 }
                } ||    ( ( $pos = $pad{I1510} ) && 0 ) )) {}; $bool = 1 }
           &&
              (   ( $pad{I1511} = $pos or 1 ) &&
               do { 
                 local $::_V6_SUCCEED = 1;
                 $::_V6_MATCH_ = $m;
                 $m->data->{capture} = \( sub { return {
                block => $_[0]->{qw(verbatim)},
                'grammar' => $_[0]->{qw(grammar)} }
        }->( $m ) ); 
                 $bool = $::_V6_SUCCEED;
                 $::_V6_MATCH_ = $m if $bool; 
                 return $m if $bool; 
               } ||    ( ( $pos = $pad{I1511} ) && 0 ) )
           );
        }
        if ( $bool ) {
          my $prior = $::_V6_PRIOR_;
          $::_V6_PRIOR_ = sub {
            local $main::_V6_PRIOR_ = $prior;
            $rule->(@_);
          };
          #warn "pos2 = ", $pos, "\n";
          pos($_[1]) = $pos if $_[3]{continue};
          last;
        }
      } # /for
      $::_V6_MATCH_ = $m;
      return $m;
    } }
    ## </global>
;



1;

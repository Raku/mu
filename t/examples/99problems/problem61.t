use v6-alpha;
use Test;
plan 1;

# P61A (*) Collect the leaves of a binary tree in a list
# 
# A leaf is a node with no successors. Write a predicate leaves/2 to collect them
# in a list.
# 
# % leaves(T,S) :- S is the list of all leaves of the binary tree T

if 1 {
    skip 1, "Test(s) not yet written: A (*) Collect the leaves of a binary tree in a list";
}
else {
    ok 1, 'A (*) Collect the leaves of a binary tree in a list';
}
